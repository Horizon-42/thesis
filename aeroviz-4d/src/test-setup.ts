// This file runs before every test suite.
// Use it to configure global test utilities or mock browser APIs.

// Example: if a module imports Cesium (which relies on WebGL),
// you would mock it here so unit tests don't crash in Node.js/jsdom.
// For now it is intentionally empty — add mocks as needed.
