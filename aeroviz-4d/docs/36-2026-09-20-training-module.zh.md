# Training 模块

前端的 **Training** 任务页（顶栏 Observe 右边）把第二层模型的"语言"画出来：一架到达航班被读成了
哪一句指令，这句话里每个词允许飞机在哪里（词的包络，也就是"飞行盒"），航迹有没有待在里面。
它不训练模型，不写磁盘；除了 §4.7 的"实时执行器"（选中一个词，后端现飞这一段）之外不调后端，只读导出好的 JSON。

词表本身的定义见 `4dTrajectory/ts_transformer/docs/2026-09-23_instruction_vocabulary_design.zh.md`
（下文简称"词表设计"）。本文只写前端这一侧：数据怎么来、每个词画成什么、界面上有什么。

---

## 0 状态

| 项 | 内容 |
|---|---|
| 词表 | 读法 `instruction-v3`（航向词按步读，词表设计 §10.1）。前端钉住的规格 sha `TRAINING_SPEC_SHA256` 是 **`145d6911e75b`**（2026-09-25：按运行日划分后在新训练集上重新测量，产物 `4dTrajectory/outputs/POOLED/instruction_language/v4_20260924/`）——现在的执行器代码只飞这份产物（执行器规格 `v6_20260924`）。按航班划分的 `v3_20260924`（规格 `0b4ea75be36d`）上导出的 `instruction_v3` 集合和画在它上面的 `executor_v5_20260924` 叠加层从此按规格 sha 拒读 |
| 导出 | `python run_ts.py instruction_training_export`（`4dTrajectory/ts_transformer/experiments/instruction_training_export.py`）；包络全部来自 `instructions/display.py`。样本格式 `aeroviz-training-sample-v7`，执行器叠加层 `aeroviz-training-executor-v2`，先验叠加层 `aeroviz-training-prior-v2`（2026-09-24 先验第二版：形状没变，第 0 步已知的五列是点质量、读数口径是第 0 步只算跑道） |
| 前端代码 | `src/data/trainingSample.ts`（数据契约与读取）、`src/data/trainingOverlays.ts`（叠加层的契约与读取）、`src/data/trainingAutopilot.ts`（实时执行器的契约与请求，§4.7）、`src/components/Training{Panel,SentenceBar,ReadbackWindow,PriorWindow,Results,Legend,AutopilotCard}.tsx`、`src/hooks/useTrainingTrackLayer.ts`、`src/hooks/useTrainingOverlays.ts`、`src/hooks/useTrainingAutopilot.ts`、`src/utils/trainingWordColors.ts`、`src/utils/checkPublication.ts` 与 `scripts/check_publication.ts`；共享状态在 `src/context/AppContext.tsx` |
| 分支 | v3 的导出与界面在 `dev-vocab-v3-frontend`（从 `dev-vocab-v3` 的 `ba77d65c` 分出）：代码与测试 `8f7a8640`，文档在其后一个提交；用户合并 |
| 发布 | 现行集合 **`instruction_v3_day_split`**（2026-09-25，五个机场各 40 架新验证集航班，直线进近 / 被引导各 20，从 `v4_20260924` 导出）。上面还没有叠加层（执行器 v6 的验证集回放、先验第三版的导出都没发布到它上面）；实时执行器（§4.7）不需要叠加层。更早的集合与叠加层按名字拒读，发布记录见仓库 `docs/CHANGELOG.md` |
| 实时执行器 | 后端 `POST /autopilot/segment`（`aeroviz_backend/autopilot_segment.py`）：在句子条上点一个词，执行器从观测飞机说这个词时的状态飞这个词的一段（到它的包络结束之处：同列下一个词说出的地方，航向词再往后一个提前量；到了句子末尾就飞到落地），只判这个词。执行器代码一行不改，用它的单步接口 `Executor` 一个控制周期一个周期地飞，到段尾就停；每次都重新飞，不读任何回放或叠加层（§4.7）。用哪份执行器规格由后端按"现在的执行器代码接受的那一份"自动找：现在是 `v7_20260925`。界面上并排给出模拟飞行时间与计算用时。2026-09-25 在五个机场 200 架航班上逐段试飞 3,032 段：无异常，1,904 段飞到段尾、1,128 段落地；唯一的拒绝是没有机型动力学的航班（252 段，正式回放同样不飞）；每段耗时中位数 0.39 s、最长 3.6 s。单步飞法与"飞满时限再截断"逐位相同（216 段，状态与结束周期一致） |
| 测试 | Python：`tests/test_instruction_training_export.py` 11 条、`tests/test_training_overlays.py` 12 条（合成航班，写入全在 `tmp_path`，与 TypeScript 常量逐字比对）；ts 全套 1360 通过、14 条失败（都在 `test_autopilot.py`，是另一个会话正在改的执行器，改之前同样失败）；前端 Vitest 90 个文件 710 条；`tsc`、`npm run build`、`npm run typecheck:scripts` 无错。没有浏览器核对（没有 v3 数据可看） |
| 盘上的旧集合 | `box`、`box_v3`、`prior_s1337_val`、`prior_s2024_val`、`instruction_v1`、`instruction_v2`（五个机场都有），`v15_nomerge_noposition`（只有 KRDU）。它们属于别的词表，仍在 `index.json` 里列着，界面按名字拒读、不下载。删不删由用户决定 |

---

## 1 这个模块回答什么

1. **这架飞机被读成了哪一句话**：每 2 s 一步，六列词（跑道、进近、航向、高度、下降角、速度，顺序与
   `instructions/words.COLUMNS` 相同）。第 0 步六列都有词；之后绝大多数步什么也不说。
2. **每个词允许什么**：航向词说"提前量之后航迹在哪个 5° 格子里"，它的包络是一条航向带——从说出后一个提前量到
   下一个航向词，航迹每一行都要在目标 ± 容差以内；截获转弯和截获后的走廊，高度词的管子，速度词的过渡和速度带。
3. **航迹有没有待在里面**：每一项都是标注器自己的判定（`Reading.checks`），前端只显示，不重算。

放在 Observe 旁边，是因为两页看的是同一批到达航班：Observe 看"飞机怎么飞的"，Training 看"这条航迹
被读成了什么"。Training 不加载 Observe 的 CZML，也不动共享的 Cesium 时钟。

---

## 2 数据

### 2.1 谁写、写到哪、抽哪些航班

```bash
python run_ts.py instruction_training_export \
    --dir 4dTrajectory/outputs/POOLED/instruction_language/<一份 instruction-v3 产物> \
    --airports-root aeroviz-4d/public/data/airports \
    --airport KMSY --airport KRDU --airport KSJC --airport KSMF --airport KSTL
```

- 每个机场写 `training/instruction_v3/sample.json`（集合名默认取读法），并把这个集合加进该机场的
  `training/index.json`。目录已存在、或索引里已有同名集合，就拒绝，什么都不写；索引里别的集合原样保留。
  五个机场全部建好之后才开始写，中途任何一处拒绝，盘上什么都不留。
- **只抽 val**（train 留给先验，test 不打开）。每个机场：把该机场已标注的 val 航班按种子 1337 打乱，
  依次读，直到直线进近和雷达引导各 20 架（分组用 `readout.flight_record`：截获前从词到词、再从最后一个词到航道
  的航向转角加起来不到 90° 为直线进近）。候选池多大、读了多少架、规则是什么，都写在文件和索引里。
- **每一架读过的航班都用 `read_flight` 重读一遍，必须和产物里存的句子完全相同**（六列词、跑道、截获行、
  许可行、"未指定"行），否则导出停止并说出是哪一架哪一格。导出器**不**要求当前代码的标注器 sha 等于
  产物的：产物在无关的代码改动之后仍然能导出，而重读保证显示的就是存下的那一句。

v2 那次导出（2026-09-24，样本 v5）每个机场的候选池、读了多少架、转弯和保持段的数字记在 `docs/CHANGELOG.md`；
v3 导出后这里写 v3 的数字。

### 2.2 `index.json`

索引的格式（`aeroviz-training-index-v1`）不随词表变：新旧集合都列在里面，每一条写着自己的读法
（`readingRule`）和规格 sha（`vocabularySha256`）。所以界面**不用下载任何文件**就知道哪个集合能读：
读法是 `instruction-v3`、规格是 `TRAINING_SPEC_SHA256`、类型是 `vocabulary-readback` 的才打开，其余按名字拒读。
`runwaySha256` 是该机场几何的 sha：候选跑道（跑道列的类别就是它们），加上落地判定要读的全部跑道端。

### 2.3 `sample.json`（`aeroviz-training-sample-v7`）

顶层：

- `vocabulary`：读法、规格 sha、标注器 sha、六列的名字和顺序、"不变"（−1）、步长、平滑窗口；**每一列
  的类别都写成表**（航向 72 个角度、高度 181 个目标加"下降至落地"、6 个下降角类及各自的名义角度和范围、
  速度 47 个目标加"未指定"、进近 3 类），界面查表出文字，不自己换算；还有画图和说明要用的数：
  - 航向：提前量 `headingLeadS`（4 s）和同一个数换成的步数 `headingLeadRows`（2，由规格的 `rows_exact` 换算，界面只核对
    两者一致）、容差 ±4.5°；
  - 截获转弯：开始的判据（朝航道转得比 `turnOnsetRateDegS` 快）、转弯率 0.5–4.7°/s（下限只管 10° 以上的转弯）、
    坡度不超过 32°；自行切入的角度 30°；
  - 落地：越过入口时离中线不超过 1000 m（有平行跑道时取到它间距的一半，两条跑道航向差 5° 以内算平行），
    离入口的高度不超过 100 m；
  - 走廊 20 m / 0.45° / ±2°，高度 ±25 m，速度 ±5 m/s，加速度 1.7 m/s²。
- `candidates`：本机场全部候选跑道，每条有入口、真航向、入口高程、长度、**这条跑道自己的落地横向界限**
  （例如 KSJC 平行跑道之间只有 106.7 m），以及画好的跑道线段和延长中线（机场坐标系的米和经纬度各一份）。
- `cohort`：抽样规则与候选池、读过的数量。

每架航班：

- 身份：`datasetId`、`flightKey`（`id_跑道_icao24_落地时刻`）、呼号、机型、跑道、分组、步数；
  截获行、许可行、"未指定"行、截获时离入口的距离。
- `signals`：每步一行。时间、机场坐标系的东/北（米）、经纬度、椭球高（给三维用）、原始信号（航迹、
  高度、地速、垂直速率）、**平滑后的信号**（标注器实际读的：航迹 6 s、高度 10 s、地速 10 s）和由平滑
  地速积分出的水平距离、相对所指跑道的沿航道距离与偏离。两种信号都给，因为平滑本身是读法的一部分。
- `words`：发令事件（行、列、值、为什么发：进入时已生效 / 按步读出的航向 / 许可 / 新目标 / 两段保持之间 /
  新下降角 / 速度交给飞行员），以及每一列每一步生效的值。后者必须等于前者逐行往后填，读取时检查。
- `envelopes`：每个词的包络与判定，见 §3。航向词每个一条：被判的行（`firstRow` 到 `stopRow` 之前）、目标在航向图
  展开分支上的值和航向带（`targetOnTrackDeg`、`bandDeg`）、每一行在不在带里（`inside`）、标注器的判定（行数和
  在带里的行数）。截获转弯：起止行（许可行、截获行）、航道在展开分支上的值、标注器的判定。v7 起不再有转弯区、
  两条转弯路径、平行四边形、航向随时间的楔形、保持漏斗、拆分的几份和"补的切入航向"。

单位只有米、米/秒、度、秒。高度是几何 MSL；只有 `altitudeHaeM`、`lowerHaeM`、`upperHaeM` 三处是椭球高，
由导出器一次换好（h = H + N，EGM96），专给 Cesium 用。

### 2.4 一条原则：前端不算包络

每一条带、每一行的判定、每一根管子都在 Python 里由词表自己的函数算好（`instructions/display.py`，
只用 `envelope.py` 和 `labeller/*` 的公开函数；它不在标注器 sha 的范围里，改画法不改句子）；每一个判定
都是标注器的。航向带更进一步：画出来的就是判的那一个——被判的行由 `envelope.heading_word_rows` 给出，每一行
在不在带里由 `envelope.heading_words_inside` 一行一行地问（与标注器、执行器的判决是同一个函数），合计必须等于
标注器的计数，否则导出停下。前端只检查"账对不对"——长度、行号、每一步的词是不是事件往后填出来的、判定里的计数
是不是等于它自己逐行标记的计数、航向带的第一行是不是这个词的行加提前量、末行有没有越过下一个词的第一行和许可行、
带是不是目标 ± 词表的容差——然后照数画。前端若再算一遍包络，屏幕上就会有两个答案。

### 2.5 按名字拒读

`trainingSample.ts` 钉住并逐项核对：样本格式名 `aeroviz-training-sample-v7`、读法 `instruction-v3`、规格 sha
全文、六列的名字和顺序、"不变"的值、标注器会写的发令原因列表（`initial`、`per-step`、`clear`、`target`、`step`、
`angle`、`unspecified`）。对不上就整份拒读，报出是哪一项；不做兼容分支。格式名不对就不往下读，不管文件里是哪个
词表：v6（`instruction-v2` 的转弯与保持段）以及更早的文件按名字拒读。
Python 这一侧的对应常量由 `test_instruction_training_export.py` 与 TypeScript 源码逐字比对。

### 2.6 叠加层：执行器的回放与先验的预测

别的模型对一个集合里这些航班做了什么，写在集合**旁边**，不写进集合：

```bash
# 执行器：从工作树（worktree）里跑——在主工作区，执行器规格的源码 sha 会把 geokit 算进去而拒读（见 ts 的 R13）
python run_ts.py executor_training_export \
    --executor 4dTrajectory/outputs/POOLED/executor/<一份 instruction-v3 的执行器规格> \
    --replay 4dTrajectory/outputs/POOLED/executor/<同一份规格>/replay-val \
    --instructions 4dTrajectory/outputs/POOLED/instruction_language/<它的 instruction-v3 产物> \
    --airports-root aeroviz-4d/public/data/airports --set instruction_v3 \
    --airport KMSY --airport KRDU --airport KSJC --airport KSMF --airport KSTL
python run_ts.py prior_training_export \
    --prior 4dTrajectory/outputs/POOLED/prior/<在 v3 句子上训练的先验> \
    --instructions 4dTrajectory/outputs/POOLED/instruction_language/<同一份产物> \
    --airports-root aeroviz-4d/public/data/airports --set instruction_v3 \
    --airport KMSY --airport KRDU --airport KSJC --airport KSMF --airport KSTL
```

- **清单**：每个机场一个 `training/overlays.json`（`aeroviz-training-overlays-v1`），一条一个叠加层：种类、所画的集合、
  那个集合样本文件的 sha256、文件位置。叠加层文件放在 `training/<叠加层 id>/`，目录已存在或清单里已有同名 id 就拒绝，
  五个机场都建好才写。`index.json` 不动。
- **执行器**（`executor.json`，`aeroviz-training-executor-v2`）：用正式规格把集合里的航班重新飞一遍、判一遍。重飞是必须的：
  正式回放的 `replay.json` 只记了每架航班"哪一类词合不合格"的列表，没有记是哪一条词。重飞后每条判定对回句子里的词，
  并且**每架航班必须和正式回放的那一行一致**：结局、是否按原话飞完、判定列表、不判 / 没说到 / 被取代的词数完全相同，
  越过入口的偏离与高度差在 1e-9 以内（换一批航班一起飞，浮点求和的次序不同，差一个末位），否则导出停下、说出是哪一架。
  evaluation 的判定取正式回放的，不重跑。每条词一个状态：在包络里、出界、不判（航向词没有自己的行——它的行从执行器
  听到它的那一步加提前量起算，到了执行器听到的许可或它的截获，或者下一个航向词在同一步说出；或者飞出的航迹没过标注器
  的门）、没说到（航班先结束了）、被取代（同一列两条词落在飞出的同一步，只飞后一条）、没有自己的检查（跑道指针、下降角词、
  "未许可"、"未指定"）。航向词在不在包络里就是判决对它的那一条结果（行数、在带里的行数）；执行器离开航向词自己去
  切入航道的那条航向词记为出界。每个被判的航向词还带上它在飞出航迹上的航向带和逐行判定（`heading`，用与样本相同的
  `display.heading_band` 算，合计必须等于判决的计数），每架被判的航班带上判决读到的飞出航迹（`judgedTrackDeg`：
  平滑、切到落地前；它的第 k 步就是导出航迹的第 k 个点，读取时核对）——两者都放在与观测航迹同一个展开分支上。**动力学失败
  的航班不画带**：判决读到了失败的那个状态（可能不是有限值），而导出的航迹有意不含它；这些航班的词照样有判决的状态和检查，
  只是没有航向带和判决读到的航迹。回放不飞的航班（没有识别出机型、机型没有公开进近速度）
  列出原因，没有航迹。飞出的航迹每 2 s 一点，到结局那一行，带 MSL 与椭球高；还附上这个机场与全部机场的回放门表。
- **先验**（`prior.json`，`aeroviz-training-prior-v3`；第三版起前 `firstPredictedRow`（= 8）行只观察、没有预测，每列每步的数组
  从第一个预测步开始；第一个预测步六列都要说；跑道在第一个预测步之后不再换，它的"换词"指标为空）：检查点必须是这份产物的规格、
  标注器与运行日划分训练的、不是冒烟试跑、候选跑道表与产物相同、状态整份载入，并且今天的航迹清单与训练时的相同（落地情况从它读）。推断用教师强制（每一步看到真值句子在它之前的词），与训练和读数时一样；第三版的先验导出时，要核对这条推断
  路径在 val 航班上复现 `readout.json` 的每步负对数似然（第二版核对过：10,540 架，差 1e-9 以内）。每架航班每列每个预测步：说一个词的概率、若说一个词最可能的
  3 个词（列的取值少于 3 个时更少，例如一个机场的候选跑道）及其概率、真值的概率；每架航班每步的负对数似然（总的和
  分列的，四舍五入前算）。val 的读数随文件一起走，原样照抄。
- **绑定与核对**：叠加层自己写出所画集合的 id、样本的写出时刻、规格 sha；前端逐项核对，并核对每架航班的键、执行器的
  每条词（步、列、值）、先验的步数，对不上整份拒读。`check-publication` 另外核对盘上样本文件的 sha256。

2026-09-24 画在 `instruction_v2` 集合上的执行器（规格 `2674ab8c71a9`）与先验（`prior/v1_20260924`）叠加层的发布与
核对记在 `docs/CHANGELOG.md`；它们按名字拒读，v3 的叠加层要等 v3 的执行器规格、回放和先验。

---

## 3 每个词画成什么（飞行盒）

### 3.1 航向词：逐行判定的航向带

- **一个航向词说什么**（词表设计 §10.1，`instruction-v3`）：标注器每一行看平滑航迹 4 s（提前量）之后落在哪个 5° 格子，
  相邻几行格子相同就合成一个词，格子一变就说一个新词。所以一个航向词说的是"4 s 之后航迹在这个格子里，并保持到下一个
  词"；一段连续的转弯就是一串按它飞的快慢说出的词。没有保持段、没有拆分、没有补出来的切入航向。
- **它的包络是一条航向带**：从说出的那一行加提前量，到下一个航向词那一行加提前量（最后一个词到许可那一行），这些行上
  平滑航迹都要在目标 ± 4.5° 以内（按圆周差算，4.5° = 半格 2.5° + 起伏余量 2°）。它不约束位置，所以**没有平面上的区域**。
- **没有自己的行**：提前量把一个词的第一行推到许可那一行或之后时（常见于许可前一两步说出的词），这个词没有行可判，
  不画带，判定里行数为 0，句子条头部单独数出来。
- **画成什么**：读数窗口的航向图上每个词一块矩形，横向是它被判的那几步，纵向是目标 ± 4.5°（在平滑航迹的展开分支上）；
  有行在带外时边线红，那几行在航迹上画红（航向图、平面图的航迹、三维的地面投影）。三维里每个词被判的那一段地面投影画成
  航向带的颜色（一个词一个实体，可以单独选中）。观测航迹按构造都在带里——标注器就是按它读的——所以红色主要出现在执行器
  的回放上。
- 判定：多少行在带里（`check.inside` / `check.rows`），全在里面才算这个词合格。

### 3.2 进近

- **所有候选跑道**及其延长中线（跑道列的选项）；所指的那条高亮，始终画出。
- **许可**所在的步：截获转弯开始的那一行，而且不早于在用的航向词已经能碰到航线的那一行（词表设计 §10.1）。
- **截获转弯**：从许可那一行到截获那一行，朝跑道航向转；判定是单调、平均与最大转弯率在 0.5–4.7°/s 之内（下限只管
  10° 以上的转弯）、最大坡度不超过 32°。航向图上画成这段时间的竖条和航道那条虚线，平面图和三维画成航迹 / 地面投影上的
  虚线段，判定不过时是红的。飞机在第 0 步已经在五边上时没有截获转弯。
- **走廊**：从截获点到入口，半宽 = 20 m + 离入口距离 × tan 0.45°，航迹与航道差 ±2°。截获之后的每一行
  都在走廊里，这是截获的定义，导出时再核一遍。
- **落地与句子的结束**：落地的判定与 harvest 和评估相同——越过所指跑道的入口平面，越过点离中线不超过这条
  跑道的横向界限、离入口的高度不超过 100 m。句子在越过之前结束，界面写出最后一步离入口还有多远。

### 3.3 高度词与下降角词：管子

- 每个高度词一根管子，就是 `labeller.vertical.tube_bounds` 给出的逐行上下沿：从发令点沿下降角类的两条
  边线下降，碰到目标高度后停住，上下各加 25 m；每遇到一个下降角词就从那里重新起算。
- "下降至落地"没有目标，一直到句子结束。
- 下降角词标出类别（平飞 / 下降 1–4 档 / 爬升）、名义角度与范围，以及读出它的那一段的实测角度。
- 画出所指跑道的入口高程。
- 判定：每根管子逐行在不在里面（红色标出不在的行），以及整根是否全部包含。

### 3.4 速度词：过渡 + 速度带

- 有目标的速度词：从发令到第一次进入 V ± 5 m/s 之前是**过渡**——地速夹在起始值和目标之间（各放宽
  5 m/s），并且离起始值不超过 1.7 m/s² × 已过时间；进入之后是 **V ± 5 m/s 的带**。
- "未指定"：飞行员自己的速度，只剩速度词的量程（15–255 m/s），灰色标出。
- 许可所在的步画一条竖线。
- 判定：过渡单调、加速度不超限、带内的行数、整段是否包含。

---

## 4 屏幕

### 4.1 左侧面板

- 集合下拉：能读的集合；别的词表的集合也列着，后面写"— refused"，选中时显示按名字拒读的原因，不下载。
- **Draw**：
  - **heading words**（默认开）：每个航向词被判的那几行上的航向带（航向图）、那一段地面投影（三维）、出界的行（红）；
  - **the capture**（默认开）：截获转弯、走廊、中线、截获后的航道带；
  - **vertical**：管子（速度图上的带也跟着它）；**every candidate runway**。
  开关同时作用于三维、读数窗口的平面图和各图表。
  下面是两个叠加层的开关（§4.6）：**执行器的回放**、**先验的预测**。集合上有叠加层时默认打开；没有就灰掉，并写出生成它的
  命令。同一种有几个时给一个下拉，默认最后列出的。开关下面折叠着回放的门表（这个机场按进近方式分、全部机场，自己机型
  动力学进门、替代动力学只报）和先验的读数表（每列：先验、两个基线、换词步上说词的概率、第一名命中率）。
- ⓘ 展开词表的说明：读法、sha、每列类别数、各项容差（航向的提前量与容差，截获转弯的开始判据、转弯率与坡度，每条跑道
  的落地横向界限）、下降角类、平滑窗口。
- 抽样说明（原文照录文件里的规则）和航班列表（呼号、跑道、分组、第 0 步之后的词数；执行器的回放打开时，每架下面一行
  它的结局和合格的词数，全在包络里且落地为青色，否则红色）。
- 各种出错状态都说清楚：没有导出（给出路径、命令和"导出后重启开发服务器"，AV5）、索引里某一条格式
  不对（只标灰这一条）、能读的集合读出错（给出字段）、别的词表的集合（给出原因）。

### 4.2 句子条（底部，全宽）

- 六行，顺序同词表：跑道、进近、航向、高度、下降角、速度。横轴是这架飞机自己的时间。
- 一条带 = 一个生效中的词，从发令的那一步到同一列下一个词；带左端的小竖条就是发令。第 0 步六行都从 0
  开始。顶上的数字是"说了话的步"，头部写出第 0 步之后的词数和沉默的步数。
- 许可、截获、速度转为"未指定"三处用虚线标出——它们不是词。
- 头部是标注器的判定汇总：航向词全在带里的个数 / 有行可判的个数（及没有自己的行的个数）、截获转弯、
  高度词包含、速度词包含。
- 点一条带：选中它这一列（词类），游标移到它的发令步；再点一次取消。点步号只移游标，选中的列不变。
  只有选中那一列在游标处生效的词亮（黄边、加深），列名变黄——别的列在同一步生效的词不亮（§4.5）。
- "Read-back check" 打开读数核对窗口。
- 执行器的回放打开时：每条词左端一个点——青 = 飞在自己的包络里，红 = 出界，空心灰 = 不判 / 没说到 / 被取代，没有自己
  检查的词不画；悬停写出每项检查与原因（航向词：从它听到的那一步加提前量起，多少行在 ±4.5° 以内）。头部写出结局、越过
  入口时离中线与入口的距离、合格的词数、evaluation 判定。
- 先验的预测打开时：头部写出这架航班每步的负对数似然和 val 整体的；"Prior predictions" 打开先验窗口（§4.6）。

### 4.3 读数核对窗口

- **平面图**（机场坐标系，两轴同一比例，按航迹取景并裁剪）：候选跑道和延长中线、走廊、截获转弯那几行（虚线段）、
  航迹、航向词判出界的行（红）、各航向词的发令点、许可、截获点、句子结束处、游标点。
- **航向—时间**：平滑航迹（亮）与原始航迹（淡）；每个航向词的航向带（矩形，有行出界时红边，出界的行在航迹上画红）；
  截获转弯那段时间（竖条和航道虚线）；截获后的航道带（跟 "the capture"）。标题写出游标处生效的航向词、它的带判的是哪几步、
  多少行在带里（或"没有自己的行"），游标在截获转弯里时写出截获转弯的判定（单调、平均与最大转弯率、最大坡度）。
- **高度—水平距离**（管子就是定义在这根轴上的）：各高度词的管子、下降角词、入口高程、出管子的行（红）。
- **地速—时间**：过渡、速度带、"未指定"段、许可、出带的行（红）。
- 鼠标移过图只移游标；在图上点一下，选中这张图的列（航向 / 高度 / 速度）。黄色只给选中的那个词：它自己的
  包络，以及它生效的那几行——画在平面图的航迹上和画它那个信号的图上（§4.5）。
- 执行器的回放打开时：平面图画它飞出的航迹（青色虚线）和终点，以及它出界的行（红）；三张图用青色虚线画它的航向、高度、
  地速——横轴是**它自己的时间和它自己飞过的路程**，与观测不对齐，坐标轴放宽到装得下两者。航向图上另画它自己的航向带（青色
  边框，从它听到每个词的那一步加提前量起算；有行出界时红边），出界的行红色虚线画在判决读到的飞出航迹上。
- 窗口经 portal 渲染到 `document.body`（AV7）。

### 4.4 三维场景

- 航迹（椭球高），和它贴地的**地面投影**：平面上的包络贴在地上，斜着看时空中的航迹和它们错开，要对着投影读。
- 每个高度词一面墙（沿飞机自己的地面航迹，上下沿为管子的椭球高），上下沿另画两条线——±25 m 的墙隔远了
  只剩一条缝。
- 平面上的东西贴地画（它们只关于平面，不给高度），各跟自己的开关（§4.1）：
  - **航向词**：一个航向词没有平面上的区域，所以画的是它被判的那一段地面投影，航向带的颜色，一个词一个实体（选中变黄、
    其余淡化）；出界的行用红线盖在上面，不淡化。航向带本身（目标 ± 容差随时间）只在读数窗口的航向图上；
  - **截获转弯**：那几行的地面投影，虚线，颜色跟判定；
  - **走廊**：贴地多边形，另画一条贴地边线。
- **图例**：句子条上方、画面右下角，只列当前打开的开关画的东西，每种颜色一行字，可以收起。
- 候选跑道与延长中线贴地；航向词发令点、许可、截获点、句子结束处。
- 选中一架航班时相机取景一次，框住航迹（留 1.5 倍余量，句子条和左栏遮住一部分画面）；之后相机归用户，游标从不动它。
- 执行器的回放打开时：它飞出的航迹（青色，椭球高）、贴地的虚线投影、它出界的行（红，贴地，跟 "heading words" 开关）、
  终点和结局标签，图例加一行；开关它不重新取景。
- 静态实体，不接 CZML，不动 `viewer.clock`。

### 4.5 共享游标与高亮

`AppContext.trainingCursorS`（游标）和 `AppContext.trainingColumn`（选中的词类）由句子条、读数窗口和三维共用。
**高亮的是一个词，不是一步**（用户 2026-09-24）：选中那一列在游标处生效的词（`trainingWordAt`）。同一步上
别的列的词不亮——各列的词起止不同，一起亮会把没对齐的几段画成同一时刻。

| 选中的词 | 它自己的包络（三处视图都亮） | 另外 |
|---|---|---|
| 跑道 | 所指的跑道和延长中线 | |
| 进近"许可" | 截获转弯、走廊及中线、截获后的航道带 | "未许可"没有包络 |
| 航向 | 航向图上它的航向带；三维里它被判的那一段地面投影 | 截获之后仍是它自己的，走廊属于许可；它生效的行（从说出到下一个词）和被判的行（晚一个提前量）不是同一段，两者都画 |
| 高度 | 它的管子 | |
| 下降角 | 三维里没有；高度图上它的竖线 | |
| 速度 | 三维里没有；地速图上它的过渡和速度带（或"未指定"段） | |

每个词还把它生效的那几行画成黄色：三维与平面图的航迹上，以及画它那个信号的图上；三维在发令处加黄点和词名。
选中的包络：线（航向词的地面段、截获转弯）变黄；面（走廊、管子）保留本色加深、边线变黄。游标在同一个词里移动时三维
什么都不重画；换词只改材质和线宽，不重建几何。

**其余的淡化**：选中一个词时，别的词的包络（填充、边线、航向词的地面段、管子）在三维里颜色透明度乘 0.3，在读数窗口
里整体透明度 0.3，候选跑道、航迹和红色的出界行不变；取消选中就全部恢复。

### 4.6 执行器的回放与先验的预测

- **执行器的回放**回答"这套词飞得出来吗"：真值句子从第 0 行交给执行器，每条词在执行器飞到观测飞机听到它的位置时说，
  飞出的航迹画在实测航迹旁边（§4.3、§4.4），每条词的判定画在句子条上（§4.2）。要记住一点：**执行器的每条词是按从它自己
  听到这条词的位置重新画的包络判的**。航向词的包络画出来了（航向图上青色边框的带，在它自己的时间轴上）；别的词画的包络是
  观测航班的，出界的红点要对照悬停里的检查读。
- **先验的预测**回答"先验对这句话有多意外"：Prior predictions 窗口上半是游标所在这一步的表——每列真值在这一步说了什么
  （或不变，以及当时生效的词）、先验给真值的概率、给"这一步说一个词"的概率、若说一个词最可能的几个词（与真值相同的
  标青色）；下半每列一条带：说词的概率（列的颜色）、真值的概率（灰），真值在第 0 步之后说的每个词一根竖线，先验排第一
  的词就是它为青色，否则红色。鼠标移动改游标，点一行或一条带选中那一列。**这是教师强制的读法**：每一步都看到真值句子
  在它之前的词，不是先验自己说出一句话；先验自己说、执行器接着飞，是下一步（先验设计 §9）。

### 4.7 实时执行器：选中一个词，现飞它的一段

**用途**：验证执行器本身——任何一个词，交给执行器从真实的那一刻接着飞，看它飞成什么样、这个词判不判得过。**必须现算**：
每次选中都由后端重新飞一遍，不读执行器的正式回放、不读叠加层（用户 2026-09-25 的要求）。

**一段是什么**：句子条上的一个色块——某一列的一个词，从它说出的那一步 `row` 到同一列下一个词说出的那一步 `endRow`
（这一列的最后一个词：到句子结束）。**飞到这个词的包络结束的地方 `stopRow`**：一般就是 `endRow`；航向词是 `endRow` 再加一个
提前量（4 s，2 步），因为航向词的航向带本来就从它说出后一个提前量判到下一个航向词说出后一个提前量（词表设计 §10.1）——只飞到
`endRow` 的话，每个航向词都少判最后几行，只持续一步的航向词（约占四成）根本没有行可判。多飞的这一个提前量里，下一个航向词
照句子在 `endRow` 说出，执行器开始转向它，和整句回放里一样。`stopRow` 不超过句子末尾。

**怎么飞**（后端 `aeroviz_backend/autopilot_segment.py`，执行器 `4dTrajectory/ts_transformer/autopilot/` 原样使用）：

1. 初态是观测飞机在 `row` 这一步的真实状态（`flights.flight_inputs(anchor=row)`：这架航班在那一行的 `rollout_context`，
   和任何一次展开读航班的方式相同）。
2. 执行器的第 0 步是 `row` 这一步六列生效的词（句子的第 0 步本来就是"飞机此刻正在做的事"，执行器设计 §2.4），之后是句子里
   `row` 之后、`stopRow` 之前的词，每条词在执行器飞到观测飞机听到它的位置时说（规格的词钟 `track`，执行器设计 §11）。
3. 执行器用它自己的单步接口（`executor.Executor`，一次一个 1 s 控制周期）飞，驱动方式与 `executor.fly` 完全相同：每个周期先读
   词钟，一步的词在这一步开始的那个周期听到。`stopRow` 不是句子末尾时：句子后面补一个不说任何词的一步（`stopRow` 本身，让词钟
   能走到那里），**在词钟第一次把某个开始一步的周期放到 `stopRow` 的那个周期之前停下**——在那里说的词不会被听到，段尾之后什么
   也不飞。时限仍是这一段的步数 × 1.5；先到了别的结局（碰地、动力学失败、时限）就按结局报。这与"飞满时限再按词钟截断"逐位
   相同（2026-09-25，216 段，状态与结束周期一致），只是不再多飞。
4. `stopRow` 是句子末尾（例如跑道指针、许可之后的航向、"未指定"速度）：像回放一样飞到落地或失败。句子最后一步才说的词
   没有可飞的一步，按名字拒绝。
5. 判定用执行器自己的判决（`judge.judge`），读的是这一段的句子（从 `row` 起重新编号），所以每个词的包络从执行器听到它的
   地方重新画——选中的词就从观测飞机说它时的那个状态画。只报选中这个词：航向词 = 它的航向带在飞出航段上逐行在不在
   ±容差内（离开航向词去自己切入航道的，记为出界）；许可 = 截获转弯的单调与转弯率 / 坡度、走廊进入与保持；高度词 = 它的
   管子；下降角词 = 它作为锚点的每一根管子；速度词 = 过渡与速度带；跑道指针、"未许可"、"未指定"没有自己的检查。

**用哪份执行器规格**：后端在 `4dTrajectory/outputs/POOLED/executor/*/spec.json` 里找**恰好一份**由现在这份执行器代码写、
测的是这个集合所属产物的词表的规格（`replay.open_executor` 的全部核对）；没有或不止一份就拒绝并列出每一份的原因。集合的
产物与划分从集合自己的样本文件（`producedBy.artefact`、`cohort.split`）读；航班从到达清单重建，必须与产物里存的信号逐行
一致、重读出的句子与存的相同。重建过的航班在后端内存里留最近 8 架（重建要约 1 s）；飞与判每次都重新做。

**返回什么**（`aeroviz-autopilot-segment-v2`，前端 `trainingAutopilot.ts` 镜像）：用的规格、执行器代码的 sha、词钟；耗时
（`timing`，后端墙钟秒数：排在前一段后面等了多久；然后是加起来等于总时间 `computeS` 的各项——找集合与执行器规格、重建航班
（或沿用前几次请求重建好的）、准备这一段（句子、词钟、物理量）、执行器飞了多少个周期用了多久、判定、写答复）；
这一段（列、`row`、`endRow`、是否飞到落地、观测飞机飞这一段用的时间、告诉执行器的每一个词）；结局（到了段尾 / 判决的结局、
飞了多久、到段尾时与观测飞机在那一步的水平距离、高度差、地速差、越过入口的偏离）；选中词的判定与检查、航向词的航向带
与判决读到的航迹；每个周期（1 s）的航迹（机场坐标、经纬度、MSL 与椭球高、地速、垂直速度、航迹角、飞过的距离）和指令
（推力比例、坡度、载荷因数）。航迹的时间从航班第 0 步起算、距离从观测飞机在 `row` 处已飞过的距离起算、航迹角放在观测平滑
航迹在 `row` 处的分支上，所以它和观测的线画在同一套坐标上。

**前端的核对**：答复必须是这个机场、这个集合、这架航班、这一段（`endRow` 等于句子条上这个色块的终点），词表规格等于集合的，
**告诉执行器的词必须正好是句子条上这一段显示的词**（`row` 处六列生效的词 + 段内说的每个词），航向带过 `headingBandProblem`
的核对。对不上整份拒读，说出是哪一项。

**界面**：

- 左侧面板 Draw 里的开关 **the autopilot, live**（默认开）。开着时，**在句子条上点一个色块**就立刻请求这一段（`trainingPick`）；
  只有点击才请求——游标不请求，因为图表上悬停就会移动游标，而飞出的线正是要悬停着看的。换一个词、或再点一次取消后再选，
  就再飞一次；"Fly again" 手动再飞；换了航班，上一架的选择不再飞。开关下面是结果卡：飞的是哪个词、哪几步（航向词写出多飞到
  哪一步）；**卡片顶部并排两个数：模拟飞行时间**（执行器飞了多少秒，下面是观测飞机飞同一段用的时间和控制周期数）**与计算用时**
  （后端从开始处理到答复的时间，下面是浏览器从发出请求到读完答复的往返时间），再下一行分项写出：执行器（算了多少个周期）、
  判定、重建航班或沿用、找集合与规格、准备这一段、写答复、排队等待、往返（不到 1 s 的写毫秒）。"模拟飞行"框里的周期数是判定
  所读的那一段（等于模拟秒数 ÷ 周期），分项里的是实际算了的周期（结局之后执行器可能还飞了几个周期）；然后是结局、到段尾时离观测飞机多远、词的判定和每一项检查、哪些限制起了
  作用、用的规格、代码 sha、词钟。后端没开、拒绝、答复对不上时说清楚原因。
- 句子条头部一行：正在飞 / 飞完的摘要，写出模拟飞行时间（对照观测）与计算用时。
- 三维：一架飞机从说这个词的地方把这一段"飞出来"（按实际时间加速，至少 8 倍、整段不超过 20 s），标签（蓝底白字）走一个模拟
  时钟"已飞 / 全段 s simulated"，并写出倍数、地速、高度、坡度；身后的线随之变长；飞完后整段保留，地面投影为虚线，航向词出界的行在地面投影上画红。"Replay in 3D" 重放同一个
  答复，不再请求。不用共享的 Cesium 时钟。
- 读数核对窗口：平面图、航向图、高度图（对飞过的距离）、速度图各一条蓝色实线，从观测线上的那一点出发；选中的是航向词时
  画出判决读到的航向带（蓝框，有出界行时红框）和出界的行。

颜色：蓝 `#2563eb`（`TRAINING_AUTOPILOT_COLOR`），和执行器回放的青色分开——回放是事先算好的，这个是现飞的。

---

## 5 核对

- `npm run check-publication`：读每个机场的 `index.json`，列出的每个集合文件必须存在；别的词表的集合记为
  **警告**（按名字拒读是有意的，删不删是另一回事）；能读的集合用前端自己的 `parseTrainingSample` 读，
  并和索引条目逐项比对（集合名、航班数、规格 sha、机场几何 sha、读法、每组数量、种子）。
- 加 `--server <地址>`：同样的文件从正在运行的开发服务器取，JSON 必须是 JSON（不是 SPA 回退页），能读的
  样本**把服务器返回的内容再用读取函数读一遍**——HTTP 200 不等于"能加载"。
- 加 `--airports-root <目录>`：核对另一个目录（例如导出到临时目录的试跑），不能和 `--server` 同用。
- 叠加层：`overlays.json` 用前端自己的读取函数读，每个叠加层对照它所画集合的样本读一遍，并核对盘上样本文件的 sha256 是
  叠加层记下的那一个（集合重新导出过就报错）；画在界面不读的集合上的叠加层记为警告。加 `--server` 时服务器返回的叠加层
  也再读一遍。

---

## 6 不做什么

1. 不在界面上改词。重飞只有 §4.7 一种：后端的执行器飞选中那个词的一段；前端不算任何飞行。
2. 不录入人工核对的判定。那属于带 sha 的产物，不属于浏览器本地存储。
3. 不浏览全体航班。导出是抽样，规则写在文件里、界面上照录。
4. 不改 Observe 和比较视图。
5. 不做多机。

---

## 7 维护约定

- 本文只描述当前设计。词表换版本（新读法或新规格 sha），界面会按名字拒读新产物：同一次改动里更新
  `trainingSample.ts` 的钉住值、测试、`aeroviz-4d/CLAUDE.md` 与 `35-viewer-reference.md` 的对应条目和本文。
- 格式名跟着文件的形状走：样本或索引的字段一有增、删、改名，Python 的 `SAMPLE_SCHEMA` / `INDEX_SCHEMA` 和
  TypeScript 的 `TRAINING_SAMPLE_SCHEMA` / `TRAINING_INDEX_SCHEMA` 在同一次改动里一起换新名字，盘上的集合重新导出；
  不为"旧文件还能读"或"现在没人读错"保留旧名字。叠加层的三个格式名（`OVERLAYS_SCHEMA`、`executor_training_export.SCHEMA`、
  `prior_training_export.SCHEMA` 与 `trainingOverlays.ts` 里的 `TRAINING_OVERLAYS_SCHEMA`、`TRAINING_EXECUTOR_SCHEMA`、
  `TRAINING_PRIOR_SCHEMA`）同理，由 `test_training_overlays.py` 逐字比对。
- 集合重新导出后，画在它上面的叠加层会被按名字拒读（样本的写出时刻变了）：重新生成叠加层，用新的叠加层 id。
- 长期事实一行写进 `aeroviz-4d/CLAUDE.md` 的索引，全文写进 `aeroviz-4d/docs/35-viewer-reference.md`
  （AV19–AV26）。导出器的说明在 `4dTrajectory/ts_transformer/docs/reference/runners.md`（R11；叠加层 R13）。
- 词表的读法再换（例如 §10.2 的高度词）时，逐项检查本文 §2.3、§3、§4 里写到词的地方，与 `display.py` 同一次改动。
- 日志式记录写进仓库的 `docs/CHANGELOG.md`。
