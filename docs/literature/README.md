# Literature: index of topic folders

Each folder holds one reading list: a `README.md` (index, findings, and the question it answers), a
`download.sh` that re-fetches every source, and the sources themselves (`papers/`, or `official/` and
`nasa_eurocontrol/` in `runway_assignment/`). PDFs are **not tracked in git** (root `.gitignore` has
`*.pdf`), so run a folder's `download.sh` after a fresh clone. The convention: a paper read for an
assessment goes into the folder for its topic, indexed in that folder's README.

| folder | date | what it answers | start here |
|---|---|---|---|
| [`aircraft_performance/`](aircraft_performance/README.md) | 2026-09-23 | For the aircraft types OpenAP lacks (today flown as A320): which published source gives their performance parameters, under which licence, and with what coverage of the training fleet? OpenAP 2.6.2, Poll–Schumann (pycontrails), BADA 3/4, ICAO/ECAC ANP, EUROCONTROL APD, ICAO engine emissions databank, plus a primary-document pack (90 EASA/FAA TCDS and maker documents) giving wing area and rated thrust/power for 46 types. The analysis that uses it is `docs/aircraft_performance/2026-09-23_missing_performance_substitution.zh.md` | *At a glance*, then `airframe_facts.csv` |
| [`jepa/`](jepa/README.md) | 2026-09-06 | Can a Joint-Embedding Predictive Architecture (JEPA) be applied to `4dTrajectory/ts_transformer`, and would it make the trajectory flown from predicted operating parameters more accurate? Seven papers, plus LeCun's position paper (not fetched) | §4 *Assessment* (the answer), then the §1 paper table |
| [`procedure_hard_constraints/`](procedure_hard_constraints/README.md) | 2026-09-08 | How to make the final-approach procedure constraint (LPV corridor + glidepath window) **hard** in ts_transformer training, on both the `state` and the `control` path. 80 PDFs in three clusters, with formula-level notes in `notes_{A,B,C}.md` | *The five findings that matter*, then `4dTrajectory/ts_transformer/docs/2026-09-08_hard_constraints_survey_and_integration_plan.md` |
| [`runway_assignment/`](runway_assignment/README.md) | 2026-09-03 | Who assigns the runway, when, and on what basis (tower/TRACON configuration, scheduler, clearance, side-step), and whether runway assignment belongs in the scheduling layer. Holds the **7110.65BB, 7210.3EE and AIM PDFs, ICAO Doc 9643 (SOIR) and the Erzberger–Itoh arrival-scheduling TP** that `arrival_separation/` cross-links | *Start here (the 90-minute version)*, then *What the documents say, in one table* |
| [`control_normalization/`](control_normalization/README.md) | 2026-09-14 | Should the `control` head's thrust channel be normalised by its **effect** (specific force `(T − D)/W`) instead of by the actuator (`T/T_max`), so one head models every airframe? 36 sources across aviation, quadrotors, cross-embodiment robotics, driving, biomechanics and dimensionless ML (31 PDFs), plus two read-only measurement scripts over the fleet and the KRDU `B1_point_matched` arm | §3 *Assessment* (the answer and the plan), then §2 *Patterns across fields* |
| [`arrival_separation/`](arrival_separation/README.md) | 2026-09-14 | The separation rules a multi-runway arrival scheduler must honour, quoted with paragraph numbers from **FAA JO 7110.65BB Change 3** (eff. 2026-07-09): same-runway radar and wake minima (CWT A–I and legacy), reduced 2.5 NM, parallels by centerline spacing, intersecting runways. Also the type → CWT mapping from JO 7360.1K (parsed CSV), ICAO Doc 4444 / SOIR / RECAT-EU for comparison, and the five study airports mapped to the rules | *At a glance*, then §7 *Constraints for the R3 scheduler*, then §4 *Applied to our five airports* |
| [`manoeuvre_tokens/`](manoeuvre_tokens/README.md) | 2026-09-18 | For a **discrete manoeuvre-token** scheme in `ts_transformer` (segment → code, causal prior over codes, learned per-code controller, joint multi-aircraft decode with separation as a vocabulary mask): what has already been built and how was it parameterised? 13 PDFs in four clusters — motion tokens + next-token prediction (MotionLM / Trajeglish / SMART: what is quantised, vocabulary size, time step, parallel vs sequential agents within a step), how the codebook is learned (VQ-VAE commitment loss, FSQ levels, VQ-BeT's code + offset head), edge features as an attention bias (Graphormer) + rollout exposure (scheduled sampling) + constraint-as-mask (grammar-constrained decoding), and terminal-airspace flow clustering + the two aircraft VQ/token precedents | §3 *What the folder says about the proposed design*, then the §1 mechanism tables |
| [`multimodal_intent/`](multimodal_intent/README.md) | 2026-09-18 | For a **multiple-hypothesis second layer** in `ts_transformer` (K candidate intents per segment, winner-takes-all / min-over-K training, selection by a probability head or a scorer, goal- / anchor-based variants, tree- or beam-structured multi-step prediction): what has been built, how the winner is chosen, how the hypothesis set is kept from collapsing, and whether any aviation work does this. 13 papers verified (one citation corrected), 9 PDFs; aviation: ASCENT (ICRA 2026) is the only explicit K-hypothesis + WTA aircraft predictor, DeepTP the only beam decode, and no aircraft/TMA paper builds a tree of branching intents | §1 verification + mechanism tables, then §3 (collapse of plain WTA, DESIRE is scorer-based not WTA, no per-segment intent tree published) |
| [`prediction_horizons/`](prediction_horizons/README.md) | 2026-09-19 | In data-driven aircraft trajectory prediction, **what input history length and what prediction horizon** do papers actually use, at what sampling interval, one-shot or rolling, with which metrics — and **does anyone evaluate closed-loop to the runway**? 14 rows read from primary setup text (6 new PDFs here, 8 cited by path in sibling folders): terminal-area history 11 s → 3 min and horizons 90 s → 5 min; en-route history 3 → 16 min and horizons 20 s → 5 min (plus two whole-flight flight-plan models). No row both samples finer than 10 s and looks back beyond 2 min, and **no row flies an arrival to the threshold and scores it there** | *At a glance*, then §3 *What this says for the thesis paragraph*, then §4 *Not verified* |
| [`trajectory_as_language/`](trajectory_as_language/README.md) | 2026-09-20 | For the design idea of an **arrival trajectory as a sentence** (each segment a discrete intent token, a causal model over tokens, a learned controller flying each one): after next-token pretraining with teacher forcing, **what makes such a model "speak" rather than imitate** — reach the runway inside the procedure and the dynamics? 20 rows from primary text (12 new PDFs, 8 cited by path in sibling folders) in seven clusters: tokenised motion models (MotionLM/Trajeglish/SMART by path, plus GUMP, BehaviorGPT, InfGen), **closed-loop supervised fine-tuning (CAT-K, the central item)**, RL/RLHF fine-tuning (TrafficRLHF, CtRL-Sim, R1Sim), goal-directed tree decoding (TPP, DTPP), constrained decoding as feasibility (GCD, SaFeR), exposure bias (DAgger, scheduled sampling, MIXER) and aviation (FlightBERT++, FTP-LLM, FLY-EVAL++, LLM4Delay). CAT-K beats a 14× model scale-up; a full GRPO stage on top of it buys +0.0001; **no aviation row goes beyond imitation** | *At a glance*, then §1.2 (CAT-K), then §3 *What this says for the two-tier plan* |
| [`multi_agent_interaction/`](multi_agent_interaction/README.md) | 2026-09-24 | For the prior's **multi-aircraft scene** design (causal attention over time + attention across the aircraft at each step with relative-geometry edge features, joint teacher-forced pretraining, interaction learned in closed-loop post-training with separation): how does agent interaction enter a model (graph message passing vs attention with edge biases vs hand-built neighbour features), joint vs parallel decoding within a step, how interaction is actually learned (teacher forcing, closed loop, collision losses, RL fine-tuning), and what aviation has built. 14 new PDFs + 21 sources cited by path. Findings: put relative geometry into the attention values as well as the logits (MTR++ Table 10, Groot 2022); no source shows a separate GNN beating agent attention on small fully connected scenes; closing the loop teaches interaction far more than a separation loss (TrafficSim 5.92 → 1.28 % collisions), and RL on top of closed-loop fine-tuning adds little; no aviation paper learns controller instructions from multi-aircraft data in closed loop | *At a glance*, then §4 *What this means for the scene prior*, then the §2 per-paper notes |
| [`approach_speeds_reduced_flap/`](approach_speeds_reduced_flap/README.md) | 2026-09-25 | For code-health follow-up 21 (single FAA approach-speed rows for multi-flap types E75L, B737, A319, E170, E190): is there a **published reduced-flap V_REF at the maximum landing weight** in a primary document? Manufacturer airport-planning documents, FAA/EASA/TC evaluation reports, TCDS, NTSB dockets, ANP (22 files). **None found for any of the five**; and the premise that the FAA's dual rows are flap pairs is doubtful (their 140 kt lower values sit on the category-C edge the FAA database fills in by calculation) | *Status*, then §1 *Answer at a glance*, then §4 |
| [`hierarchical_prediction/`](hierarchical_prediction/README.md) | 2026-09-16 | Is a **two-tier** transformer worth building for `ts_transformer` — a re-asked short-horizon control model (tier 1) conditioned on a segment-tokenised long-horizon plan model (tier 2), plus a later multi-aircraft graph (tier 3)? 36 sources in four clusters (32 PDFs): hierarchical motion forecasting (TNT → QCNet), multi-scale/segment-token time-series transformers (Crossformer → Pathformer), the iterated-vs-direct theory (Ross & Bagnell `T²ε`, DaD's `O(exp(T log L))`, the well-specified/misspecified reversal), and multi-aircraft interaction + intent inputs | §2 *Patterns across the four clusters*, then §3 *Results that contradict the premise* |
