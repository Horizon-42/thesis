"""The training plane: the loop, the objective, the validation replay and checkpoint selection, the batch-size probe, cross-validation, the experiment index.

Nothing is re-exported here: a group is a directory, not a namespace (2026-09-10, review §4).
"""
