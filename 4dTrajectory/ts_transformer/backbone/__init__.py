"""The vendored networks and the one interface over them: `adapters` wraps iTransformer and PatchTST behind ``forecaster(x) -> Tensor[B, N, C]`` and builds the state forecaster; `vendor/` is byte-identical to upstream (see its PROVENANCE files).
"""
