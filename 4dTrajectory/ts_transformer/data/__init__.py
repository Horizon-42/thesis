"""The data plane: observed arrivals → series → windows, and the contracts they carry (channels, frames, grids, the anchor axis, splits, provenance, eligibility).

Nothing is re-exported here: a group is a directory, not a namespace (2026-09-10, review §4).
"""
