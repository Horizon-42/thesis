"""What a predicted schedule is scored against.

``components`` assembles the tracking objectives (position, velocity, imitation, terminal)
and ``fixed_dt`` is the fixed-physical-time supervision variant.
"""
