"""The predict side: the forecast contract and batch entry point, the calibrated ETA interval, the record export, the test-release protocol, the readout builders.

Nothing is re-exported here: a group is a directory, not a namespace (2026-09-10, review §4).
"""
