# 跑道意图：把"降落跑道"从已知输入变成预测对象（计划，2026-09-13）

## 状态

| 项 | 状态 |
|---|---|
| 本文 | 计划 + R0 结果。§1 的数字来自写计划前的现有产物，§11 是 R0 的读数 |
| R0 零训练测量 | **完成 2026-09-13**（§11）：方向层已由同时段落地解决（96–99 %），左右层是问题所在（KRDU 73–75 %、KSMF 61–65 %、KSTL 约 90 %）；按 B1 / B3 选跑道时，plan 头在全部航班上的 FDE 均值比已知跑道高 +380–650 m（KRDU / KSTL / KSMF，配对） |
| R1 | **进行中**：D1 已定为 (a) 按天划分（用户 2026-09-13）；设计见 §12，门槛见 §11.4 |
| R2–R4 | 未开始；按 §7 的门槛依次推进，前一级不过门槛不开下一级 |
| 待用户决定 | D1–D5（§9）；R0 对 D1–D3 的建议见 §11.4 |
| 分支 / 提交 | `worktree-runway-intent-plan`：计划 `e4412f8`，R0 代码 `fc57d80`，复核修正 `9e3ac49` |
| R0 产物 | `4dTrajectory/outputs/POOLED/experiments/runway_intent_r0_20260913/`（`readout.md` 由 `docs/runway_intent_r0_readout.py` 重新生成） |

## 0. 为什么要做

用户 2026-09-13 指出：飞机最终落哪条跑道应当是模型学到的部分，而不是给定的输入；多机交互时，这种
意图学习尤其重要。

现状恰好相反：

- **ts 模型**（state / control / closure / plan）的坐标系都锚在该航班**实际降落跑道**的入口上
  （`coordinate_frame="enu"`，`FlightSeries.target_chart`），跑道来自 harvest 按最终进近做的
  arg-min 分配（`final_approach/`，见 `final_approach/CLAUDE.md`）——也就是说读了未来。唯一的例外是
  2026-09-03 机场坐标消融的 B / C 臂（`airport_frame_20260903/B_airport_enu`、`C_airport_enu_target`），
  它们是诊断用的，结论见 §1.1。
- **plan 路径**更进一步：航路的最后一段按这条跑道的 CIFP 程序骨架铺到入口
  （`outputs/plan/skeleton.py::runway_skeleton`，`outputs/plan/guidance/route.py`），入口闭合是结构保证的。
- **后果**：目前发布和文档里的所有 ADE/FDE，都是"已知跑道"条件下的数字，是上限而不是端到端预测
  精度。论文里必须如实声明（D5）。

## 1. 已知证据（写本计划前已测过的东西）

### 1.1 让主干网络隐式学跑道会失败 — 机场中心坐标消融（2026-09-03）

`docs/2026-09-03_airport_frame_ablation_results.md`，验证集，两个种子：

- 去掉入口锚点（arm B：机场中心 ENU，从不告诉模型目标跑道），确定性模型在平行跑道之间**取平均**：
  KRDU 预测终点更接近相邻跑道的比例 **1.5 % → 12–15 %**，每对平行跑道里的少数跑道被拉向多数跑道
  **570–680 m**，该跑道 FDE 中位数升高 30–45 %。
- 把目标跑道坐标作为输入通道（arm C）**救不回来**；只有时长头从中得益（final-time MAE 约 −10 %）。
- 结论当时是"保留入口锚点，在预测器外面选跑道"。本计划就是把"外面那个选择"变成被学习、被评估的对象。

### 1.2 方向容易，平行跑道的左右才是难点 — 跑道假设展开（2026-09-03）

`docs/2026-09-03_runway_hypothesis_expansion.md`，runner `experiments/runway_hypotheses.py`，
产物 `4dTrajectory/outputs/{KRDU,KSJC}/experiments/runway_hypotheses_20260903/`（arm A 的 state
checkpoint，对每个验证航班、每条有 CIFP 目标的候选跑道各预测一次，再用选择规则挑）：

| 选择规则（因果，不读未来） | KRDU 选中率 | KRDU FDE 均值（种子 1337） | KSJC 选中率 |
|---|---:|---:|---:|
| assigned（真值标签，基准） | 100 % | 1163 m | 100 % |
| active_config（入场前 30 min 内各跑道落地数取最多） | **65 %** | 1388 m | **94 %** |
| self_consistency（预测离哪条跑道最近） | 37 % | 1535 m | 34 % |

- **方向**靠同时段落地航班就能判：KSJC 93 %，KRDU 多数跑道 80–83 %（上表的 94 % 是选中确切跑道的比例）。
- **平行跑道左右**判不了：KRDU 少数跑道 05R / 23L 上 active_config 只对 **29 % / 31 %**，错一次
  付出整段间隔——05R 的 FDE 中位数 983 → 1456 m，23L 789 → 1630 m；约三分之一的 KRDU 航班受影响，
  值 500–800 m FDE。KSJC 的平行跑道只隔约 230 m，选错几乎无代价。
- **预测器不能自证跑道**（self_consistency 33–45 %）：跑道选择只能来自上下文，不能来自轨迹本身。
- **min-over-K 有一半是运气**：真实相邻跑道 oracle 比镜像假跑道对照只多挣约一半；任何 minFDE@K
  都必须和镜像对照一起报。

### 1.3 五个机场的跑道分布与平行间隔（本计划测，2026-09-13）

降落跑道分布来自 v5 到达名册 `trajectory_data_process/outputs/harvest/<ICAO>/arrivals/manifest.json`
（`harvest-arrivals-v5-takeoff-excluded`）；平行间隔用 `aeroviz-4d/public/data/airports/<ICAO>/runway.geojson`
的道面多边形算中心线横向距离。

| 机场 | 到达数 | 各跑道到达数 | 近平行跑道对（中心线横距） | 需要解决的层级 |
|---|---:|---|---|---|
| KRDU | 14,435 | 23R 6638 · 23L 3230 · 05L 3076 · 05R 1491 | 05L/23R–05R/23L **1073 m（3519 ft）**，中心错开 855 m | 方向 + **左右（代价已测：大）** |
| KSJC | 11,082 | 30L 9560 · 30R 943 · 12R 565 · 12L 14 | 12L/30R–12R/30L **228 m**（11/29 另有 221 / 449 m，名册里无到达） | 方向；左右代价已测：可忽略 |
| KSTL | 8,767 | 12L 3943 · 30R 3676 · 11 645 · 12R 262 · 30L 121 · 29 96 · 24 23 · 06 1 | 12L/30R–12R/30L **397 m**；11/29 与二者 854 / 1251 m | 方向 + **三条近平行（未测）** |
| KSMF | 4,219 | 17R 2484 · 17L 1142 · 35L 593 | 17L/35R–17R/35L **1824 m（5984 ft）** | 方向 + 宽间隔平行（未测）；35R 不在 v5 名册 |
| KMSY | 4,147 | 11 2614 · 29 1247 · 02 284 · 20 2 | 无 | 方向 + 交叉跑道 02（未测） |

注意：KRDU 32 与 KSMF 35R 只在 v6 harvest 进入名册（根 `CLAUDE.md` Open Items），当前 v5 名册里没有，
候选集因此与 harvest 版本绑定（D3）。

### 1.4 现成可用的数据

- **全量交通**：`harvest/<ICAO>/tracks/manifest.json` 含所有结局的航迹（KRDU 66,942 条，其中
  `assigned` 16,056 条落地），带 `landing_time_utc`、`runway`、`outcome`；可作同时段上下文池，
  不受训练队列的横向合格筛选限制。
- **到达名册**每行有 `entry_time_utc`、`landing_time_utc`、`runway`、`callsign`、`icao24`。
- **风**：`data/metar/<ICAO>/asos_*.csv`（IEM ASOS：`valid, drct, sknt, gust`，逐小时），覆盖五个机场
  的 harvest 期（2026-04-30/05-16 起至 07-22/07-23）。
- **harvest 期长度**（落地日数）：KRDU 57、KSJC 56、KSTL 42、KSMF 26、KMSY 30 天——按天分块划分时
  验证集只有几天，运行方向的切换次数可能很少（R0 要数）。

## 2. 问题定义

- **谁选跑道**：管制员，不是飞行员。分两层：
  - **L_cfg 机场运行构型**（朝哪个方向落、开哪几条跑道）：由风、噪声程序、时段决定，**同一时刻所有
    航班共享**；
  - **L_assign 单机分配**（在开放的跑道之间选哪条）：负载均衡、进场方向、停机位所在侧、机型尾流、间隔，
    以及**其他飞机**——这一层天然是多机问题。
- **学习目标**：管制员的分配策略，即 `p(r | 此刻能观测到的一切)`。
- **标签**：到达名册的 `runway`（harvest 按最终进近分配）。进近末段换到平行跑道（side-step）被记成最终
  跑道；更早的改派在数据里不可见。
- **候选集**：机场上有已发布 RNAV(GPS) 程序的跑道——plan 路径需要每个候选的骨架。以 R0 核实的列表为准。

## 3. 架构

### 3.1 显式分解：跑道是离散意图，不是主干网络的隐式特征

```
p(轨迹 | 上下文) = Σ_r  p(r | 上下文) · p(轨迹 | r, 上下文)
```

- **跑道头** `p(r | 上下文)`：对候选跑道分类。
- **条件专家** `p(轨迹 | r, 上下文)`：现有的入口锚定预测器，原样复用——它本来就是"给定跑道"的模型。
- 依据：§1.1 表明隐式学习会在平行跑道间取平均且坐标输入救不回；§1.2 表明选择必须来自上下文。
  显式分解避开取平均，给出可校准的概率，也和 plan 路径"每条跑道一个程序骨架"的结构对齐。

### 3.2 跑道头

**输入全部因果**（只用锚点时刻之前已经发生、已经发布的信息）：

- **本机**：历史窗口，放在**机场固定坐标系**里（`coordinate_frame="airport-enu"` 已有，来自 §1.1 的消融）——
  用降落跑道的坐标系会直接泄露标签；到每个候选跑道 IF / FAF / 过渡段的方位与距离，以及在各候选跑道
  轴系下的沿航向 / 偏航距离（`skeleton.runway_axes` 已有）；进场扇区（终端区入口点相对机场的方位，
  由 `entry_time_utc` 处的位置得出）；机型尾流等级（typecode 已在数据选择里）；航司（callsign 前缀，
  代表停机位所在侧——§1.2 当时列出的"左右"候选特征）。
- **机场状态**：锚点前 30 / 60 min 内各候选跑道的落地数、最近 N 次落地各用了哪条跑道——**取自全量交通**
  （§1.4），只取 `landing_time_utc` 严格早于锚点的；METAR 取观测时间早于锚点的最近一份（风向、风速、
  阵风，及对每个候选跑道的顶风 / 侧风分量）。
- **交通**（R3 起）：此刻终端区内其他飞机的位置、机型，以及已建立在五边上的飞机的跑道（已知）。

**结构**：逐候选打分——共享打分器 `logit_r = f(上下文, 候选跑道嵌入_r)`（嵌入含入口、航向、程序），
再在该机场的候选集上 softmax。候选数不同的机场共用一个模型，满足五个机场均衡采样的原则。

**先做独立小模型**（R1），不挂在 ts 主干上：可解释（特征贡献看得见）、训练便宜、不会扰动已有的轨迹头。
只有当历史窗口在手工特征之外还带有明显信息时，才并入主干（D4）。

### 3.3 条件专家与混合输出

- **每个候选各跑一次专家**：复用 `experiments/runway_hypotheses.py` 的机制——克隆航班字典换成候选跑道
  的 `runway_target`，在那条跑道的坐标系里重建序列，用训练时的同一个 `build_series` 和 `predict`
  预测。**state / control / plan 三条路径都不用重训**。plan 路径要让
  `outputs/plan/skeleton.py::runway_skeleton(series)` 接受显式指定的跑道（今天它从 `series.target` 取）。
- 专家仍用真实跑道训练（teacher forcing），与现在一致。
- **输出**：跑道混合 `{r: (p_r, 轨迹_r)}`，另给 top-1。前端以"跑道扇"展示（每个候选一条，按 p_r
  着色 / 透明度）；发布时每个假设是一个 `--category-variant`，意图按根 `CLAUDE.md` 的规则写进
  `docs/experiments/intents.json`。

### 3.4 滚动预测

- 每个 lockstep 步（30 s）也重新问一次跑道头，记录跑道信念随剩余航程的变化，画 anytime 曲线
  （准确率、NLL 对剩余航程 40 → 2 km）。
- **何时锁定跑道**（切到该跑道的五边）是 R2 的实测项，不预先规定。候选规则：汇入时锁定 / p 超过阈值时
  锁定 / 从不锁定。先量 top-1 翻转有多频繁、翻转是否有害——与 plan 路径 3(f) 的指令保持实验同一思路
  （那次保持规则反而变差，所以锁定规则必须实测后才能采用）。

## 4. 多机交互

### 4.1 阶段 A（R3）：独立预测 + 联合分配

把 plan-and-guidance §9 step 6（多机调度演示，2026-09-12 提出，**一条跑道、一个钟点**）扩成**多条开放
跑道**：

- 决策变量：每架机的（跑道, 时隙）。
- 目标：`Σ log p(r_i)` 最大，减去延误代价。
- 约束：同一跑道的最小间隔（step 6 待定的间隔选项：3 NM 雷达间隔或尾流间隔）；**平行跑道的相关 / 独立
  进近规则**——KRDU 的 3519 ft 低于独立平行进近通常要求的中心线间距（FAA JO 7110.65：一般 4300 ft，特定
  监视条件下可降至 3600 ft），按相关（错开）进近处理；KSMF 的 5984 ft 满足独立进近；具体条款在 R3 定约束时
  核对原文；闭合层报告的 X ≤ 0（能飞到）。
- 求解：每钟点二十架左右，按 ETA 顺序贪心或小规模整数规划即可。
- 分配结果交给 step 4 已有的到达时刻闭合（`Assignment`）去飞。
- 检查：与真值分配的一致率、间隔损失、延误，沿用 step 6 的核验项。

### 4.2 阶段 B（R4，条件性）：联合意图模型

- 场景编码器：对锚点时刻终端区内所有飞机做注意力，每架机的跑道 logits 以其他飞机为条件，按预测落地
  顺序自回归输出；用观测到的联合分配训练。目的是学到"23R 排队长就派 23L"这类均衡策略。
- **只在阶段 A 测出交互误差时才做**：例如繁忙时段独立预测的分配一致率明显下降、或出现可解释的均衡模式。
- 场景要从全量交通按统一时钟构建，不能只用训练队列。

## 5. 评估协议

**跑道本身**

- 准确率、NLL、可靠性图 / ECE；分层报告：方向层（L_cfg）与左右层（L_assign）；按机场、按跑道
  （少数跑道单列）、按剩余航程（anytime 曲线）。
- 因果基线（都要打败）：B0 机场多数跑道；B1 active_config（§1.2，入场前 30 min）；B2 B1 + 航向门限；
  B3 同进场扇区上一架落地航班的跑道；B4 构型内顶风最大的跑道。

**轨迹**

- 主指标：**用 top-1 预测跑道的端到端误差**。辅助：概率加权误差 `Σ p_r · err_r`、各假设误差。
- minFDE@K 只和镜像对照一起报（§1.2）。
- "选对跑道"与"选错跑道"两个子集分开报告。
- **与"已知跑道"基准在同一批航班上配对**：差值就是"不知道跑道的代价"，论文必须引用这个数。

**门槛写法**：每一级的门槛在上一级读数出来后**预注册**写进本文，不事后挑。

## 6. 数据与泄漏

- **划分（D1，卡住 R1）**：现在按航班随机划分（`sha256(seed:airport-qualified-flight-id)`，见各 run 的
  `data_selection.json`）。同一时段的航班共享运行构型，验证集航班的"邻居"在训练集里——带时间或构型特征
  的跑道头会借此泄露。跑道实验需要**按天分块**划分。连带问题：条件专家若仍是按航班划分训练的，它们在
  新验证集的日子里训练过，端到端轨迹指标会被污染；所以 R2 的端到端评估需要在同一按天划分上重训专家。
- **外部测试集**照旧封存，不碰（`freeze-test`）。
- **上下文池**用全量交通（§1.4），只取锚点之前的事件。
- **METAR**：ASOS 的 `valid` 是观测时刻，报文发布晚几分钟——因果规则取 `valid + 发布延迟 ≤ 锚点`，
  延迟取值在 R0 定下并写进本文。
- **候选集随 harvest 版本变化**（D3）。

## 7. 分级计划

| 级 | 内容 | 产物 | 门槛 / 否决 | 成本 |
|---|---|---|---|---|
| **R0**（完成，§11） | 零训练测量：把 `runway_hypotheses` 扩到五个机场；实现基线 B0–B4；B1 的 anytime 曲线；每机场选错跑道的配对代价；数构型切换次数与按天划分后每折的切换数；定 METAR 发布延迟 | readout JSON/表 + 本文 §12 结果表 | 决策点：某机场 B1 在两层上都 ≥ 95 % 时，该机场只报告不建模；否则进入 R1 并据此预注册 R1 门槛 | CPU 小时级，只读 checkpoint |
| **R1** | 独立跑道头（§3.2），按天划分（D1），五机场均衡 | checkpoint + 分层读数 + 校准图 | 过：在两个种子上 NLL 与少数跑道准确率都优于最佳因果基线（具体幅度 R0 后预注册）；否：打不过 B1 → 左右层按"不可约"处理，改为发布两个相邻跑道假设及其概率（§1.2 的选项 c） | 分钟级训练 |
| **R2** | 跑道混合预测：候选专家 + 跑道头；端到端指标；滚动信念与锁定规则实测 | 端到端配对读数；前端跑道扇 | 过：用 top-1 预测跑道的端到端误差优于用 B1 选跑道；"不知道跑道的代价"量化 | 专家按 D1 重训的 GPU 时间 |
| **R3** | 多跑道联合分配 + step 6 调度 | `plan_schedule` 多跑道版读数 | 依赖 step 6 的三个用户决定；指标：分配一致率、间隔损失、延误 | 小 |
| **R4** | 场景级联合意图模型（条件性） | 场景数据集 + 模型 | 只在 R3 测出交互误差时启动 | 大（场景数据构建） |

每一级的实验在设计时就把 campaign 与 run 的意图写进 `docs/experiments/intents.json`，再提交、再启动。

## 8. 代码落点

| 需要 | 位置 | 性质 |
|---|---|---|
| 候选预测 + 选择规则 | `experiments/runway_hypotheses.py` | 扩展（加 B3 / B4、五机场、anytime） |
| 因果上下文特征（构型、METAR、进场扇区、航司） | 新 `data/runway_context.py` | 新建 |
| 跑道头 + runner | 新模块 + `run_ts.py runway_intent` | 新建（R1） |
| 机场固定坐标系 | `coordinate_frame="airport-enu"` | 已有 |
| 指定跑道的程序骨架 | `outputs/plan/skeleton.py::runway_skeleton` | 加一个显式跑道参数（R2） |
| 混合预测与按假设导出 | `inference/`，发布时每个假设一个 `--category-variant` | 新建（R2） |
| 多跑道调度 | step 6 的 `plan_schedule` | 扩展（R3） |
| 按天划分 | `data/splits` | 取决于 D1 |

## 9. 待用户决定

- **D1 数据划分**（卡住 R1）：跑道实验改用按天分块划分。选项：(a) 只给 R 系列用新划分，端到端评估时
  在新划分上重训条件专家；(b) 全项目切换到按天划分——所有 ts 数据集身份改变，已发布结果需要重新产出。
- **D2 范围**：先做 KRDU（左右层代价已知且大），还是五个机场同时做（R0 本身五个都做，成本低）。
- **D3 候选集**：用 v5 名册（缺 KRDU 32、KSMF 35R），还是先跑 v6 harvest。后者会改变所有 ts 数据集的
  划分，并受根 `CLAUDE.md` 的 `--evaluate-only` 删除名册警告约束。
- **D4 跑道头的位置**：独立分类器（建议先做）还是 ts 主干上的一个头。
- **D5 论文表述**：已发布结果在"已知跑道"条件下测得，这一前提写在哪里、怎么写。

## 10. 风险

- 标签只反映最终跑道；进近末段的改派不可见。
- 按航班划分时构型特征会泄露（§6），按天划分后训练天数少（KSMF 只有 26 天）。
- 左右层可能在现有数据下本质不可预测（管制员的均衡决策）。那就如实报告为不可约，并以校准的两个假设
  交付——仍是有效结论。
- 候选集随 harvest 版本变化。
- 场景数据构建（R4）成本高，所以设为条件性。

## 11. R0 结果（2026-09-13）

**范围与产物**。评估航班是各机场当前 plan 头的验证集：KRDU `step3g_fan4_head` 1405 条；KSJC /
KSTL / KSMF / KMSY 的 `step5b_<ICAO>_fan4_head` 分别 1084 / 974 / 522 / 511 条。上下文池不含任何
外部测试哈希的航班（标签和航迹都不读，`data/runway_context.build_airport_context`）。R0a
（`experiments/runway_intent_r0.py`）不用模型；R0b（`experiments/runway_hypotheses.py` v4）用这些
plan 头在其 L−1 锚点对每条候选跑道各预测一次——v5 名册里所有跑道的骨架都能建，没有候选被拒。
产物在 `4dTrajectory/outputs/POOLED/experiments/runway_intent_r0_20260913/<ICAO>/`
（`runway_intent_r0.json`、`hypotheses.json` 和各自的 log），汇总 `readout.md` / `readout.json` 由
`docs/runway_intent_r0_readout.py` 从这些产物重新生成（R0b 在那里用配对的 `summarise` 重算）。
代码 `fc57d80`，复核后修正 `9e3ac49`：代价配对到同一批航班、跨夜间隔不计翻转、锚点航向只看过去。
五个机场的 R0a 都在 `9e3ac49` 上跑。

### 11.1 方向已由上下文解决，左右才是问题

入场时刻（`readout.md` 的 R0a 表）：

| 机场 | n | 方向（B1） | 左右｜方向正确（B1 / B3） | 确切跑道（最好的规则） |
|---|---:|---:|---|---:|
| KRDU | 1405 | 98.4 % | 72.9 / 75.2 % | 74.1 %（B3） |
| KSJC | 1084 | 99.4 % | 95.1 / 92.9 % | 94.6 %（B1） |
| KSTL | 974 | 96.3 % | 90.4 / 88.7 % | 87.1 %（B1） |
| KSMF | 522 | 98.3 % | 60.8 / 65.0 % | 68.8 %（B3） |
| KMSY | 511 | 96.5 % | 无平行跑道 | 96.5 %（B1、B3） |

- **失分都在少数跑道**（入场时刻 B1 / B3）：KRDU 05R 34 / 49 %（n = 82）、23L 34 / 46 %（153）；
  KSMF 17L 33 / 47 %（150）；KSJC 30R 42 / 48 %（91）；KSTL 11 34 / 39 %（64）、12R 38 / 38 %（29）。
- **规则沿进近不变好**：B1 / B3 在覆盖全部航班的各锚点（入场、20 km 到 3 km）之间的变化都在约 3 个
  百分点以内（30 km 锚点只覆盖 19–53 % 的航班，是另一个人群，不比）（KRDU 71.7 → 72.2 /
  74.1 → 74.5 %），因为它们不看本机航迹。航向门限 B2 远处有害（KRDU 入场 45.8 %）、近处才追平 B1
  （10 km 处 73.8 %）。本机航迹是规则没用上的信号——R1 的学习头应当随剩余航程变好，这条 anytime
  曲线是 R1 的主要读数之一。
- **风（B4）处处低于 B1**（KRDU 66.4、KSJC 88.3、KSTL 77.6、KSMF 61.3、KMSY 76.7 %）：同时段落地是比
  风更强的构型线索。

### 11.2 选错跑道的代价（配对，plan 头，L−1 锚点）

每格：跑道准确率 / 同一批航班上相对已知跑道的 FDE 均值变化。

| 机场 | 已知跑道的 FDE 均值 | B1 | B3 |
|---|---:|---|---|
| KRDU | 698 m | 72.2 % / **+509 m**（直线 +402，引导 +655） | 74.4 % / +376 m |
| KSJC | 554 m | 94.5 % / +29 m | 92.8 % / +20 m |
| KSTL | 818 m | 87.3 % / **+648 m** | 87.4 % / +567 m |
| KSMF | 2877 m | 64.4 % / **+492 m** | 68.4 % / +572 m |
| KMSY | 2891 m | 96.5 % / +211 m | 96.7 % / +160 m |

- **按跑道的中位数**（已知跑道 → B1 所选）：KRDU 05R 36 → 1180 m、23L 34 → 1616 m；KSTL 11
  61 → 2233 m、12R 34 → 1020 m；KSMF 17L 215 → 1852 m；KSJC 30R 32 → 212 m。给定正确跑道时
  plan 头的终点几乎总在入口上（结构保证）；跑道一选错，误差就是整段平行间距加错开量。
- **同方向 oracle 的变化为负**（KRDU −307 m）：plan 路径没有镜像对照（镜像入口没有程序），这个数含
  选择噪声，**不能**当跑道知识引用（§1.2）。
- **顺带发现，与跑道选择无关**：plan 头在少见跑道上即使给定正确跑道也飞不好——KMSY 02（n = 29）
  FDE 中位数 23.2 km，KSTL 24（n = 1）23.0 km，KSJC 12L（n = 1）5.7 km；KSMF 17R 已知跑道的中位数
  956 m、17L 215 m（多数跑道 30–70 m）。已记入 `OPEN_ITEMS.md`，不属本计划。

### 11.3 构型翻转（D1 的证据）

15 分钟一格、至少 2 次落地、新方向保持 2 格才算翻转；间隔超过 3 h 的方向变化单独计。

| 机场 | 天数 | 翻转 | 有翻转的天 | 跨 >3 h 间隔的方向变化 | 按天划分：验证 天 / 翻转 | 测试 天 / 翻转 |
|---|---:|---:|---:|---:|---|---|
| KRDU | 57 | 36 | 27 | 3 | 9 / 5 | 7 / 3 |
| KSJC | 56 | 8 | 6 | 4 | 9 / 0 | 7 / 0 |
| KSTL | 42 | 31 | 18 | 4 | 9 / 6 | 3 / 1 |
| KSMF | 26 | 6 | 4 | 3 | 8 / 3 | 2 / 0 |
| KMSY | 30 | 23 | 15 | 7 | 9 / 6 | 2 / 3 |

按天划分后每个验证折只有 0–6 次翻转。但方向层已经由上下文解决（§11.1），要学的左右层是逐机的，
不靠翻转次数，所以按天划分的这个代价不卡 R1。

### 11.4 结论、对待决事项的建议、R1 预注册门槛

- **要建模的是 KRDU、KSMF、KSTL 的左右层**。KSJC 选错的代价约 +20–30 m，只报告不建模；KMSY 没有平行
  跑道、B1 已达 96.5 %，也只报告。
- **对待决事项的建议**（仍由用户拍板）：
  - D1 选 (a)：R 系列用按天划分。plan 头在新划分上重训很便宜（设计文档 step 5b：每机场约 10 分钟
    GPU），所以 R2 的条件专家可以与跑道头用同一划分。
  - D2：R1 五机场均衡池化训练，门槛只看 KRDU、KSMF、KSTL。
  - D3：R1 用 v5 候选（R0b 表明全部可飞）。
- **R1 门槛（预注册）**。都在按天划分的同一验证折上、两个种子，因果基线在同一批航班上重算：
  1. 主门槛：KRDU 与 KSMF 的"左右｜方向正确"准确率比最好的因果规则（本读数里是 B3）高至少 10 个
     百分点；KSTL 不低于它。
  2. 少数跑道（KRDU 05R + 23L、KSMF 17L）准确率 ≥ 60 %（本读数 B3：46–49 %）。
  3. 概率质量：NLL 低于"B1 概率版"（窗口内各候选落地数加一平滑）；ECE ≤ 0.05。
  4. 方向层不低于 B1。
  5. 否决：两个种子上 KRDU 与 KSMF 的左右提升都不到 5 个百分点 → 左右层按不可约处理（§7 R1 的否决
     分支），R2 改为交付两个相邻跑道的校准假设。
- R1 要新增的基线：B1 概率版（门槛 3 用）。

## 12. R1 设计（2026-09-13，D1 已定）

**用户决定 D1 = (a)**（2026-09-13）："按天划分；先针对跑道实验按天划分看一下效果"。所以 R1 同时回答两件
事：跑道头能学到多少，以及按天划分与按航班划分相比差多少（也就是泄露有多大）。

**数据与划分**

- 可用航班 = 到达名册里按航班哈希**不属于**外部测试集的航班；封存的测试航班一架都不碰（和 R0 一样，
  上下文池也不含它们）。
- 日期折：`sha256("1337:day:<日期>")`，15 % 天为测试折、15 % 为验证折、其余为训练折（与 R0 的
  `day_fold` 相同）。**测试折的日子整天不用**——留给最终测试：那时用"落在测试日上的封存航班"，它们既
  没被用过、同一天也没有邻居进过训练。
- 第二个读数：测试折不动，把非测试日重新随机分成训练 / 验证（种子 2024）。梯度提升树本身没有种子方差，
  所以 §11.4 的"两个种子"在 R1 里指这两种日期划分。
- **泄露对照**：同一个跑道头，改用现在的按航班划分训练（训练哈希的航班，同样排除测试折日子）。两个模型
  在**同一批航班**上评估——按航班哈希属于验证、又落在验证日上的航班：按天模型没见过那些天，按航班模型
  见过那些天约 70 % 的其他航班。两者之差就是同日邻居带来的泄露。

**样本与特征**（全部因果：只用锚点时刻之前的信息；剩余航程只用来定锚点，**不**作特征）

- 锚点：与 R0 相同——入场，以及剩余航程 30 / 20 / 15 / 10 / 6 / 3 km；每个锚点一个样本，标签是该航班
  的降落跑道。
- 上下文（`RunwayContext`，锚点之前）：各候选跑道 30 / 60 min 内的落地份额、最近一次落地的跑道与间隔、
  同扇区最近一次落地的跑道与间隔、METAR 风的 u / v / 风速及报文时效。
- 本机：锚点处相对机场参考点的位置、到每个候选入口的沿航向 / 偏航距离、航迹与每条候选航向之差、
  地速、高度、垂直速度、过去 60 s 的位移、入场后时间；入场扇区；航司（呼号前缀，每机场前 15 个）；
  一天中的时刻（正余弦）。
- 模型：scikit-learn `HistGradientBoostingClassifier`，**每机场一个**（不是五机场池化——按实验原则
  (3) 说明：R1 先看每个机场能学到多少，池化留到有效果之后）；固定迭代数、不做早停（早停的随机内部
  验证会跨同一天取样）。

**读数**：每机场、每锚点：确切 / 方向 / 左右｜方向准确率，少数跑道准确率，NLL，ECE；同一批航班上重算
B0–B4 与"B1 概率版"；泄露对照的配对差；排列重要性（哪些信号决定左右）。门槛按 §11.4，**在全部
R0 锚点合并的样本上读**（入场锚点另报，作为最难的一格）——五机场正式运行前写定。

**次要读数（KMSY 试跑后加入，写在正式运行前）**：`day_a_nowx` / `day_b_nowx`——同样的按天模型，但
去掉风和一天中的时刻两组特征。原因：KMSY 试跑时 `day_a` 在入场锚点低于 B1（85.5 % 对 96.5 %），而
`day_b` 高于 B1；逐小时的 METAR 读数几乎每小时唯一，可以当时间戳用，模型借它记住训练日某个钟点的
构型——正是按航班划分会奖励的捷径。去掉后 KMSY `day_a` 入场 95.9 %、合并 98.5 %（最好的规则 97.2 %）。
预注册门槛仍读完整模型。

**代码落点**：`data/runway_features.py`（特征，以及从 R0 挪过来的锚点与航迹几何，一处定义），
`experiments/runway_intent_r1.py`（runner）。

**代码复核后的三处修改（2026-09-13，在正式结果之前）**。第一版五机场运行（`db1e701`）后，opus 复核发现
三个会让数字偏乐观的问题，全部修正后重跑，§13 只报修正后的结果：

1. **日期在 UTC 午夜切开了傍晚的运行时段**：这些美国机场 22Z–03Z 占到达的 27–33 %，UTC 午夜正落在
   傍晚高峰里，一个验证日的傍晚和一个训练日同属一个时段——正是按天划分要去掉的邻居泄露。改为**运行日**：
   在夜间流量最低处切（`operational_day`：UTC 时间减 9 小时取日期；最安静的是 07–11Z）。R0 的日期折和
   翻转统计同样改用运行日并重跑。
2. **锚点位置泄露了真实跑道**：R0 的锚点是"剩余航程 D km"，航程是量到**真实**入口的，所以在锚点处真实
   跑道的沿航向距离恰好约等于 D；平行入口有错开时，模型学会了这个图样（15–20 km 处侧向准确率虚高
   1.5–3.4 个百分点，偏离网格时反而更差）。R1 改用**机场参考点周围的圆环**：入场（25 km 环）以及
   首次进入 20 / 15 / 10 / 6 km 环的样本（`ring_anchors`）——与降落跑道无关、管制员当时就能发出的查询。
   6 km 是最小环：最远的入口离参考点 3.65 km（KSTL 11），所有航班都会穿过它。R0 的规则不读沿航向距离，
   其按航程分格不受影响。
3. **静态多数读了验证日和测试日的标签**：B0、B2 的回退、B4 的"在用"过滤和少数跑道集合原来用全部日子
   的训练哈希落地计数。改为每个划分只用**自己训练日**的航班（`RunwayContext.with_majority`）。

另：复核指出 §11.4 的门槛"读全部锚点合并"几乎必过——近处（10、6 km）航机已对准跑道，位置本身就给出答案，
而规则都不看位置。门槛是事先写定的，不改；§13 同时报告入场锚点（最难的一格）上的同一组判定，作为更有
信息量的读数。另有约 6 % 的航班入场锚点带事后信息（到达片段从**最后一次**进入 25 km 环算起），记为局限。
