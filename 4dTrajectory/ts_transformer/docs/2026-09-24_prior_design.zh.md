# 先验设计（阶段 5，2026-09-24）

**一句话**：先验是一个自回归模型，读航班的背景（机场与候选跑道）和到此刻为止的状态与生效的词，写出下一步
六列词（每列"不变"或一个新词）。它从句子产物里学（教师强制）；以后与执行器接起来，一步说词、一步飞。

词的定义见[指令词表](2026-09-23_instruction_vocabulary_design.zh.md)，执行器见[执行器设计](2026-09-23_executor_design.zh.md)，
框架见[两层模型框架](2026-09-23_two_tier_framework.zh.md) §1、§4。

---

## 0 状态

| 项 | 状态 |
|---|---|
| 目标（用户，2026-09-24） | 执行器能把现在的词表飞出来之后，完成先验的第一次训练；先不做交叉验证定参数，先跑通一次，确保流程通 |
| 本设计 | 第一稿：数据、输入、输出、模型、训练、读数；参数都是第一次的选择（§6），不调 |
| 实现 | 包 `ts_transformer/prior/`，runner `experiments/prior_train.py`（分支 `dev-executor`，`11888357`） |
| 第一次训练 | **完成**（2026-09-24，干净的工作树、`11888357`）：`4dTrajectory/outputs/POOLED/prior/v1_20260924/`。val 每步负对数似然 0.1778，两个基线 0.3444 / 0.3260，每一列都赢（§7） |
| 第二版 | **计划已写（§8，用户 2026-09-24 同意写入）**：输入不再用拟合的速度、第 0 步哪些列当已知条件、每条候选跑道的相对量、候选跑道 token；§8.2 第 2 项的逐列划分用户已确认（跑道预测）；在下一代句子产物上实现和训练 |
| **第三版** | **计划已写（§9，用户 2026-09-24 同意写入），§9.7 的值待用户确认**：用户问了三个问题（泛化、多机、离跑道 24 km 怎么会已许可），查下来第二版第 0 步的条件有一部分是标注器事后读出的，跑道读数被已建立在五边上的航班抬高，划分按航班、没有机场层面和其他飞机的信息。第三版的原则：**输入只给那一刻看得到的东西，标注器读出的词只当预测目标** |

---

## 1 数据

- **来源**：句子产物 `4dTrajectory/outputs/POOLED/instruction_language/v2_20260924/`（规格 `103a6eae6b90`）。train 50,214 架、
  970 万步；val 10,540 架。只读这一份产物，核对规格 sha 与标注器源码 sha（框架 §1："句子产物是唯一的真值来源"）。
- **划分**：train 训练；val 只用于早停和读数；test 不打开。
- **一步** = 句子的一行（2 s）。一架航班的句子是信号的前 `len(words)` 行（在落地前结束）。

## 2 每一步的输入与输出

第一版在第 t 步看到的如下（**第二版的改动见 §8**：这里的航迹、地速、垂直速率含有之后 7.5 s 的信息）：

- **状态**（信号的第 t 行，机场坐标系，米制，按固定尺度缩放）：E、N、MSL 高度、航迹（sin、cos）、地速、垂直速率、
  从第 0 行起的时间；
- **相对生效跑道的量**：离入口沿航道的距离、偏离中线、入口上方的高度、航迹与跑道航向之差（sin、cos）。第 0 步
  跑道指针还没说，这几个量为 0，另给一个标志位——否则第 0 步就从特征里知道了要说哪条跑道；
- **生效的词**（第 t−1 步为止）：六列各一个嵌入；第 0 步用"还没有词"的嵌入；另给每列"上次换词之后过了几步"
  （对数缩放）；
- **背景**（每架一份，加在每一步上）：机场的嵌入；候选跑道的几何（最多 8 条：入口 E、N、高程，跑道航向 sin、cos，
  有效位）。

输出第 t 步的六列词。每列一个分类头，类别 = "不变" + 这一列的全部取值：

| 列 | 取值数 | 类别数 |
|---|---|---|
| 跑道指针 | 候选数（≤ 8，按机场屏蔽无效的） | 9 |
| 进近 | 3（未许可、许可加入、复飞） | 4 |
| 航向 | 72（5° 一档） | 73 |
| 高度 | 181 档 + 下降至落地 | 183 |
| 下降角 | 平飞、4 个下降档、爬升 | 7 |
| 速度 | 47 档 + 未指定 | 49 |

第 0 步六列都写（初始的词），之后每列在约 1–2 % 的步上换词。

## 3 模型

因果 Transformer（只看自己和之前的步），每步的输入是上面各项投影后相加，六个分类头。第一次的尺寸（§6）：
d = 192、4 层、6 头、前馈 768、dropout 0.1，约 200 万参数。按长度把航班分桶成批，不截断（最长 1,133 步）。

## 4 训练

- 损失：六列交叉熵之和（跑道头只在有效候选上做 softmax）。
- 优化：AdamW，学习率 3e-4，权重衰减 0.01，梯度裁剪 1.0。
- 早停：看 val 的平均每步负对数似然，3 个 epoch 不降就停，最多 30 个 epoch，保留 val 最好的一次。
- 产物：`4dTrajectory/outputs/POOLED/prior/<名字>/` 写一次、不覆盖：`checkpoint.pt`、`config.json`（含产物目录、规格 sha、
  标注器 sha、git 状态）、`history.json`（每个 epoch 的 train / val 损失）、`readout.json`。只在干净的工作树上跑。

## 5 读数（这次只读，不设门）

框架 §4 的先验门有四项：困惑度、前 K 覆盖、自由生成能否以落地结束、赢过"重复上一条"和"只看上一个词"。第一次
训练读前两项和两个基线；自由生成要和执行器接起来，放在下一步。

- **每步负对数似然与困惑度**，分列报；
- **换词的步**（真值换了词）：模型给"换词"的概率；取值的前 1 / 前 5 命中率；
- **不换的步**：模型说要换词（取最大概率的类不是"不变"）的比例；
- **两个基线**（都从 train 数出来，在 val 上算同样的负对数似然）：
  - 重复上一条：每列按 train 的换词频率给"换词"的概率，换成什么按 train 各取值的频率；
  - 只看上一个词：换成什么按"这一列上一个词 → 下一个词"的 train 频率（加一平滑）。

## 6 这次定下的选择（都是第一次的值，不经交叉验证）

| 选择 | 值 | 理由 |
|---|---|---|
| 模型尺寸 | d 192、4 层、6 头 | 数据 970 万步，一个小模型先跑通；显卡 8 GB |
| 批 | 按长度分桶，每批约 16,000 步 | 显存 |
| 学习率、衰减、裁剪 | 3e-4、0.01、1.0 | 常用值 |
| 早停 | val 负对数似然，耐心 3，最多 30 epoch | |
| 状态缩放 | 位置 10 km、高度 1 km、地速 100 m/s、垂直速率 10 m/s、时间 600 s | 让各量在 ±几 之内 |
| 不用的输入 | 机型 | 第一次先不加；先看只靠几何与状态能学到多少 |

## 7 第一次训练的读数（val，2026-09-24）

230 万参数，28 个 epoch（第 25 个最好，每个约 58 s，共 27 分钟），train 970 万步、val 205 万步。

| 列 | 模型 | 重复上一条 | 只看上一个词 | 换词的步数 | 换词的步上 P(换) | 取值前 1 | 取值前 5 | 不换的步上误说要换 |
|---|---|---|---|---|---|---|---|---|
| 跑道指针 | 0.0023 | 0.0082 | 0.0082 | 10,540（都是第 0 步） | 1.000 | 0.808 | 1.000 | 0.0000 |
| 进近 | 0.0120 | 0.0222 | 0.0222 | 16,092 | 0.693 | 0.923 | 1.000 | 0.0001 |
| 航向 | 0.0460 | 0.0925 | 0.0832 | 25,827 | 0.507 | 0.557 | 0.843 | 0.0005 |
| 高度 | 0.0184 | 0.0433 | 0.0429 | 18,374 | 0.815 | 0.645 | 0.886 | 0.0007 |
| 下降角 | 0.0452 | 0.1003 | 0.0959 | 39,536 | 0.549 | 0.772 | 1.000 | 0.0020 |
| 速度 | 0.0539 | 0.0778 | 0.0736 | 26,752 | 0.444 | 0.483 | 0.830 | 0.0001 |
| **合计（每步）** | **0.1778** | 0.3444 | 0.3260 | | | | | |

读法：

- 每一列都比两个基线低，合计约低 45 %，每步困惑度 1.19。流程（产物 → 数据 → 训练 → 早停 → 读数）跑通了。
- "什么时候换词"比"换成什么"难：换词的步上模型给"换"的平均概率只有 0.44–0.82（速度、航向最低）；换成什么，前 5
  覆盖 83–100 %。换词的时刻本身有几秒的读法误差（执行器设计 §9 办法 B：高度、角度词读晚 4–6 s，速度词 8–9 s），
  模型学到的时刻也就带着这个误差。
- 跑道指针在第 0 步说，前 1 命中 80.8 %：只靠机场、候选跑道和第 0 步的状态，平行跑道之间还分不清。

## 8 第二版：输入与结构的改动（计划，2026-09-24）

### 8.1 为什么改

1. **速度类的输入含有未来的信息。** 信号里的航迹、地速、垂直速率来自 `flight_scenarios.state_samples_from_track`：对每个
   ADS-B 点，在它前后各 7.5 s 的窗口里做最小二乘拟合（`DEFAULT_WINDOW_S = 15`，repo `docs/open-items.md`）。所以第 t 步的
   这些量已经含有 t 之后 7.5 s 的信息。合成例子：100 m/s、第 0 s 起每秒右转 2°，这样拟合出的航迹在 −2 s 已偏 0.89°、
   0 s 偏 2.30°；只用过去 15 s 拟合的在 0 s 仍为 0。
   - 航向词正好标在转弯开始的那一行，先验可能学成"看到航迹开始弯了才说转"。闭环时执行器要先听到词才会转，这样学到的
     先验会说晚或不说。影响有多大还没量（§8.3 的变体 0 就是量它的）。
   - 位置（E、N、高度）直接取自报告点，没有拟合，不受影响。标注器照旧用拟合：它读完整条航迹产出标签，看到未来是允许的。
2. **没有相对每条候选跑道的量。** 第一版只算"相对生效跑道"的量，第 0 步跑道还没说时为 0。所以第 0 步选跑道时，模型只能
   用绝对位置和拼成一个向量的候选几何，在网络里自己算出相对关系。词表设计 §4.2 本来规定每一步对每条候选都算相对量，
   §4.1 规定候选跑道编码成与排列顺序无关的 token、§4.3 用"本步隐状态对每条候选打分"选跑道；第一版把这些简化了，
   当时没有写明。
3. **第 0 步的航向、速度词是从拟合的速度里读出来的。** 去掉速度类输入后，只有一个位置点，看不出往哪飞、飞多快。

### 8.2 改动（四项）

1. **不输入拟合的速度类量**：去掉航迹（sin、cos）、地速、垂直速率，以及"航迹 − 跑道航向"（它也用拟合的航迹）。
   保留 E、N、MSL 高度、从第 0 行起的时间。飞多快、往哪飞，模型从之前各步的位置里自己看（每 2 s 一个位置）；要不要
   另外给"只看过去"的速度，由 §8.3 在 train 上比。
2. **第 0 步：交接时已知的列当条件，不预测。** 每步输入里"生效的词"从第 0 步起就含这些列；损失和读数里第 0 步只算要
   预测的列。逐列（用户 2026-09-24 确认，跑道预测）：

   | 列 | 第 0 步 | 理由 |
   |---|---|---|
   | 航向 | 已知条件 | 描述交接时的状态（当前航迹取最近一格）；一个位置点看不出方向 |
   | 速度 | 已知条件 | 同上（当前速度的档） |
   | 高度、下降角 | 已知条件 | 交接时正在执行的高度指令（平飞在当前高度，或正下降到某个目标）；它是交接之前定下的，一个位置点也看不出下降的目标 |
   | 进近 | 已知条件 | 进入切片时已在五边上就是"许可加入"，否则"未许可"，由状态决定 |
   | 跑道 | **预测** | 先验要做的决定；靠 8.2 第 3、4 项的相对量和候选 token |

   闭环时这些条件来自交接的场景（观测航班第 0 步的词），和给执行器的初始状态是同一份交接。另一种做法是跑道也当条件
   （真实运行里跑道在进入终端区之前多半已经定了）；这里选预测，保留"选跑道"这一项读数。
3. **每一步对每条候选跑道算相对量**（词表设计 §4.2）：离入口多远（沿航道，入口之前为正）、偏离航道多远（右为正）、
   高出入口多少，都只由位置算；方向那一项（运动方向 − 跑道航向）不用拟合的航迹，改用"上一步位置指向这一步位置"的方向，
   第 0 步没有上一步，记 0 并给标志位；这一项留还是去由 §8.3 比。对所有候选都算，不只算真值跑道，所以不泄露答案。
   第一版"相对生效跑道的量"去掉：跑道说出之后，"生效的跑道"就是那条候选的向量（词表设计 §4.3 "说出之后"）。
4. **候选跑道 token**（词表设计 §4.1、§4.3）：每条候选的几何（入口 E、N、高程、航向 sin、cos、长度）和 8.2 第 3 项的相对量
   经同一个小网络变成这一步的候选向量，与排列顺序无关；汇总进这一步的输入（对候选求和或注意力汇总，实现时二选一，
   写进 §6）。跑道列的输出改成对每条候选打分：分数 = 本步隐状态 · W · 候选向量，另加一个"不变"的分数，在本机场的
   候选上做 softmax（补齐的位置为负无穷）。

**不在这次四项里、另外记下的差别**：词表设计 §4.4 规定同一步里各列依次给出、后一列能看到前面刚选的值，并按规则屏蔽；
第一版六个头互相独立。教师强制下影响不大，但自己采样时同一步可能出现不配的组合（例如给了"许可加入"却没给切入航向）。
和执行器接起来自由生成时一起定。

### 8.3 在 train 上比什么

- **选择用的数据**：从 train 按航班留出一份（例如 10 %，按机场分层）做内部选择集；val 只用于早停和最后的读数，不用来挑变体。
- **变体**（其余都按 8.2）：
  - 变体 0：照第一版给拟合的航迹、地速、垂直速率——**只用来量未来信息的影响，不会用于闭环**；
  - 变体 1：只给位置；
  - 变体 2：位置 + 只看过去的速度（上一步到这一步的位移除以 2 s：地速、运动方向、垂直速率）；
  - 8.2 第 3 项的方向那一项：在变体 1、2 上各试留 / 去。
- **读什么**：内部选择集上第 1 步起的每步负对数似然（分列）；换词的步上给"换"的概率和取值前 1；第 0 步跑道的前 1。
  变体 0 与变体 1、2 在"换词的步上给'换'的概率"上的差，就是先验靠未来信息的程度。
- **怎么选**：在变体 1、2（及方向项留 / 去）里取内部选择集上每步负对数似然最低的；差别小于随机种子之间的差时取输入少的。
  选定后在 val 上读一次（与两个基线比），变体 0 不参与选择。
- 读数口径与第一版不同（第 0 步只算跑道），两版的数不直接比；各自与按同一口径重算的两个基线比。

### 8.4 不变的

词表不变；标注器照旧用拟合的量读句子；模型尺寸、训练设置沿用 §6（交叉验证另做）。

### 8.5 什么时候做

- 数据：下一代句子产物（信号格式 `ts-instruction-signals-v2`，以及词表第三版修订：词表设计 §10）。
  第一版的产物在现在的代码下打不开（信号格式已升），第二版只能在下一代产物上训练。
- 代码可以先写（`prior/data.py` 的输入、`prior/model.py` 的候选 token 与打分头），用合成数据的测试先跑通。

## 9 第三版：输入只给那一刻看得到的东西（计划，2026-09-24）

用户 2026-09-24 看了第二版第 0 步"已许可"的问题后问：这种做法有没有考虑泛化和多机交互？初始状态离跑道 20 km 左右，
怎么会已经批准进近？这一节是回答和改法；用户同意先写进本文，§9.7 的值确认之后再实现。

### 9.1 查到的事实

**句子从哪里开始、"第 0 步已许可"是什么**（train 50,223 架，句子产物 `v3_20260924`，2026-09-24 量，脚本只读）：

- 第 0 步离落地跑道入口的直线距离 p10 / p50 / p90 = 23.1 / 24.1 / 26.4 km（harvest 的 25 km 到达切片）。
- 第 0 步进近词是"许可加入"的有 13,039 架（26.0 %）。其中 **11,941 架第 0 步就已在五边的走廊里**：离中线中位数 37 m、
  航迹偏差中位数 0.4°、高出入口中位数 1,267 m（24 km 处 3° 下滑道是 1,258 m）——已经建立在航道和下滑道上；另外 1,098 架
  第 0 步正在转向航道。各机场：KSJC 57 %、KMSY 28 %、KRDU 20 %、KSMF 11 %、KSTL 9 %。
- 所以许可是我们的窗口开始之前、前一个管制员给的（ILS 截获常在 20 km 以外）。标注器把"已在走廊里"与"正转向、且在用的
  航向能切上航道"读成第 0 步的"许可加入"（`instructions/labeller/lateral.py` `read_lateral`）：前者只看状态就能认出，后者要先
  知道飞机后来切上了哪条线，是事后读出的。

**第二版哪些输入不是那一刻看得到的**（§8.2 第 2 项的第 0 步条件）：

| 第 0 步的条件 | 那一刻看得到吗 |
|---|---|
| 航向、速度（当前航迹、地速取最近一档） | 看得到，但信号里的航迹、地速是居中 15 s 拟合，含之后 7.5 s（§8.1） |
| 高度、下降角（交接时正在执行的高度指令） | **看不到**：下降到哪个高度，是看到后面在哪里改平才读出来的 |
| 进近"许可加入" | 已在走廊里的看得到；**正转向的看不到**（要知道后来切上哪条线） |
| （结果）跑道 | 给了"许可加入"又让模型猜跑道：已建立在五边上的航班，位置就说出了跑道。第二版 V1 在内部选择集上：还没许可的 3,747 架前 1 命中 75.7 %（机场最常用跑道 50.5 %），已许可的 1,275 架 98.0 %（81.4 %），合计 81.4 % 被后者抬高 |

**泛化**：

- 划分按航班随机（`data.splits`）。同一天、同一时段的航班共用机场的跑道构型，分在 train 和 val 两边。跑道意图研究
  （`docs/2026-09-13_runway_intent_plan.zh.md`，下同）的 R1 在按运行日划分下才看到：KSJC 30L 关闭的两天整天落 30R，按
  航班划分训练的跑道头当天只判对 55–57 %，而"入场前 30 min 各跑道落地数取最多"（B1）是 97–98 %；原因是逐小时的风和
  钟点在训练日里成了"今天是哪种构型"的指纹（traps T4）。第二版的输入里没有钟点和风，但同样没有按运行日量过。
- 第 0 步的条件里有事后读出的量（上表），部署时拿不到。

**多机**：

- 先验只看一架飞机。选跑道首先是机场层面的事（当时往哪个方向落、开了哪几条跑道），所有飞机共享；跑道意图 R0 在入场
  时刻（KRDU / KSJC / KSTL / KSMF / KMSY，按航班划分的 val，harvest v5）量到：只用"入场前 30 min 同机场的落地"就能把
  **方向**判对 96.3–99.4 %，难的是平行跑道**左右**（KRDU 73–75 %、KSMF 61–65 %，少数跑道 33–49 %）。这是证据，不是
  本版的结论：对象（句子产物、新 harvest）不同，要在第三版上重量。
- 什么时候许可、引导绕多远、减多少速，很多是为了和前后飞机保持间隔。跑道意图 R3（独立 ETA + 联合调度，五个机场）没有
  在落地时刻上量出交互带来的增益，R4（联合意图模型）因此没有启动；但那是落地时刻，不是逐步的指令词——引导和速度词上
  有没有交互，没有量过。

### 9.2 原则

1. **输入只给"说这一步的词之前，管制员能知道的东西"**，每一项写明来源和时刻（≤ 这一步）：本机过去的位置、机场此前的
   落地、其他飞机此刻的位置。
2. **标注器读出的词只当预测目标**；到上一步为止已说的词是自回归的前文（训练时是标注的词，闭环时是模型自己说的）。
   第一个预测步六列全部预测，不再当条件给。
3. **机场层面的信息与其他飞机单独成项**，给以后的多机（阶段 8）留好接口；本版只填单机能拿到的，第二步（§9.8）再加其他飞机。
4. **按运行日划分**读数，跑道另按"方向层 / 左右层"和"第一个预测步已建立在五边上 / 没有"分开报。

### 9.3 输入

1. **本机的位置历史**：每步的 E、N、MSL 高度、从第一行起的时间（第二版已如此）；运动只从过去的位置算（上一行到这一行的
   位移，§8 的"只看过去的速度"），不用拟合的航迹、地速、垂直速率。
2. **看够过去再开口**：句子的前 `N_look` 行只当观察，不预测；先验从第 `N_look` 行起说话，第一个预测步给出这时生效的六列词
   （由句子里到这一行为止说过的词得出）。这样第一个预测步已有 `N_look` 行的过去位置，能看出方向、快慢和升降，不用改数据
   平面。另一种做法是在信号里加窗口开始之前的位置（航迹在切片之前还有一段，中位数约 45 s，repo `CLAUDE.md`），要改 ts 的
   数据平面和信号格式（新格式名、新句子产物目录），这里不选；列在 §9.7 供用户比较。
3. **每条候选跑道一个向量**（第二版 §8.2 第 3、4 项）：几何、只由位置算的相对量，再加**机场层面的一项**——这条跑道在
   第一个预测步之前 `T_cfg` 分钟内同机场的落地架数，以及离上一次在它上面落地过了多久（没有就记"无"并给标志位）。
   不给每条跑道固定的量（跑道意图 R1.1 的教训：每条跑道的常数会让模型认出"这是 30L"，构型变了就错）。
4. **落地从哪里数**：harvest 的到达清单（`arrivals/manifest.json` 的跑道与落地时刻），全部到达航班，但**不含外层 test 划分的
   航班**（沿用 `data/runway_context.py` 的规矩：test 的标签不进任何上下文）；只取落地时刻早于本机第一个预测步的。
5. **其他飞机（接口）**：每步一个"其他飞机"的集合输入，本版为空（掩码），§9.8 第二步再填。

### 9.4 输出

- 第一个预测步：六列都预测（跑道、是否已建立在五边上、航向、高度目标、下降角、速度）；之后每步照旧（每列"不变"或新词）。
- **同一步里各列按顺序给出**（词表设计 §4.4，第二版记下没做）：跑道 → 进近 → 航向 → 高度 → 下降角 → 速度，后一列看得到前面
  这一步选的值（训练时用真值）。这样"已许可"和跑道不会各说各的，自由生成时组合是配得上的。
- 损失和读数只算预测的步（前 `N_look` 行不算）。

### 9.5 划分与读数

- **划分**：只动开发部分（外层 test 照旧封存，不打开）。train ∪ val 的航班按运行日（`data/runway_context.operational_day`，
  09Z 切分）分组，按天分给 train / 内部选择 / val；比例与现在一致。
- **读数**：每列每步负对数似然、两个基线（§5）；跑道：前 1、前 2，分"方向层 / 左右层"，分"第一个预测步已建立 / 没有"，
  与因果规则比（`data/runway_context.py` 的 B0 机场最常用、B1 此前 30 min 落地最多、B3 同一进场扇区上一架）；换词的时刻与
  取值（§5）。
- **只用过去的检查**（测试）：把第 t 步之后的位置、落地清单里 t 之后的落地随便改，第 t 步的输出不变。

### 9.6 实现之前先量（train，只读）

1. `N_look` 行之后已建立在五边上的航班占多少、各机场多少（`N_look` 的候选值各量一次）。
2. 第一个预测步之前 `T_cfg` 分钟内同机场有落地的航班占多少（夜间可能没有）；B1、B3 在句子产物上的前 1（与跑道意图 R0
   的数对照，只作参考）。
3. 按运行日划分后 train / val 的航班数、每个机场的天数。
4. 每步同机场 50 km 内其他到达飞机的架数分布（给 §9.8 第二步定规模）。

### 9.7 要定的值（每一项待用户确认）

| 项 | 建议 | 理由 / 另一种做法 |
|---|---|---|
| 怎么看过去 | 句子前 `N_look` 行只观察，不改数据 | 另一种：信号里加切片之前的位置——改 ts 数据平面与信号格式，要重建句子产物目录 |
| `N_look` | 8 行（16 s） | 与数据平面拟合窗口 15 s 同长，但只看过去；代价是每句少 16 s（约 1.5 km）的词当目标 |
| 运动的读法 | 上一行到这一行的位移 ÷ 2 s（第二版 §6 已有） | 与第二版相同，不另选 |
| 机场层面的时窗 `T_cfg` | 30 min | 跑道意图 B1 用的值；这里的对象不同，§9.6 第 2 项再看覆盖 |
| 落地的来源 | 全部到达，去掉外层 test 航班 | 与 `runway_context` 的规矩一致 |
| 各列顺序 | 跑道 → 进近 → 航向 → 高度 → 下降角 → 速度 | 词表设计 §4.4 |
| 划分 | 开发部分按运行日（09Z），种子 1337，比例与现在一致 | traps T4；外层 test 不动 |
| 模型尺寸、训练设置 | 与第二版相同（§6） | 只改输入与划分，别的不动，便于对照 |
| 比变体 | 在内部选择集上：有 / 没有机场层面一项；有 / 没有各列顺序 | 规则在跑之前写进本文（同 §8.7） |

### 9.8 分两步

1. **第一步（单机，本版）**：§9.3–§9.5。门：每列都赢两个基线；跑道在"没有建立"的航班上赢 B1 和 B3（左右层单独看）；按
   运行日划分下成立。
2. **第二步（其他飞机）**：第一步读完再写设计。每步把同机场附近其他到达飞机此刻的位置（相对本机、相对各候选跑道）作为
   集合输入；在同一划分上比有 / 没有，看速度词、航向词的换词时刻有没有变好（交互应该出现在这些词上）。数据：到达清单里
   同一时段的其他航班的航迹（没有离场航班，这一点要写进读数）。

### 9.9 与现有产物的关系

- 句子产物 `v3_20260924` 不变（词只当目标）；执行器不变（闭环时从第 `N_look` 行的观测状态起飞）。
- 第二版队列（`outputs/POOLED/prior/v2_20260924/`）照常跑完：它的 V0 量"先验有多依赖未来信息"，是改这版的证据；它的数
  不当第三版的基线（输入和划分都不同），第三版的读数重新量。

---

## 10 下一步

1. 用户确认 §9.7 的值 → 量 §9.6 → 实现第三版第一步（先把 `dev-prior-v2` 并进来）、代码审查、按运行日划分训练、读数。
2. 第二版队列跑完后按 §8.7 写读数（§8.8），作为证据。
3. 先验与执行器接起来自由生成：每步由先验说词、执行器飞 2 s，看能否以落地结束（框架 §4 先验门的第三项）。
4. 第三版第二步：加其他飞机（§9.8）。
5. 交叉验证定尺寸和学习率；加机型。
