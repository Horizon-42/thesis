# 后训练设计（两层模型：先验说词，执行器飞）

**一句话**：预训练让先验学会"管制员在这种情况下一般说什么"（teacher forcing）。后训练让先验自己说词、执行器照着飞，只看飞出来的
结局来改先验：落地了就加强这句话，没落地就减弱；同时用程序（RNAV 进近的高度限制、LPV 下滑道）把不允许说的词先屏蔽掉，
保证飞出来的航迹不违反它们。

预训练、单机自由生成（后训练用的闭环就是它）见[先验设计](2026-09-24_prior_design.zh.md) §7、§9.1；每次运行的结果见
[先验的读数](2026-09-24_prior_readouts.zh.md)；词和执行器见[指令词表设计](2026-09-23_instruction_vocabulary_design.zh.md)、
[执行器设计](2026-09-23_executor_design.zh.md)。本文只写现行的设计；设计是怎么改过来的，看 git 历史和 `docs/CHANGELOG.md`。

**base 模型**：先验第 1 步只用数据（teacher forcing）训出的模型 `outputs/POOLED/prior/v3_step1_20260924/full_s1337`，没做过任何后训练。
各阶段的拉回项都拉回它（用户 2026-09-25 定名）。

文中路径都相对 `4dTrajectory/`（`outputs/…`、`optimization/…`）或 `4dTrajectory/ts_transformer/`（`docs/…` 和包名），仓库根目录的
写明。

---

## 0 状态

| 项 | 状态 |
|---|---|
| 第一阶段：按落地强化（§2） | 代码已合并进 `dev-two-tier`（`e8ee1373`，合并提交 `7e1e2df0`）。正式运行 `outputs/POOLED/prior/v3_rl_20260925/` 在最后一轮：选择集落地率起点 90.0 %，第 1–7 轮 95.1–96.5 %，teacher forcing 负对数似然不变（约 0.280）。跑完后按 §2 的规则选轮、读一次验证集（读数文档 §7） |
| 第二阶段：程序高度的硬约束 + 起始状态扩充（§3–§5） | **设计中**（用户 2026-09-25 提出）。§8 的取值用户 2026-09-25 全部确认；代码没有写。顺序：先在标注数据上验屏蔽（§3.6，不训练），再训练 |
| 第三、四阶段：多机 | 只列要求（§1），等先验设计 §9 第 2–4 步 |
| 闭环监督微调（CAT-K） | 不采用（§6，读数文档 §6） |

---

## 1 后训练做什么，按什么顺序

1. **按落地强化（单机，§2）**：先验自由地说，执行器飞到结局；同一架航班说几句，落地的相对没落地的得到加强。带一个把模型拉回
   起点的项和 teacher forcing 数据项。
2. **程序高度的硬约束 + 起始状态扩充（单机，§3–§5）**：
   - 从训练集航班的起始状态出发，加一点随机变化，得到"情况相似、但不在训练集里"的起始状态；只靠终点奖励和拉回 base 模型的项
     来训练，提高没见过的情况下的落地能力（用户 2026-09-25）。
   - 为了不让模型完全自由发挥，RNAV 进近的高度限制和最后进近段 LPV 下滑道的下沿作为硬约束，做成屏蔽：说出来会让飞机低于它们的
     词不许说（用户 2026-09-25：高度限制作屏蔽；LPV 最好也做成硬约束，参考优化器的做法）。屏蔽照顾不到的，由飞出来的航迹再查一遍，
     违反了这架就算失败（§3.5）。
3. **间隔作为生成时的筛选和屏蔽（多机，先验设计 §9 第 5 步）**：不作损失。门槛取规章的最小间隔（FAA JO 7110.65 的雷达间隔和尾流
   间隔，原文在 `docs/literature/arrival_separation/`），并以记录数据里本来就有的低于最小间隔的比例作参照——比数据"更安全"就离开了
   数据。屏蔽要在同一步里按顺序（例如按落地顺序，前机先说）算，才能看到别的飞机这一步选了什么；预训练仍是各架同时说。
4. **多机的强化**：奖励用间隔与落地，拉回项同第 1、2 项；不用只有间隔的奖励。

**各阶段共同的规矩**：

- 闭环就是先验设计 §9.1 的单机自由生成：每架航班的前 `N_look` = 8 行用观测的位置，执行器从第 8 行的观测状态起飞；先验每 2 s
  按"跑道 → 进近 → 航向 → 高度 → 下降角 → 速度"的顺序逐列采样，带词表的相容规则屏蔽和跑道锁；执行器飞到判定结束。
- 训练只用训练集运行日；选轮用内部选择集运行日；验证集每个阶段只在定稿后读一次；**测试集运行日一架都不读**。
- 每个阶段的门先只记录（与先验设计 §9.1 相同），不设通过线。

---

## 2 第一阶段：按落地强化（单机）

- **为什么**：让先验自由地说，只看结局——落地了，就提高这句话里各个词的概率；没落地，就降低。这正对准"说到飞机落地"这个目标，
  也不需要回答"闭环轨迹偏开之后标注此刻该说什么"（CAT-K 就败在这里，§6）。信号是现成的：验证集自由生成里，每架航班说 4 句，
  591 架（30 %）是有的落地、有的没落地（读数文档 §5.2）。
- **起点**：base 模型。
- **每一轮**：
  1. **自由生成**：从训练集运行日抽航班（有自己动力学的，与回放门同一组），每架从第 `N_look` 行的观测状态起飞，先验说 `K` 句
     （温度 1，与先验设计 §9.1 完全相同的屏蔽与跑道锁），执行器照着飞到判定结束。
  2. **奖励**：落地（执行器判定的 `landed`）并且落在"机场此刻的落地方向"上的跑道，得 1；其余结局（超时、偏出跑道、未截获、
     触地、动力学失败，或落在反方向的跑道上）得 0。**机场此刻的落地方向**：第一个预测步之前 30 分钟里有落地的跑道（与输入里的
     落地情况同一来源，去掉本机自己的落地、去掉测试日），与其中任何一条航向相差小于 90° 的跑道都算；这 30 分钟里没有落地时，
     任何跑道都算。不要求落在观测的那条跑道上——那是数据项的事。
  3. **同一架航班的几句互相比**（GRPO 的做法；R1Sim 在 CAT-K 之上用过，`docs/literature/trajectory_as_language/` C3）：每句的
     "优势" = 它的奖励 − 同一架航班 `K` 句奖励的平均。全部落地或全部没落地的航班，优势都是 0，不参与更新——只在有对比的航班
     上学，本来就会落地的航班不被推向更确定。
  4. **损失**，三项相加：
     - **奖励项**：− 优势 × 这句话每一步六列所说的词（含"不变"）的对数概率之和，除以这句的步数；一句话里所有步用同一个优势
       （分不清是哪一步说错了，这是这种做法的代价，靠句数平均掉）。概率用模型不加屏蔽的分布（屏蔽掉的概率每步只有约 0.06 %，
       读数文档 §5），训练时关掉 dropout（模型内部的随机屏蔽）：打分用的就是采样时的那个分布，拉回项量的是权重变了多少，
       不是 dropout 的噪声（第一次更新时它正好是 0）；
     - **拉回项**：β × 这句话上，模型与 base 模型（就是起点）的差异（按采到的词估计的 KL 散度，GRPO 的做法）——防止为了落地离开原来的说法；
     - **数据项**：λ × 训练集运行日的 teacher forcing 负对数似然（与预训练相同的损失，每次更新配一批）——防止忘掉真实的说法。
  5. 每轮在这一轮的句子上过一遍（只用当前模型生成的句子，不累积：奖励项要求句子来自正在训练的模型）。
- **选哪一轮**：每轮之后在内部选择集上自由生成（每机场 200 架 × 2 句，每 64 架一批，每轮同一批航班、同一个种子；第 0 轮因此与
  CAT-K 的第 0 轮逐位相同），先排除"护栏"变差的轮：落地在观测跑道上的比例比起点低 2 个百分点以上，或每架的航向词数比起点多 20 %
  以上；剩下的里面落地率最高的一轮胜出，差 1.5 个百分点以内取更早的。同时列出 teacher forcing 下选择集每步的负对数似然、落地时刻
  与观测之差、落在反方向跑道的比例。
- **验证集**：选定的那一轮只读一次（每机场 400 架 × 4 句，与先验设计 §9.1 的验证集读数同一批航班、同一个种子），与 base 模型在同一
  代码上的验证集重读（读数文档 §6）并排。
- **不做的**：不用落在观测跑道上作奖励（那会把"强化落地"变回模仿）；不加间隔项（单机没有别的飞机，到多机时再加）；不用价值网络
  （奖励只有句末一个，同组比较已经给出基线）。
- **代码**：runner `experiments/prior_landing_reward.py`（runners R19），奖励 `prior/landing_reward.py`，训练 `prior/train.py`
  `RewardTuner`。

---

## 3 第二阶段（一）：程序高度的硬约束

### 3.1 为什么是屏蔽，而且是硬的

- 用户的规矩：从程序算出来的航迹不能当模型的答案（不当目标、不当奖励）。屏蔽不告诉模型该说什么，只把"说了就会让飞机低于程序
  高度"的词拿掉，剩下的仍由模型自己选——它是可行性，不是答案。词表设计 §5.1 本来就规定程序规则"只做屏蔽和评估"；本阶段把 LPV
  下滑道的下沿也从"只用来评估"改为屏蔽（用户 2026-09-25）。
- 文献（`docs/literature/procedure_hard_constraints/notes_B.md` §2.2、§7）：每一步把"安全的动作集合"交给学习者的做法叫前置屏蔽
  （Alshiekh 2018）；屏蔽在训练和使用时都要开着——只在使用时开，模型没见过屏蔽把它带进去的状态（Geiger & Straehle 2022）；训练和
  使用都开时，最宽松的正确屏蔽不损失性能（Oh & Fisac）。所以本阶段的训练、选轮、验证集读数都开着屏蔽，以后用这个模型时也开着。
- 屏蔽只看"此刻说这个词会怎样"，照顾不到以后的所有情形（例如截获航道之前飞的路比直线长）。照顾不到的，由飞出来的航迹再查一遍
  （§3.5）：违反了这架就算失败，奖励为 0。这就是用户说的"屏蔽不行时作为奖励的一部分"，只用在屏蔽之后剩下的情形上。

### 3.2 约束从哪来

- **RNAV 进近的高度限制**：FAA CIFP 编码的 RNAV(GPS) 进近（前端的 `procedure-details/<uid>.json`），读取器
  `flight_scenarios.procedure_final.procedure_skeleton`（仓库根目录）：最后进近段（IF → FAF → 入口）的各定位点和每条过渡段
  （IAF → … → 汇入最后进近段的定位点），每个定位点带它的高度限制（不低于 / 不高于 / 正好 / 区间）。五个机场的 25 条候选跑道
  **全部**有这份文件。
- **25 km 以内只有"不低于"**：编码的"正好"限制都在 IAF 上，离入口 26–62 km（KRDU OTTOS、BUTTS、ESELL，KSMF TENCO、MYNRR），在我们
  数据的 25 km 范围以外。所以本阶段只有下限，没有上限。
- **下滑道**：入口高程 + 入口跨越高度 TCH + d · tan(下滑角)，d 是沿航道到入口的距离；TCH 与下滑角用执行器和 evaluation 读的同一份
  数据（执行器设计 §5.2，`autopilot/runway_data.py`）。23 条跑道公布的是 LPV，KRDU 32 与 KSMF 35R 公布的是 LNAV/VNAV，垂直路径是
  同一种，evaluation 也按同一种判（evaluation EV2），这里同样处理。
- **FAF 的下限与下滑道在 FAF 处的高度几乎相同**（FAF 本来就是下滑道的切入点）：25 条里 24 条相差在 −2…+11 m 之内，KSJC 30L 的 FAF
  下限比下滑道低 31 m。所以 FAF 以外用 RNAV 的阶梯下限、FAF 以内用下滑道的下沿，两段在 FAF 处接得上。
- **优化器的做法**（`optimization/approach_constraints/`，README §4b；4dTrajectory 索引 K5、K7）：FAF 以外，每条航段上的下限取该段
  终点定位点的"不低于"高度（阶梯）；FAF 以内，下滑道窗口 [−60 m, +120 m]（`DEFAULT_GLIDEPATH_BELOW_M = 60`、
  `DEFAULT_GLIDEPATH_ABOVE_M = 120`），最后进近段不再用阶梯下降点。本阶段取同样的两段：
  - 下滑道只取下沿（"LPV minimum" 读作不低于下滑道 60 m；上沿不屏蔽——偏高不是越障问题，且上沿会逼飞机下降，用户 2026-09-25 确认）；
  - FAF 以内的阶梯下降定位点（12 条跑道有，例如 KRDU 05R AMORT 2.6 km 不低于 274 m）不用，与优化器相同——它们与下滑道在该点的高度
    相同，是给没有垂直引导的 LNAV 用的（我的读法；不用它们，用户 2026-09-25 确认）；
  - 常数从优化器照抄并注明"必须一致"，加一个测试核对（ts 包不能导入优化器的包，`outputs/guidance/skeleton.py` 与
    `tests/test_guidance_skeleton_mirrors.py` 就是这样做的）。
- **气压高度与几何高度**：程序的数字是气压高度（按当地高度表拨正），我们的高度是几何 MSL。两者之差约为"离机场的高度 × 当天气温偏差 / 273"
  （词表设计 §2.4）：在 FAF 附近（离机场约 550 m）气温偏差 ±20 K 时约 ±40 m。仓库里的 METAR 只有风（`data/metar/`，没有气温），这一版
  直接比较，差别由容差（§3.4）吸收，并在标注数据上量（§3.6）。
- **没有的**：MVA（最低引导高度，雷达引导时的下限）仓库里没有数据，本阶段不屏蔽；过渡段和最后进近段都没有编码速度限制。

### 3.3 下限怎么随飞机的位置变

对这一步生效的跑道（这一步跑道列采样之后），设 d = 沿该跑道航道到入口的距离（入口前为正），xt = 离延长中线的横向距离：

| 飞机在哪 | 下限 `floor(p)` |
|---|---|
| (a) FAF 以内（d ≤ d_FAF），且在最后进近航道的 LPV 扇形里（|xt| ≤ 扇形半宽，`flight_scenarios/fas_geometry.py`：入口处 107 m，10 km 处约 460 m） | 下滑道高度(d) − 60 m |
| (b) FAF 以外，离最后进近段的航段（IF → … → FAF）不超过 1 NM（RNP APCH 的横向容差，`RNP_HALF_WIDTH_M`） | 阶梯：d 以内最近的那个最后进近段定位点的"不低于"高度（`RunwaySkeleton.floor_altitude_m(d)` 的读法） |
| (c) 不在 (b) 里，离某条过渡段的航段不超过 1 NM | 该航段终点定位点的"不低于"高度（几条都挨着时取最高的） |
| (d) 其他地方（雷达引导的空域） | 没有下限（管 MVA，仓库里没有数据） |

KRDU 05L 的例子：离入口 20.8 km 的 BOULE 不低于 1,219 m；到 IF（SCHOO，15.9 km）不低于 914 m；到 FAF（WEPAS，10.3 km）不低于 671 m；
FAF 处下滑道高 671 m，以内按下滑道减 60 m 一路降到入口。

**另外两种划法，在第 0 步一起量（§3.6）**：
- **更严**：不看横向，(b) 的阶梯按 d 投影到任何位置都算（优化器在各航段上也是按投影算的，但优化器的航迹本来就在程序上）；
- **更松**：(b)(c) 只对已许可进近的飞机算（管制说"保持某高度直到建立"，许可之前飞机听的是管制高度，不是程序高度）。

按"在标注数据上通过的最严的一种"取（§3.6）。

### 3.4 屏蔽规则（每一步，跑道列采样之后）

1. **高度词 T**：只许 `T ≥ floor(p) − 15 m`（半档：让含有下限的那一档仍可说）。为什么够：执行器下降中和到达后都不低于 T − ε
   （ε = 25 m，词表设计 §2.4、§2.5）；下限朝跑道只降不升，所以此刻合法的 T，飞机往跑道飞时一直合法。
2. **"下降至落地"**：从飞机此刻的位置到越过点（入口上方 TCH）画一条直线；只许这条线在前方每个定位点处（d_F < d）不低于该点的
   下限 − 15 m，并且（飞机在 FAF 以外时）在 FAF 处不低于下滑道 − 60 − 15 m。为什么够：截获航道之后，执行器下降不比"到越过点的直线"
   更陡（执行器设计 §5.2），航迹在这条线上或线上方；直线是直的，阶梯在两个定位点之间是平的，所以只查定位点处就够了；到了 FAF 以内，
   这条线终点在下滑道上（入口处的 TCH），起点不低于下沿，整段都不低于下沿。截获之前飞机飞的路比直线长，这条保证不成立，由 §3.5 管。
3. **高度列的"不变"**：生效的那个词如果在此刻的位置上已经不满足第 1 或第 2 条（例如飞机从 (d) 进入 (b)，生效的目标低于这里的下限），
   就屏蔽"不变"，先验这一步必须说一个新的高度词。第一个预测步本来就屏蔽"不变"。
4. 下降角列、其他列：只有词表原来的相容规则，不加新的屏蔽。
5. 屏蔽之前模型放在被屏蔽的词上的概率照样记下（与先验设计 §9.1 记录相容规则屏蔽的做法相同），分规则、分区域报：看它自己学会了
   多少，也看它是不是越来越依赖屏蔽。

屏蔽、下限和位置的计算都写在先验这边（新模块 `prior/procedure.py`），读的是先验本来就读的执行器位置、自己说过的词（是否已许可）和
候选跑道的几何；执行器一行不改（规格仍是 `executor/v7_20260925`，执行器只接受词）。

### 3.5 飞出来的航迹再查一遍

- 每飞完一步（2 s），查这一行：高度 < `floor(p) − 40 m`（15 m 半档 + 执行器的 25 m 容差）就结束这架，结局记为"低于程序高度"，
  奖励 0。在 runner 里查，不改执行器的判定。
- 读数里按下限的来源（RNAV 阶梯 / 下滑道下沿）、按"截获前 / 截获后"分开报；如果屏蔽有效，这一项应该很少。
- **起点已经低于下限的航班**（观测的前 8 行里就有一行低于下限 − 40 m，且在 (a)(b)(c) 里）：不作训练的起点；选择集、验证集读数里
  单独列一类，不参与选轮。

### 3.6 先在标注数据上验（第 0 步，不训练）

屏蔽写的是"管制员和飞行员本来就遵守的规则"，那么真实的航迹和标注的词应该几乎都满足它。在训练集运行日上，对 §3.3 的三种划法各量：

- 标注的高度词里，被第 1 条屏蔽的比例；标注的"下降至落地"里，被第 2 条屏蔽的比例；被第 3 条强制换词的步数；
- 观测航迹有一行低于下限 − 40 m 的航班比例，分 (a)(b)(c)、分机场 × 跑道、分"许可前 / 后"；
- 执行器照标注的句子飞（回放），被 §3.5 结束的航班比例。

**取哪一种（量之前写定）**：标注的高度词被屏蔽 ≤ 1 %、回放被结束的航班 ≤ 1 % 的里面，取最严的一种；三种都不过，就带着数字回来找
用户，不自己放宽容差。之后用第一阶段选定的模型，在内部选择集上开着屏蔽自由生成一次（不训练），与不开屏蔽的读数并排：屏蔽本身
让落地率、每架的词数变了多少。

---

## 4 第二阶段（二）：起始状态扩充

### 4.1 起始状态是什么

一架航班的起始状态 = 观测的前 8 行（位置、高度、航迹角、地速、垂直速度、时刻）+ 执行器在第 8 行的初始状态（位置、高度、速度、
航向、航迹倾角、质量）+ 航班的背景（机场、运行日与进入时刻——决定输入里的落地情况和奖励的落地方向——、机型与动力学）。

### 4.2 怎么变

从训练集运行日的一架航班（有自己动力学的，与回放门同一组）出发，对 8 行和执行器初始状态做**同一个**变换：

- **绕机场转 Δφ**：Δφ 在 ±15° 里均匀取。离机场的距离不变（仍在 25 km 的进入圈上），航迹方向跟着转，所以"朝机场飞"的关系不变；
  ±15° 在 25 km 处约 ±6.5 km 的弧长，进场的方向变了，但还是同一类进场。
- **高度平移 Δh**：±150 m 里均匀取（5 个高度档；约为 1,000 ft 垂直间隔的一半）。
- **速度缩放 1 + κ**：κ 在 ±5 % 里均匀取；地速、垂直速度和执行器的速度乘同一个系数，前 8 行的位置以第 8 行为中心按同一个系数拉伸
  （p_i' = p_8 + (1 + κ)(p_i − p_8)），2 s 一行的间隔不变。
- 不变的：机场、运行日与进入时刻（所以落地情况和"机场此刻的落地方向"是源航班的）、机型、质量与动力学。

### 4.3 合格检查（不合格就重抽，连续 10 次不合格就换一架源航班）

- 8 行和第 8 行的执行器状态都不低于 §3.3 的下限；
- 高度在这个机场训练集航班进入时高度的 1–99 百分位之间（只用来判断"情况相似"，不进模型）；
- 速度在这架航班机型的可飞包线里（执行器设计 §2.2，现有的数）；
- 读数报每个机场的重抽比例、换源航班的比例。

### 4.4 数据规矩

扩充的起始状态只从训练集运行日的航班造。内部选择集上另造一套固定的扩充起点（固定种子，每轮相同），专门读"没见过的情况"；
验证集同样造一套，定稿后只读一次。测试集运行日一架都不读。

---

## 5 第二阶段（三）：训练与读数

- **起点**：第一阶段选定的那一轮（第一阶段的提升由此带进来）。**拉回的对象**：base 模型（用户 2026-09-25 确认）——各阶段都拉回
  同一个 base，说法不会一个阶段一个阶段地漂走。拉回不是退回：它只按"离 base 多远"罚一个小的量（β = 0.04），与落地的奖励相抵；
  带拉回项时最好的模型是"base 的说法里，把能落地的句子的概率放大"，所以为落地做的改变被保留，被拉回的只是与落地无关的漂移。
  第一阶段也是这样拉回 base 的，照样从 90.0 % 提到 96.5 %，而选中那一轮与 base 每步只差约 0.01（按采到的词估计的 KL）。
- **每一轮**：2,000 个扩充起点（每机场 400，每个起点来自随机抽的一架训练集航班、一次新的变换），每个说 `K` = 8 句；开着 §3.4 的屏蔽
  和 §3.5 的检查；奖励同第一阶段，"低于程序高度"得 0；优势同第一阶段（同一起点 8 句互相比）。
- **损失**：奖励项 + β × 拉回项，**不要数据项**（用户：只靠终点奖励和 base 模型的拉回；扩充的起点也没有标注）。句子是从屏蔽后的分布
  采的，所以奖励项和拉回项都用屏蔽后重新归一的分布算（当前模型与 base 模型在同一个允许集合上比）；第一阶段用不加屏蔽的分布，是因为
  那里屏蔽掉的概率只有约 0.06 %，这里高度词被屏蔽的概率可能不小。被屏蔽的概率不进损失，只记录（§3.4 第 5 条）；如果它一轮轮变大，
  再考虑加一项把它压下去（文献里"依赖屏蔽"的修法，`notes_B.md` §7）。
- **优化与轮数**：同第一阶段（AdamW，学习率 1e-5，dropout 关，每轮在这一轮的句子上过一遍），8 轮。
- **选哪一轮**：每轮之后读两份，都开着屏蔽：内部选择集的真实起点（每机场 200 架 × 2 句）和内部选择集的扩充起点（每机场 200 个 × 2 句，
  §4.4）。先排除护栏变差的轮：真实起点上落地率比第 0 轮低 1 个百分点以上；落地在观测跑道上的比例低 2 个百分点以上；每架航向词多
  20 % 以上；teacher forcing 下选择集每步的负对数似然比第 0 轮高 10 % 以上（没有数据项，要看它离数据多远）。剩下的里面，扩充起点上
  落地率最高的一轮胜出，差 1.5 个百分点以内取更早的。
- **验证集**：选定的那一轮只读一次：真实起点（每机场 400 架 × 4 句，同先验设计 §9.1 的那批航班和种子）和验证集的扩充起点（每机场
  400 个 × 4 句），都开着屏蔽；与第一阶段选定的模型在同样的屏蔽、同一批起点上并排。
- **读数**（写进读数文档）：结局分布（多一类"低于程序高度"），分真实 / 扩充、分机场 × 进近方式；被屏蔽的概率（分规则、分区域）；
  飞机过 FAF 时高度减下滑道高度的分布，与观测比；落地的航班过 DA（按跑道公布的决断高度，
  `trajectory_data_process/config/runway_thresholds.json`）时在不在下滑道窗口 [−60 m, +120 m] 里，与观测比；每架每列的词数；落地时刻与观测之差；落在反方向跑道的比例。

---

## 6 不采用：闭环监督微调（CAT-K）

- **做过什么**：每架航班同时飞 4 个分支，都由先验自己说词，每 20 s 留下离观测航迹最近的一支，其余换成它；把标注的词按"标注说词的
  那些行"对齐到留下的这条链上作目标（说早、说晚各容忍 10 s，许可进近一直要求到说出），只用链上的数据训练，8 轮（代码 `dev-prior-v3`
  `e760ab1d`：runner `prior_closed_loop`，`prior/relabel.py`；runners R18）。
- **为什么不采用**（读数文档 §6）：文献里闭环监督微调的收益大且复现过（先验设计 §11），但那里一个词就是 0.5 s 的一段运动，"让车回到
  真实位置的词"可以从当前状态直接算出来。我们的词是交给执行器的目标，效果要十几秒才显出来，目标只能取"标注在那个时刻说了什么"；
  而链离观测越飞越远（留下的分支 220 s 时中位数偏 1 km），标注"此刻许可、此刻转向"在链上的飞机那里往往不成立。模型学成"早许可、
  少说话"：验证集上直线进近 94.0 → 99.7 %，被引导的 83.3 → 81.0 %（偏出跑道 4.8 → 9.5 %），航向词只有标注的一半；teacher forcing
  负对数似然 0.284 → 0.452，8 轮后 1.114。按落地强化只看结局，不需要这种对齐。
- **代码**：已归档（用户 2026-09-25）到 `archive/closed_loop_sft_2026_09/`（分支 `dev-prior-fast` `e5f18692`，README 说明搬了什么）；
  逐行编码（`Prior.extend`）、`chain_record` 留在活代码里，自由生成和第一阶段用到它们。

---

## 7 不做的

- 不用程序算出的航迹作目标或奖励（不奖励"离下滑道近"）：屏蔽只拿掉不可行的词。
- 不屏蔽 MVA（没有数据）、不屏蔽上限（25 km 以内没有编码的上限）、不屏蔽下滑道上沿。
- 词表设计 §5.1 的其他几条（切入角、切入点离进近门的距离、切入时不高于下滑道、9,260 m 以内不指定速度、3,048 m 以下的速度上限）
  这一阶段不做；要做时按同样的办法（屏蔽 + 事后查）加。
- 不改执行器。

---

## 8 取值

| 项 | 取值 | 状态 | 理由 / 另一种做法 |
|---|---|---|---|
| **第一阶段** | | | |
| 起点 | base 模型 | 已定 | CAT-K 不采用 |
| 每架采几句 `K` | 8 | 在用，未逐项确认 | 验证集上 4 句时 30 % 的航班有对比；8 句对比更多。每轮 2,000 架 × 8 句，约半小时（与别的运行同时跑时约 45 分钟） |
| 每轮航班、轮数 | 每机场 400 架（每轮 2,000 架），8 轮；每轮换种子抽 | 在用，未逐项确认 | |
| 奖励 | 落地且在机场此刻的落地方向上 = 1，其余 = 0 | 在用，未逐项确认 | §2；"此刻的落地方向"：前 30 分钟有落地的跑道，航向差 < 90° 的都算，没落地时任何跑道都算 |
| 优势 | 奖励 − 同一架航班 `K` 句的平均，不除以标准差 | 在用，未逐项确认 | 除以标准差会放大只有一句落地（或只有一句没落地）的航班；先不除，读数里报两种航班各占多少 |
| 拉回项 β、数据项 λ | β = 0.04，λ = 1 | 在用，未逐项确认 | β 取 GRPO 原论文（DeepSeekMath）的值——凭记忆，还没在本仓库的文献里核对；λ = 1 让数据项与预训练同一量级 |
| 优化器 | AdamW，学习率 1e-5，前 20 步预热，权重衰减 0.01，梯度裁剪 1.0；每批约 16,000 步（奖励项和数据项各占一半），dropout 关 | 在用，未逐项确认 | 学习率是预训练的 1/30：奖励项的梯度方差大 |
| 护栏 | 落地在观测跑道上的比例降 ≤ 2 个百分点，每架航向词数增 ≤ 20 % | 在用，未逐项确认 | 防止学成"绕来绕去才落地"或乱挑跑道 |
| **第二阶段：约束** | | | |
| 高度限制的来源 | CIFP 的 RNAV(GPS) 进近，`procedure_skeleton`；25 km 以内只有"不低于" | 已定（用户 2026-09-25） | §3.2；25 条候选跑道全有 |
| "LPV minimum" 的读法 | FAF 以内不低于下滑道 60 m（优化器的下沿） | 已定（用户 2026-09-25） | 另一种读法：公布的决断高度 DA。DA 是下滑道上的一个点（飞行员在这里决定落地还是复飞），不是航迹的下限，只能当"到 DA 时是否已稳定"的单点检查；它按跑道公布（接地带上方 61–125 m，多数 61 m），已按跑道存在 `trajectory_data_process/config/runway_thresholds.json`（量的是接地带以上，不是入口以上）。DA 只作读数（§5）：落地的航班过 DA 时在不在下滑道窗口里，不进屏蔽和奖励 |
| 下滑道上沿 | 不屏蔽 | 已定（用户 2026-09-25） | 优化器还有 +120 m 的上沿；偏高不是越障问题，屏蔽它会逼飞机下降 |
| FAF 以内的阶梯下降定位点 | 不用 | 已定（用户 2026-09-25） | 与优化器相同；它们与该点的下滑道高度相同，是给 LNAV 用的（我的读法） |
| 下限按位置怎么算 | §3.3 的 (a)–(d)，横向容差 1 NM，LPV 扇形用 8260.58D 的公式 | 已定（用户 2026-09-25） | 第 0 步与"更严（只按投影）"和"更松（只对已许可的）"一起量，取通过的最严的一种 |
| 词的容差 | 15 m（半档） | 已定（用户 2026-09-25） | 让含有下限的那一档仍可说 |
| 航迹检查的容差 | 40 m（15 m + 执行器的 25 m） | 已定（用户 2026-09-25） | 气温造成的气压 / 几何高度差在 FAF 附近约 ±40 m，第 0 步量 |
| 第 0 步的通过线 | 标注的高度词被屏蔽 ≤ 1 %，回放被结束的航班 ≤ 1 % | 已定（用户 2026-09-25） | 三种划法都不过时回来找用户 |
| 违反后 | 这架结束，结局"低于程序高度"，奖励 0 | 已定（用户 2026-09-25） | 屏蔽照顾不到的情形才会走到这里 |
| **第二阶段：起始状态扩充** | | | |
| 变换 | 绕机场转 ±15°，高度 ±150 m，速度 ±5 %，均匀取，对 8 行和执行器初始状态做同一个变换 | 已定（用户 2026-09-25） | §4.2；另一种做法：只换进入时刻（换一个落地情况） |
| 每轮起点里扩充的比例 | 100 % | 已定（用户 2026-09-25） | 真实起点第一阶段已经训过；另一种做法：一半真实、一半扩充 |
| 合格检查 | §4.3 | 已定（用户 2026-09-25） | |
| **第二阶段：训练** | | | |
| 起点、拉回对象 | 起点 = 第一阶段选定的一轮（做过按落地强化）；拉回 = base 模型 | 已定（用户 2026-09-25） | 第一阶段也是拉回它；本阶段没有数据项，拉回项是唯一把模型拴在数据上的东西。另一种做法：拉回第一阶段选定的模型（只管本阶段自己的变化，各阶段的偏离会累加） |
| 损失 | 奖励项 + β 拉回项，β = 0.04，没有数据项；都按屏蔽后的分布算 | 已定（用户 2026-09-25） | §5 |
| 每轮、轮数、`K`、优化器 | 2,000 个起点，8 轮，`K` = 8，同第一阶段 | 已定（用户 2026-09-25） | |
| 选轮 | 扩充起点上的落地率；护栏见 §5 | 已定（用户 2026-09-25） | 目标是没见过的情况下的落地 |

---

## 9 依据

- **优化器**：`optimization/approach_constraints/README.md` §4b（FAF 以外阶梯、FAF 以内下滑道窗口、FAF 处的下限）；
  `optimization/approach_constraints/segments.py`（`DEFAULT_GLIDEPATH_BELOW_M`、`LpvFinalSpec`、最后进近段没有阶梯）；
  `aeroviz_backend/procedure_segments.py`（仓库根目录，每条航段的下限取终点定位点）；LPV 扇形 `flight_scenarios/fas_geometry.py`
  （FAA Order 8260.58D Formula 3-1-1）。
- **程序数据**（2026-09-25 量，`procedure_skeleton`）：FAF 离入口 7.7–15.1 km；FAF 下限减去下滑道在 FAF 处的高度 −31…+11 m
  （24/25 条在 −2…+11 m）；12 条跑道在 FAF 以内有阶梯下降定位点；25 km 以内没有编码的上限。
- **屏蔽的文献**：`docs/literature/procedure_hard_constraints/`（Alshiekh 2018 的前置屏蔽；Geiger & Straehle 2022、Oh & Fisac 关于
  训练与使用都开着屏蔽；Pizarro Bejarano 关于"依赖屏蔽"），`notes_B.md` §2.2、§7。
- **按结局强化**：GRPO（DeepSeekMath）；R1Sim、SMART-R1 在闭环微调之上做强化（`docs/literature/trajectory_as_language/`、
  `docs/literature/multi_agent_interaction/`）。
