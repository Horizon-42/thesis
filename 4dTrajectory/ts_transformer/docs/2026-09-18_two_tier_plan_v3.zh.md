# 两层计划 v3：分段执行器与第二层（草案，逐项审核中）

**状态**：草案，2026-09-18 下午起与用户逐项过；每一节的"审核"栏由用户定。在本计划签字前不启动任何实验
（用户 2026-09-18："先停下所有实验。我们需要认真敲定计划"）。取代 `2026-09-18_manoeuvre_token_plan.zh.md` 的 §0、§2–§4；
那份文档的读数（results 文件）全部保留、可引用。

## 审核表

| 节 | 内容 | 审核 |
|---|---|---|
| §1 | 意图 | 待过 |
| §2 | 大纲 | 待过 |
| §3 | 指标 | 待过 |
| §4 | 实验框架（阶段 A / B / C / D） | 待过 |
| §5 | 门 | 未写 |
| §6 | 决定项 | 未写 |
| §7 | 保留与搁置的代码 | 未写 |
| §8 | 队列与成本 | 未写 |

---

## §1 意图

v3 回答的问题只有一个：**在跑道已知的 TMA 里，一个可飞的 L1 执行器分段再问能把进近飞到什么程度；它的表现由哪些自身参数决定；
什么样的第二层信息能在此之上再加分。**

与 09-18 计划的差别：那份计划的前提是"L1 需要意图码才能短、单峰"。2026-09-18 的读数（results 文件 `p23_S60_*` 各节与"无 token 执行器的段长曲线"节）说：
短本身就够了——无 token 的分段执行器闭环建立 0.70–0.75，过了 v2 两层的进展线；真值码只换跟踪不换穿越；先验给的码一无所得；学习码本只装航向；
语义词表的码执行器不看；瓶颈是执行器在雷达引导段的穿越（0.3–0.4）。

所以 v3 把**执行器自身**当研究对象，先把它的基线定牢（段长 Δ、回看 L，以及雷达引导段为什么穿不过）；"第二层"不再预设为意图码，
而是定义为"任何在同一闭环协议下好过无 token 基线的东西"，并把它放到单机历史里真正没有的信息上：落地跑道、其他飞机。
意图码降为一个有界的候选，过相对门才继续。

---

## §2 大纲

0. 一页摘要：与 09-18 计划的差别，一张证据表。
1. 证据：今天的读数（无 token 闭环 0.70–0.75；码不加分；门 T 无区分力；瓶颈在雷达引导段穿越）。
2. 执行器契约：输入（回看 L、跑道系、机型）、输出（Δ 长的一段控制经动力学滚出）、执行协议（每 Δ 在飞出行上再问，协议 `none`），
   "建立 / 可飞 / ADE"的定义。
3. 指标（§3）。
4. 实验框架（§4）：阶段 A 执行器自身轴，阶段 B 第二层的相对门，阶段 C 缺失信息（跑道、多机），阶段 D 发布与跨机场。
5. 门：改写后的 S、X、E，新的相对门，R、G 保留。
6. 决定项：编号、理由、不解决什么；含继承的（120 s cohort 契约、锚点 59、pa 契约、两种子）。
7. 保留与搁置的代码：lockstep 四协议、门、readout 保留；分词器、先验搁置不删。
8. 队列与成本。

---

## §3 指标

全部在协议 `none`（或第二层的对应协议）下读，KRDU val 1392 架，固定锚点 59，两种子；开环的段内 ADE 只作训练诊断，不作门。

| 指标 | 定义 | 用途 |
|---|---|---|
| 建立份额 | 在预算内于最后进近航道上穿过入口的份额，分池化 / 直线 / 雷达引导 | 主指标 |
| 完全可飞 | 整条飞出轨迹通过可飞性检查的份额 | 硬约束，≥ 0.95 |
| 雷达 ADE 均值、池化 ADE p50 | 整条飞出段对真值 | 几何 |
| FDE p50 | 终点误差 | 到达精度 |
| 雷达 120 / 180 s 位移 p50 | 锚点后固定时刻的误差 | 与 B61、v2 可比 |
| 逐轮 e_track | 每一问末端对真值 | 误差怎么叠加 |
| 结束方式 | 穿越 / 预算 各多少 | 失败在哪 |
| 到达时刻误差 | 飞出时长对真值 | 时间维 |
| 种子线 | 建立 3 点、ADE 125 m | 任何差小于它不算差别 |

预算规则沿用：T₀ + max(30 s, 0.1·T₀)，T₀ = 真值在首问时的剩余时长。

---

## §4 实验框架

### 阶段 A：执行器自身的轴（无 token）

- **A1 网格**：Δ ∈ {30, 60, 90, 120} × L ∈ {30, 60, 90, 120}（L ≤ 120 s，锚点 59 处都可行），两种子。已有 L = 60 的 30 / 60 / 90 三格，
  新增 13 格 × 2 = 26 臂，约 6 h；每臂协议 `none` 各 2 min。用门 S 选 (L, Δ)。
- **A2 诊断**：在最好的格子上逐架看雷达引导段为什么穿不过（停在哪、是转晚还是转不到位、与规则制导的 0.88 差在哪）。
  这是后续改执行器的依据，不预设修法。
- **A3**（若 A2 指向具体缺陷）：对应的执行器改动，一个臂族，同网格读。

### 阶段 B：第二层的相对门

- 任何第二层（码、上下文、别的条件）只有在同一 (L, Δ) 下，闭环建立与雷达 ADE 两种子都不差于 `none`、且至少一项好过种子线，才算加分。
- 意图码作为一个有界候选：目标里加垂直 / 速度分量，一个 K、一个段长、两种子；过相对门才有后文。

### 阶段 C：单机历史里没有的信息

- **C1 跑道意图**：按 `2026-09-13_runway_intent_plan.zh.md` 的分类器，输出决定执行器坐标系；读数是门 R 加"预测跑道对已知跑道"的闭环差。
- **C2 多机**：先 oracle（前机真值作上下文）量上限，够大再学；读后机的雷达位移与建立、间隔违反率对观测数据。
  条件进执行器用连续向量，不经过码。

### 阶段 D：发布与跨机场

- 发布到 picker（每个读数臂）；跨机场（KSJC / KSTL / KSMF / KMSY）验证选出的 (L, Δ)。

---

## §5 门（未写）

## §6 决定项（未写）

## §7 保留与搁置的代码（未写）

## §8 队列与成本（未写）
