# 两层计划 v3：分段执行器与第二层（草案，逐项审核中）

**状态**：草案，2026-09-18 下午起与用户逐项过；每一节的"审核"栏由用户定。在本计划签字前不启动任何实验
（用户 2026-09-18："先停下所有实验。我们需要认真敲定计划"）。取代 `2026-09-18_manoeuvre_token_plan.zh.md` 的 §0、§2–§4；
那份文档的读数（results 文件）全部保留、可引用。

**引用规则**：本文引用任何早先的实验结果时，只作证据或候选，必须写明它是在什么配置上测的（执行器、lookback、segment、cohort、锚点、protocol）
以及能不能迁移到 v3 的对象；本文自己的结论只来自本文的臂。不写"X 够了 / X 有效"这类句子。

**术语**：没有现成中文译名的概念直接用英文，不硬翻——lookback（回看窗口 L）、segment（段长 Δ）、established、fully flyable、ADE / FDE、oracle、gate、protocol、runway configuration、cohort、seed。

## 审核表

| 节 | 内容 | 审核 |
|---|---|---|
| §1 | 意图 | 待过 |
| §2 | 大纲 | 待过 |
| §3 | 指标 | 待过 |
| §4 | 实验框架（阶段 A / B / C / D） | 待过 |
| §5 | 实验阶段与门（阶段 A 已写；B / C / D 待 A 结果） | 待过 |
| §6 | 决定项（阶段 A 的 D1–D11） | 待过 |
| §7 | 里程碑与交付（阶段 A） | 待过 |
| §8 | 队列与成本（阶段 A） | 待过 |
| §9 | 风险与后备 | 未写 |

---

## §1 意图

v3 回答的问题只有一个：**在跑道已知的 TMA 里，一个可飞的 L1 执行器一段一段地预测能把进近飞到什么程度；它的表现由哪些自身参数决定；
什么样的第二层信息能在此之上再加分。**

09-18 计划的前提是"L1 需要意图码才能短、单峰"。这个前提没有被验证也没有被推翻：那份 campaign 的执行器基线自身没有定——回看被固定在 60 s，
段长只在 60 s 回看下读了 30 / 60 / 90 s 三点，而 90 s 在一条 300–400 s 的进近里不算短。今天的读数只能当作**一个配置下的迹象**：
在 (L = 60 s, Δ = 60 s) 上，无 token 的分段执行器闭环建立约 0.70，真值码换来跟踪不换来穿越，先验给的码没有加分，学习码本只装了航向，
语义词表的码执行器不看，雷达引导段的穿越是 0.3–0.4。这些迹象决定了 v3 的顺序，不决定 v3 的结论。

所以 v3 先把**执行器自身**的轴做成真正的消融（段长 Δ 与回看 L 都作轴，Δ 要包含真正短的段），定出基线，再弄清雷达引导段为什么穿不过；
"第二层"不预设为意图码，定义为"任何在同一闭环协议下好过无 token 基线的东西"，候选放在单机历史里真正没有的信息上（落地跑道、其他飞机）；
意图码是其中一个有界候选，在定牢的基线上重新测一次，过相对门才继续。

---

## §2 大纲（开发计划的结构；后面各节按此编号展开）

1. **目标与范围**：要交付什么（一个可飞的分段执行器 + 定量的第二层结论）；不做什么（风、ATC 文本、天气不作输入；机场当时的 runway configuration——哪几条跑道在用于进场——也不作输入，模型只知道本机的落地跑道，configuration 的信息只能经它间接带入）。
2. **对象与契约**：执行器的输入（lookback L、跑道系、机型）、输出（Δ 段控制经动力学滚出）、执行协议（每 Δ 秒在飞出的轨迹上再预测一次）；哪些是固定的
   （cohort、锚点、动力学契约、种子），哪些是轴。
3. **问题与假设**：Q1 执行器的 (L, Δ) 怎么选；Q2 雷达引导段为什么穿不过；Q3 什么样的第二层信息在基线上加分；Q4 跑道未知时执行器损失多少、
   什么形式的跑道信息补得回来；Q5 有前机时执行器缺什么、什么形式的多机信息补得回来。每个问题写清"用什么读数回答、答案长什么样"。
   **第二层（L2）在 v3 里的地位**：它的形式是待回答的问题，不是继承的设计。它的任务由新执行器的失败方式（A2）和"信息缺失时损失多少"的测量来定义；
   09-13 计划里做过的跑道头 `r11_lift` 与 R3 的多机联合分配是在旧的整段执行器上测的，在 v3 里是**候选与证据**（左右跑道可预测、交互对落地时刻无增益），
   不是 v3 的第二层。
   **Q6 多假设的第二层**：飞机在一段里可能有几个方向可飞（继续下风、现在转 base、延长再转），临近时有平行跑道可选。
   第二层能否每段输出 K 个意图节点（segment frame 里的段末目标状态或连续向量，各带概率），L1 对每个节点飞一段，训练时只让最接近真值的那条
   拿到梯度（winner-takes-all / min-over-K），推理时按概率或按对 K 条 rollout 的评分选；段与段串成一棵意图树，跑道是树顶的分支。
   文献（`docs/literature/multimodal_intent/README.md`，13 篇已核实）：MultiPath / TNT / DenseTNT / MTR 是一次性输出 K 条的做法；plain WTA 会塌缩且有测量
   （MultiPath 的 min-of-K 基线只剩 1–2 个活模式），要从 evolving / annealed WTA 起步；DESIRE 不是 WTA，它靠评分器选；航空里显式 K 假设只有 ASCENT
   （ICRA 2026，一次性 120 s），**没有任何航空论文把多步预测做成意图分支的树**。用什么读数回答：它的闭环（top-1 或评分选出的那条）对基线协议的相对 gate（阶段 B），
   加多假设自己的读数（K 条候选里最好的一条对真值的 ADE、模式概率的校准、跑道模式命中率）。答案长什么样：过相对 gate 且多假设读数好于单假设，
   才说明多模态被显式化了。
4. **指标与读数协议**：§3 的表；两种子、种子线、预算规则、固定锚点。
5. **实验阶段与门**：阶段 A（执行器轴）、B（第二层相对门）、C（跑道、多机）、D（发布、跨机场）；每阶段的臂、门、go / no-go，
   以及"不过门时做什么"。
6. **每阶段需要的开发**（每项：现在有什么、缺什么、为什么要做）：
   - 阶段 A：现在的 campaign 臂文件只把 segment（`control_horizon_s`）当轴，lookback（`seq_len`）是写死的；要让臂文件能声明 (L, Δ) 网格，
     现有的训练 runner 就能跑。A2 需要一个逐架诊断脚本：读 lockstep 的每架记录，对没穿越的雷达引导航班说清它停在哪
     （final approach course 的正横、之前、之后、另一侧）、转弯比真值早还是晚——先把失败方式叫出名字，再谈改法。
   - 阶段 B：现在的 gate 都是和固定数字比；要一个"相对 gate"的 runner：读同一 (L, Δ) 下两份 lockstep 产物（第二层的 protocol 和 `none`），
     判"established 与雷达 ADE 两种子都不差、至少一项好过 seed 线"。
   - 阶段 C：先做测量，后做开发。C0 量新执行器在跑道未知（错跑道的坐标系）和有前机时损失多少；开发内容由 C0 与 A2 的结果决定，
     现在只能列可能项：让坐标系由预测的跑道来建（若跑道是主要损失）；给执行器加前机 context 输入，先真值后模型（若前机是主要损失）。
     具体形式在 C0 之后再定，写进决定项。
   - 阶段 D：发布用现有的 publisher 加 `--category-variant`，只需登记 intents，没有新开发。
7. **决定项**：计划里每一个被定死的参数或规则都在这一节有一个编号条目，写明是什么、为什么这样定、它没有解决什么。
   今天的教训是 lookback 被写在设计文字里而没有条目，于是没人审它。从 09-18 计划继承、需要在这里重新列出的有四条：
   - 用哪些航班（cohort）：KRDU 的进场，要求锚点之后至少还剩 120 s，这样五个 segment 长度比的是同一批航班；train 6798 架、val 1392 架。
   - 开始预测的时刻（锚点 59）：进入 25 km 切片后第 59 行，即 118 s；与 v2 相同，数才能对比。
   - 执行器的控制律（pa 契约）：specific-force + path-angle，带一阶滞后；v2 三种契约里唯一关掉平滑项后仍可飞的。
   - 两个 seed：每臂用 1337、2024 各训一次；差别小于 seed 线的不算差别。
8. **里程碑与交付**：每阶段完成的标志（表、发布的类目、写进哪份文档）。
9. **队列与成本**：GPU 小时、磁盘、顺序、通知。
10. **风险与后备**：每条风险的信号与后备动作。

---

## §3 指标与读数协议

### 3.1 训练怎么取样、读数怎么做

**数据**：每架航班的记录从它进入机场周围 25 km 圆圈那一刻开始，每 2 s 一行，直到落地。KRDU 的航班按航班随机分成训练集和验证集，
所有读数都在验证集上做，训练时没见过。（集合的大小随 §6 的 cohort 决定项定，每个 (L, Δ) 报 n。）

**训练（锚点是随机的）**：一个训练样本 = 记录里的某一行往前 L 秒的历史 → 这一行之后 Δ 秒的真值。这一行按剩余路程均匀随机抽
（`random_train_anchor_sampling = remaining-path-uniform`），要求它之后至少还剩 Δ 秒。所以执行器学的是整条进近的每一个阶段，
从下风边到 final；L 只是窗口长度，Δ 只是目标长度，训练里没有"固定锚点"。

**评估（闭环，基线协议）**：
- **开始预测的时刻**：进入圆圈后 L 秒——有了 L 秒历史就让模型开始预测，不再等到 118 s（09-18 的读数固定在第 59 行，60 % 的验证航班那时已在 final 上，
  短航班被整个排除；这是从 v2 继承的可比性约定，v3 不再用）。不同 L 的臂开始预测的时刻不同，但每个臂都是"有 L 秒历史就开始"，与部署一致。
- **验证集的条件**：开始预测时剩余时长 ≥ Δ（能飞满一段）。短航班回到集合里；每个 (L, Δ) 的集合略不同，报 n。
- **闭环**：从开始预测起，执行器看最近 L 秒的飞出历史，预测并飞出 Δ 秒，接到历史后面，再看、再预测、再飞；直到轨迹在 final approach course 上穿过跑道入口
  （记 crossed），或用完预算（记 horizon）。预算 = 该航班真值剩余时长 T₀ + max(30 s, 0.1·T₀)，只是上限。基线协议（代码里叫 protocol `none`）
  不给执行器任何第二层信息；第二层的读数用同样的轮次与预算，只多它提供的信息，两者可直接比。
- **开环读数**（从真实历史预测一次、算段内误差）只在训练时作诊断，不进任何判定：历史已决定段的大部分，它看不到闭环里的差别
  （09-18 的 gate T 是开环的，三个段长六个词表全分不出差别）。
- **两个 seed**：seed 指训练的随机数种子（权重初始化、数据顺序），数据和划分完全相同；每个臂训两次（1337、2024），读数成对出现。
  **seed 线**：两个 seed 之间 established 通常差 3 个百分点、ADE 差 125 m 以内，差在这以内的算同一水平（控制路径上的历史测量，
  `ts_transformer/CLAUDE.md`"How to read results"）。只读大小，不读 p 值。
- **三个分组**：每个数报三次——全部；直线进近（straight-in，开始预测时航线曲折度 < 1.05、基本已对准跑道）；雷达引导（vectored，还在被引导、要转弯）。
  雷达引导组是难点所在，也是 v3 主要看的一组。

**评估的第二种读法：从离入口固定距离处开始预测（已采用；与训练无关）**：按上面的规则，各航班开始预测时离落地远近不同，"从进圈后 L 秒起的 established"会被航班进圈时的
阶段左右。所以每个臂再读一组：每架航班在离入口固定距离处（剩 12 / 8 / 6 km，距离点在 §6 定）各开始一次闭环预测、从那一刻飞到底，每个距离点报一套 §3.2 的指标。
这样"从远处起飞"和"从近处起飞"分开看，长短航班在同一尺度上比（包里已有 anchor grid / anytime curve 的机制）。它只是评估的第二种读法，不改训练、不改验证集。

**评估的第三种读法：所有格从同一行开始预测（2026-09-19 M-A1 之后补，D28）**：读法 (a) 把"历史长度"和"起点"混在一起——L = 120 的执行器进圈后 120 s 才接手，比 L = 60 少飞 60 s、起点更靠近五边，
赢的是少飞一段而不是多看一段（M-A1 的证据：每轮误差只差约 10%，同一起点的 12 km 读数上 L60_D20 反而更好）。所以每个臂再读一组：每架航班都从第 59 行（进圈后 120 s，L = 120 自己的起点）开始预测、飞到底，
20 格飞的是同一段进近、同一批真值，只有历史窗长短不同，这才是纯粹的信息优势对比（`manoeuvre_lockstep --first-prediction-row 59`，`lockstep.from_row`，与 (b) 同一种切法；那一行后面要有 Δ 秒真值，没有的航班计数不飞）。
论文里"历史多长够用"用这一读法答。

### 3.2 指标定义

| 指标 | 一句话定义 | 单位 | 出处 |
|---|---|---|---|
| established | 在预算内于 final approach course 上穿过入口的航班份额 | 份额 | `manoeuvre_lockstep` 每架的 `reference.established` |
| fully flyable | 整条飞出轨迹每一点都在飞机的坡度 / 载荷 / 速度包线内的份额 | 份额 | `geometry.flyability` |
| ADE | 飞出轨迹对真值逐时刻位置误差的平均，一架一个数；报组内均值与 p50 | m | `observed_series_metrics` |
| FDE | 飞出轨迹终点对真值终点的误差；报 p50 | m | 同上 |
| 位移 @120 / 180 s | 锚点后 120 / 180 s 那一刻飞出位置对真值的误差 p50 | m | 每架 `at` |
| 逐段预测的误差（e_track） | 闭环里执行器预测了好几次（第 0、1、2…次）。每预测一次飞一段，段飞完的那一刻，量它所在位置与真实飞机同一时刻所在位置的距离；把所有航班第 r 次的这个距离取中位数，就得到第 r 次的一个数。按 r 排成一列，看误差随着一次次预测怎么长（09-18 的例子：175 → 434 → 812 → 1497 m，每预测一次翻一倍） | m | 每架 `asks_e` |
| 结束方式 | crossed / horizon 各多少架 | 架 | 每架 `ended` |
| 到达时刻误差 | 飞出时长减真值时长 | s | `final_time_error_s` |
| 失败方式（A2 用） | 没穿越的航班停在 final 的哪一侧、离入口多远、转弯比真值早或晚多少 | 类别 + m + s | A2 的诊断脚本（待写） |

### 3.3 每个阶段用哪些指标、怎么判

| 阶段 | 用哪些指标 | 判定规则 | 不过时做什么 |
|---|---|---|---|
| A1 选 (L, Δ) | 主：全部航班的 established 与雷达引导组的 established；约束：fully flyable ≥ 0.95；辅：雷达 ADE、FDE p50 | 两 seed 都最好的 (L, Δ) 胜出；主指标与辅指标不一致时以 established 为准；差在 seed 线内算平手，平手取 Δ 短、L 短的（信息少、承诺短的更保守） | 若整张网格 established 都在 seed 线内，段长与 lookback 不是关键变量，记为结论，A2 决定下一步 |
| A2 诊断 | 失败方式的分布（雷达引导层没穿越的那 ~300 架）；e_track 逐轮 | 不是 gate，是分类：哪种失败占多数，写成 A3 的假设 | — |
| A3 改执行器 | 与 A1 相同 | 在同一 (L, Δ) 上，两 seed 的 established 都好过 A1 胜者且至少一个超过 seed 线，fully flyable 不掉 | 假设不成立，回 A2 换下一种失败方式 |
| B 第二层相对 gate | 第二层的闭环对基线协议，同一 (L, Δ)：established（全部与雷达引导）、雷达引导 ADE | 两 seed 都不差（seed 线内），且至少一项好过 seed 线 | 该第二层不加分，不进 C / D |
| C0 量损失 | 跑道错建坐标系时、有前机时的 established / ADE 相对已知跑道、无前机的差 | 不是 gate，是测量；差大于 seed 线才值得建第二层去补 | 差在 seed 线内 → 该信息不值得建层，记为结论 |
| D 跨机场 | 选出的 (L, Δ) 在 KSJC / KSTL / KSMF / KMSY 上的同一套指标 | 每个机场按三个分组报，机场之间不比"全部"这一组的数（各机场直线 / 雷达引导的比例不同，`data/approach_difficulty.py`） | — |

第二层若是多假设形式（Q6），除相对 gate 外再报它自己的读数：K 条候选里最好的一条对真值的 ADE（minADE_K）与未命中率、模式概率的校准（预测概率对实际被选中的频率）、
跑道模式命中率（树顶分支选对跑道的份额）。这些是多模态被显式化的证据，不替代相对 gate。

## §4 实验框架

### 阶段 A：执行器自身的轴（无 token）

- **A1 网格**：Δ ∈ {30, 60, 90, 120} × L ∈ {30, 60, 90, 120}（L ≤ 120 s，锚点 59 处都可行），两种子。已有 L = 60 的 30 / 60 / 90 三格，
  新增 13 格 × 2 = 26 臂，约 6 h；每臂协议 `none` 各 2 min。用门 S 选 (L, Δ)。
- **A2 诊断**：在最好的格子上逐架看雷达引导段为什么穿不过（停在哪、是转晚还是转不到位、与规则制导的 0.88 差在哪）。
  这是后续改执行器的依据，不预设修法。
- **A3**（若 A2 指向具体缺陷）：对应的执行器改动，一个臂族，同网格读。

### 阶段 B：第二层的相对门

- 任何第二层（码、上下文、别的条件）只有在同一 (L, Δ) 下，闭环建立与雷达 ADE 两种子都不差于 `none`、且至少一项好过种子线，才算加分。
- 意图码作为一个有界候选：目标里加垂直 / 速度分量，一个 K、一个段长、两种子；过相对门才有后文。

### 阶段 C：单机历史里没有的信息（形式待定，先测后建）

- **C0 测损失**：在阶段 A 选出的执行器上，(i) 跑道未知：用错的平行跑道建坐标系时 established / ADE 掉多少，与 09-13 R2a 在旧执行器上的 +55 m 对照；
  (ii) 有前机：把验证集按"前方有没有飞机、间隔多大"分组，读基线协议下每组的失败率，看多机是不是失败的来源之一。
- **C1 / C2 的设计**：由 C0 与 A2 决定，之后再写臂与 gate。候选：跑道——09-13 的跑道头（`r11_lift`，左右层可预测）或别的形式；多机——前机 context 进执行器，
  先 oracle 量上限（gate G 的第一步），R3 / R3.1 的"交互对落地时刻无增益"作为已知证据。第二层若走离散码，必须过阶段 B 的相对 gate；连续向量是默认。

### 阶段 D：发布与跨机场

- 发布到 picker（每个读数臂）；跨机场（KSJC / KSTL / KSMF / KMSY）验证选出的 (L, Δ)。

---

## §5 实验阶段与门 —— 阶段 A（执行器自身的轴，无第二层）

只写阶段 A；B / C / D 等 A 出结果后再写。

### 5.1 A1：(L, Δ) 网格

**臂**：无第二层信息的执行器（`plan_conditioning = off`），lookback L × segment Δ 的全网格，每格两个 seed。

| | Δ = 20 s | 30 s | 60 s | 90 s | 120 s |
|---|---|---|---|---|---|
| L = 30 s | ● | ● | ● | ● | ● |
| L = 60 s | ● | ● | ● | ● | ● |
| L = 90 s | ● | ● | ● | ● | ● |
| L = 120 s | ● | ● | ● | ● | ● |

20 格 × 2 seed = 40 臂。Δ 含 20 s 是为了有真正短的段；L 上限 120 s 是数据里开始预测前最多能有的历史（更长要更早的数据，不在范围内）。
其余训练设置全部相同（§6 D5–D8）。09-18 campaign 的三格（L = 60，Δ = 30 / 60 / 90）不复用：它们的训练集排除了短航班、评估从第 59 行起，与 v3 的读法不同。

**每臂的读数**（§3.1 的三种）：(a) 从进圈后 L 秒开始预测的闭环；(b) 从离入口 12 / 8 / 6 km 处开始预测的闭环，各一套；(c) 从第 59 行开始预测的闭环（2026-09-19 补，D28）。都在该格的验证集上（记录 ≥ L + Δ 的航班，报 n）。

**判定（选 (L, Δ)）**：按 §3.3 A1 行——两 seed 都最好的格胜出；主指标 established（全部与雷达引导组），约束 fully flyable ≥ 0.95，辅指标雷达引导 ADE、FDE p50；
差在 seed 线内算平手，平手取 Δ 短、L 短的。seed 线不再沿用旧数，从这 20 对 seed 自己的差里读（§6 D9）。
**不过时**：若整张网格的 established 都在 seed 线内，结论是"在这个范围内 L、Δ 不是关键变量"，记入结果文件，A2 在最短的格上做。

### 5.2 A2：雷达引导段为什么穿不过

**对象**：A1 胜出格的两个 seed，读数 (a) 里没穿越的雷达引导航班（09-18 的配置下约 300 架）。

**读什么**：每架一行——预算用完时它在 final approach course 的哪一侧、离入口多远（沿航道 / 横向）、有没有对准过（航向与 final 方向差 ≤ 30° 的第一时刻，
执行器与真值各一个）、转弯比真值早还是晚多少秒、到预算末尾时速度与高度对真值的差。按这些归成几类（例如：对准了但没飞到；切过了延长线没对准；一直没转；
转到另一侧），报每类的架数与份额，并把每类几架发布到 picker 供人看。

**产出**：失败方式表 + 至多三条假设，每条写明"若成立，A3 的哪一种改动会让哪一类失败减少、其他读数不变"。**不是 gate。**

### 5.3 A3：按假设改执行器

A2 之后再写。形式固定：每条假设一个臂族，在 A1 胜出格上两 seed；判定按 §3.3 A3 行（established 两 seed 都好过 A1 胜者、至少一个超过 seed 线、fully flyable 不掉）。

### 5.4 阶段 A 需要的开发（launch 前全部完成、测试通过、opus review）

| # | 开发 | 现状 | 要做的 |
|---|---|---|---|
| A-dev1 | 网格臂文件 | 臂文件只把 Δ 当轴，L 与开始预测的 floor 写死 | 臂文件声明 (L, Δ) 网格；每臂 `seq_len = L / 2` 行、`control_horizon_s = Δ`、`n_segments = Δ / 10`；不设 `anchor_floor_index` |
| A-dev2 | cohort | 按"第 59 行后剩 ≥ 120 s"选，短航班被排除 | 每格一份 cohort（`plan_cohort --arms 臂文件` 一次加载写出 20 份）：KRDU 全部进场按航班一次划分（split seed 1337 钉在臂文件里），每格可用集 = 前有 L 秒历史、后有 Δ 秒真值的航班（train / val 同一条件），再去掉随机锚点覆盖不到的 train 航班（0–3 架）；`rebuild_cohort` 读 checkpoint 自带的 split，不用改；旧 cohort 文件保留 |
| A-dev3 | 开始预测的时刻 | `default_anchor` = L−1 行，但 campaign 用 floor 把它推到 59 | 不设 floor 即为"进圈后 L 秒"；加一条测试钉住"无 floor 时第一次预测在第 L−1 行" |
| A-dev4 | 从固定距离处开始预测 | lockstep 只从固定的那一行开始预测 | `manoeuvre_lockstep --anchor-remaining-km X`（12 / 8 / 6，取 `data/anchor_strata.DEFAULT_ANCHOR_GRID_KM` 里的值）：每架从剩余路程最接近 X km 的那一行开始预测（包里 anchor grid 的同一定义 `anchor_grid.bin_anchor`；该行前要有 L 秒历史、后要有 Δ 秒真值，否则该航班计数不飞）；实现是把航班从该行往前 L 秒切起再走同一套闭环。同时协议 `none` 不再要 codebook |
| A-dev5 | 训练时的固定锚点份额 | `random_train_anchor_l1_share = 0.5`：一半训练样本钉在第 59 行 | 改为 0（训练全随机），或 L−1 行占小份额；§6 D7 定 |
| A-dev6 | A2 失败方式脚本 | 无 | 新 runner `executor_failure_modes`：读带记录的 lockstep 产物，对某组（默认雷达引导）里没穿越的每架，在跑道 course 坐标系里算：预算末尾沿航道离入口多远、横向偏多少、在哪一侧；有没有"对准过"（用包里 established 的同一定义：横偏 < 500 m、航向差 ≤ 30°、在入口前）；最后一次转上航向的时刻与真值之差（转弯早晚）；末尾速度、高度对真值之差。六类按序归类：对准了没飞到 / 切过延长线没对准 / 一直没转 / 飞过入口侧方 / 平行偏置 / 其他。每类前几架复制成可发布的记录目录 |
| A-dev7 | 选格 runner | gate S 按旧规则 | 新 runner `executor_grid_gate`：读 40 份 lockstep 产物，按 5.1 / §3.3 规则输出整张表与胜出格；seed 线 = 本网格 20 格两 seed 之差绝对值的 p75（p50 一并记录；D9）；主指标两项（全部与雷达引导组的 established）各自"离每个 seed 的最好值在 seed 线内"的格为并列，两项都并列的格胜出，两项不一致时以全部航班的 established 为准，再取 Δ 短、L 短；所有可用格都并列时判"不决定性" |
| A-dev8 | 队列脚本 | campaign 队列按段长串行 | 新 runner `two_tier_grid_queue`：按格串行（Δ 升序、L 升序），每格两 seed 训完立即读两种读数（共 8 份，不写记录；胜出格事后带记录重飞）并跑一次选格表；每行带时间戳，"CELL … complete" 供监视；第一步失败即停（D11）；PID 文件；每格 ≥ 3 GB 空余。通知由监视者（Monitor + PushNotification）发 |

## §6 决定项 —— 阶段 A

每条：是什么 / 为什么 / 没解决什么。

- **D1 验证集与训练集**：KRDU 全部进场航班（openap 能提供机型参数的），按航班随机一次划分（split seed 1337，与 09-18 相同），所有格共用同一划分；
  每格只用记录 ≥ L + Δ 的航班（训练与验证同一条件），报 n。为什么：短航班回到集合，各格在"能开始预测且能飞满一段"的航班上比。没解决：各格 n 略不同；跨机场（阶段 D）。
- **D2 开始预测的时刻**：进圈后 L 秒（第 L/2 − 1 行），不设 floor。为什么：与部署一致，不同 L 各自有了历史就开始。没解决：各航班开始预测时所处的阶段不同——由 D3 补。
- **D3 从固定距离处开始预测的读数**：剩余 12 / 8 / 6 km 三点（`DEFAULT_ANCHOR_GRID_KM` 里已有的 bin；开始预测的行 = 剩余路程最接近 X km 的行，包里 anchor grid 的同一定义，与"首次 ≤ X km"最多差一行）。
  为什么：把"从远处起飞 / 从近处起飞"分开看，长短航班同尺度。没解决：> 12 km 的远端（很多航班进圈时已 < 20 km）；12 km 处按该行重分组时雷达引导组很薄（冒烟：24 架里 0 架），
  这一读法主要看"全部"与 straight-in，各组报 n。另一个没解决的点（review 2026-09-18）：开始预测的那一行后面要有 Δ 秒真值，所以同一距离点上各格的航班集随 Δ 变
  （6 km 处约剩 80 s：Δ = 20 s 几乎全覆盖，Δ = 120 s 大半为空）；跨格比 (b) 读数时要在各格共同覆盖的航班上比，或把每格的覆盖数写在数旁边。
  跑出来（2026-09-19）：6 km 处 Δ = 120 s 是全空（不是大半），Δ = 90 s 只剩约 360 架；8 km 处 Δ = 120 s 只剩 275 架；雷达引导组在 8 / 6 km 全空、12 km 只有 9 架。
- **D28 所有格从同一行开始预测的读数 (c)**（2026-09-19，M-A1 之后补）：第 59 行（进圈后 120 s，即 L = 120 自己的 L−1），只此一行；那一行后面要有 Δ 秒真值，没有的航班计数不飞；门表在这份读数上另跑一次（`executor_grid_gate --reading row59`）。
  为什么：读法 (a) 里 L 大的格少飞一段、起点更靠近五边，胜出可能是起点效应；同一行起飞后 20 格飞同一段、只差历史窗，才能回答"历史多长够用"。取第 59 行是因为它是所有格都能开始的最早一行。
  没解决：第 59 行时部分雷达引导航班已在下风/基边，更早的起点（如第 29 行）只有 L ≤ 60 的格能读；(c) 只是读数，不改判定规则（A1 的判定仍按 (a)），论文里的 L 结论按 (c) 写。
- **D4 网格范围**：Δ ∈ {20, 30, 60, 90, 120} s，L ∈ {30, 60, 90, 120} s。为什么：Δ 含真正短的段；L 到数据允许的上限。没解决：L > 120、Δ > 120，以及非均匀段。
- **D5 执行器控制律**（继承）：specific-force + path-angle，一阶滞后（pa 契约）。为什么：v2 三种契约里唯一关掉平滑项仍可飞的。没解决：其他控制律；
  它在雷达引导段的已知退化（D27）留给 A2 / A3。
- **D6 控制段数**：N₁ = Δ / 10 s（20 s 段 2 段，120 s 段 12 段），与 09-18 campaign 同一约定。为什么：每 10 s 一个控制决策，各 Δ 的控制分辨率相同。没解决：N₁ 本身的消融。
- **D7 训练锚点**：全随机、按剩余路程均匀（`remaining-path-uniform`），不再钉一半样本在固定锚点（09-18 是 0.5）。为什么：评估开始预测的时刻不再固定，钉着没有理由。
  没解决：抽样分布本身（等距 / 等时）的消融。
- **D8 训练预算**（继承）：180 epoch、不早停、lr 3e-5、batch 512、按固定锚点的验证 ADE 选 epoch（锚点 = L−1 行）。为什么：与 09-18 相同，每臂 12–15 min。
  没解决：更长训练是否有增益。
- **D9 两个 seed 与 seed 线**：seed 1337 / 2024；seed 线不用旧数（3 点 / 125 m 是别的配置上测的），从本网格 20 对 seed 的差里读：取各格两 seed 差绝对值的 p75 作线（p50 一并写进结果文件，
  `executor_grid_gate` 实现）。为什么：seed 线属于配置；取 p75 而不是中位数，是让"平手"判得保守一点。没解决：两 seed 只能估一个粗线。
- **D10 阶段 A 只做无第二层的执行器**（`plan_conditioning = off`）。为什么：先定基线。没解决：任何第二层。
- **D11 通知与停止**：每格读完发通知；任何一臂训练失败链停下并通知；不自动重跑。

## §7 里程碑与交付 —— 阶段 A

| 里程碑 | 完成的标志 | 交付 |
|---|---|---|
| M-A0 代码就绪 | A-dev1…8 写完，测试通过，opus review 无 BLOCKER，提交在主树；runs worktree 移到该提交；20 份 cohort 已写出（`two_tier_v3_grid_20260918/cohorts/`，train 6798–6857 / val 1392–1405） | 提交列表；测试数；本文 §5.4 每行标"完成" |
| M-A0′ 用户签字 | 用户看过 §5–§8 后说"跑" | — |
| M-A1 网格读完 | 40 臂训完，每臂两种读数各一份 | 结果文件一节：20 格 × 2 seed 的表（n、established 三组、fully flyable、雷达 ADE、FDE、逐次误差），两种读数各一张；seed 线；胜出格；胜出格两 seed 的记录发布到 picker |
| M-A2 失败方式 | 胜出格两 seed 的失败方式表写好 | 表 + 至多三条假设（各带预期特征）；每类几架发布到 picker；用户选 A3 跑哪条 |
| M-A3 | 每条假设一个臂族读完 | 与 A1 胜者的对比表；结论写进状态表 |

## §8 队列与成本 —— 阶段 A

- **GPU**：40 臂 × 12–15 min ≈ 8–10 h（RTX 4060，串行）；两种读数每臂各 1–2 min，随格插队；A2 脚本 CPU 几分钟。
- **磁盘**：每臂约 45 MB（09-18 实测），40 臂 ≈ 2 GB；胜出格发布记录约 40 MB / seed；现有 15 GB 空余够用。链启动前查空余 ≥ 3 GB。
- **顺序**：Δ 升序、L 升序（20/30 → 20/60 → … → 120/120），每格两 seed 连着训、训完立即读；这样短段的结果最先出，用户可早看。
- **运行方式**：从 detached 的 runs worktree（移到 M-A0 的提交）用 `nohup setsid` 跑队列，PID 文件；主树可继续改文档。Monitor 每 30 min 续；
  队列 agent 只做通知与读数汇报，不做决定；每格读完与链结束 PushNotification。
- **不做的事**：不跑任何带第二层的臂；不删除、不覆盖旧 campaign 的产物（`manoeuvre_tok_20260918/` 原样保留）。

## §9 风险与后备（未写）
