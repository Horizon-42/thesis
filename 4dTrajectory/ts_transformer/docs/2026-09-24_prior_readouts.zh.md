# 先验的读数

先验的设计在[先验设计](2026-09-24_prior_design.zh.md)；这里只放每一次训练的读数，以及跑之前写定的比较规则。每次训练一节，
写明它当时的配置（与现行设计不同的地方），数字只和同一节里、按同一口径算的基线比。

---

## 1 第一版（2026-09-24，`4dTrajectory/outputs/POOLED/prior/v1_20260924/`）

**配置**：句子产物 `instruction_language/v2_20260924`（规格 `103a6eae6b90`），按航班划分（`data.splits`）；每步输入是信号的状态
（含居中 15 s 拟合的航迹、地速、垂直速率——含之后 7.5 s 的信息）、相对生效跑道的量（第 0 步为 0 并给标志位）、到上一步为止
生效的词、机场与候选跑道几何；六个互相独立的头；d 192、4 层、6 头。代码 `11888357`（分支 `dev-executor`，干净的工作树）。
读数在 val 上。

230 万参数，28 个 epoch（第 25 个最好，每个约 58 s，共 27 分钟），train 970 万步、val 205 万步。

| 列 | 模型 | 重复上一条 | 只看上一个词 | 换词的步数 | 换词的步上 P(换) | 取值前 1 | 取值前 5 | 不换的步上误说要换 |
|---|---|---|---|---|---|---|---|---|
| 跑道指针 | 0.0023 | 0.0082 | 0.0082 | 10,540（都是第 0 步） | 1.000 | 0.808 | 1.000 | 0.0000 |
| 进近 | 0.0120 | 0.0222 | 0.0222 | 16,092 | 0.693 | 0.923 | 1.000 | 0.0001 |
| 航向 | 0.0460 | 0.0925 | 0.0832 | 25,827 | 0.507 | 0.557 | 0.843 | 0.0005 |
| 高度 | 0.0184 | 0.0433 | 0.0429 | 18,374 | 0.815 | 0.645 | 0.886 | 0.0007 |
| 下降角 | 0.0452 | 0.1003 | 0.0959 | 39,536 | 0.549 | 0.772 | 1.000 | 0.0020 |
| 速度 | 0.0539 | 0.0778 | 0.0736 | 26,752 | 0.444 | 0.483 | 0.830 | 0.0001 |
| **合计（每步）** | **0.1778** | 0.3444 | 0.3260 | | | | | |

读法：

- 每一列都比两个基线低，合计约低 45 %，每步困惑度 1.19。流程（产物 → 数据 → 训练 → 早停 → 读数）跑通了。
- "什么时候换词"比"换成什么"难：换词的步上模型给"换"的平均概率只有 0.44–0.82（速度、航向最低）；换成什么，前 5
  覆盖 83–100 %。换词的时刻本身有几秒的读法误差（执行器设计 §9 办法 B：高度、角度词读晚 4–6 s，速度词 8–9 s），
  模型学到的时刻也就带着这个误差。
- 跑道指针在第 0 步说，前 1 命中 80.8 %：只靠机场、候选跑道和第 0 步的状态，平行跑道之间还分不清。

---

## 2 第二版（2026-09-24，`4dTrajectory/outputs/POOLED/prior/v2_20260924/`，队列 19:41–22:24 UTC 跑完）

**配置**：句子产物 `instruction_language/v3_20260924`（规格 `0b4ea75be36d`），按航班划分；不用拟合的速度类输入（变体 0 除外）；
第 0 步的进近、航向、高度、下降角、速度五列是已知条件（交接时的词），跑道预测；对每条候选跑道算相对量，候选跑道 token
（共用的小网络，求和进每步输入）与跑道打分头；其余同第一版。代码 `e7f0cf66` + 审查修正 `8f5af0a3`（分支 `dev-prior-v2`）。
队列：`v2_20260924/queue.sh`（日志 `queue.log`）。

**比较的规则（2026-09-24，跑之前写定）**：

| 变体 | 状态里的速度 | 候选的方向项 | 比输入少的顺序（加了几个量） | 参加选择 |
|---|---|---|---|---|
| V0 | 拟合（航迹、地速、垂直速率） | 拟合航迹 − 跑道航向 | — | 否，只量未来信息 |
| V1 | 无 | 无 | 0 | 是 |
| V1d | 无 | 过去运动方向 − 跑道航向 | 1 | 是 |
| V2 | 过去的地速、方向、垂直速率 | 无 | 3 | 是 |
| V2d | 过去的地速、方向、垂直速率 | 过去运动方向 − 跑道航向 | 4 | 是 |

- **内部选择集**：train 50,223 架里每个机场抽 10 %（种子 1337，按航班），每个变体用同一份（sha 写进每次的 `config.json`）。
  其余约 45,200 架训练；val 只做早停（每个 epoch 的 val 负对数似然）和最后一次读数。
- **种子**：`--seed` 同时定初始权重、批次顺序和 dropout（审查后改：原来模型在设种子之前就建好了，初始权重每次随机）。GPU 上的
  运算本身仍不完全确定，同一种子重跑只能近似复现。
- **位置插值**（审查提出，未量）：信号每 2 s 一行是报告点之间的线性插值；报告中断时，中断里各行的位置在"到中断后第一个报告点"
  的直线上，"只看过去"的位移就指向了中断之后飞机的位置。有多少行落在超过 2 s、超过 7.5 s 的中断里还没量（需要读原始航迹）。
- **跑的顺序**（一个接一个，同一台显卡）：V1、V1d、V2、V2d（种子 1337）→ 领先者再用种子 2024 跑一次 → V0（种子 1337）→
  选择与 val 读数。
- **规则**：
  1. 领先者：V1、V1d、V2、V2d（种子 1337）里，内部选择集上每步负对数似然（第 0 步只算跑道）最低的；
  2. 种子线：领先者两个种子的这个数之差的绝对值；
  3. 选：与领先者相差不超过种子线的变体里，加的量最少的（上表的顺序）；一样少时取负对数似然低的。
- **读什么**（写进 `choice.json`）：每个变体在内部选择集上的每步负对数似然（总的；第 1 步起分列，按第 1 步起的步数平均）、换词的
  步上 P(换) 与取值前 1、不换的步上误说要换、第 0 步跑道前 1 / 前 2（另按交接时的进近词分开）；变体 0 减去每个参选变体的 P(换) 与前 1（分列），就是先验靠未来信息的程度。
- **val 读一次**：只读选中的那一次（它在 train 去掉选择集后训练的那个模型，不重训），与按同一口径、从它自己的训练航班数出的
  两个基线比。

**审查发现的一处（交用户定，后来由第三版设计改掉）**：第 0 步的"进近"条件里带着跑道的答案。标注器判"交接时已许可加入"，看的是
第 0 行是否已在**落地那条跑道**的走廊里（或正转上它）；"速度未指定"也从许可那一行起算。审查在 v3 val 上粗数：10,542 架里 2,793 架
交接时已许可、1,828 架速度未指定。所以第 0 步跑道的读数按交接时的进近词分开报（`by_handover_approach`）。

**报告间隔**（审查提出，第二版量）：信号每 2 s 一行是 ADS-B 报告点之间的线性插值，一行的位置会用到它之后最近的报告点。
用到 2 s 以后报告点的行占 0.86 %，7.5 s 以后的占 0.044 %。

**读数**：

- V1（种子 1337），内部选择集 5,022 架（train 里按机场抽的 10 %；val 只用于早停）：每步负对数似然 0.2752（第 1 步起 0.2744），
  两个基线（同口径：第 0 步只算跑道）"重复上一条" 0.8432、"只看上一个词" 0.5598。第 0 步跑道前 1 为 81.4 %、前 2 为 98.3 %；
  "每个机场按 train 里各跑道的落地频率猜"为 58.3 %。按交接时的进近词分开：

  | 交接时 | 架数 | 前 1（模型） | 前 1（机场最常用跑道） |
  |---|---|---|---|
  | 未许可 | 3,747 | 75.7 % | 50.5 % |
  | 已许可 | 1,275 | 98.0 % | 81.4 % |

- **全部变体**（内部选择集 5,022 架；每步负对数似然"总"含第 0 步的跑道，分列从第 1 步起按步平均；P(换) = 换词的步上模型给
  "换"的平均概率；前 1 = 换词的步上取值前 1；每个约 28 分钟，27 个 epoch 左右，245 万参数）：

  | 变体 | 总 | 进近 | 航向 | 高度 | 下降角 | 速度 | P(换) 高度 / 下降角 | 下降角前 1 | 第 0 步跑道前 1（未许可） |
  |---|---|---|---|---|---|---|---|---|---|
  | V1 | 0.2752 | 0.0124 | 0.1259 | 0.0166 | 0.0673 | 0.0522 | 0.18 / 0.11 | 0.690 | 81.4 %（75.7 %） |
  | V1d | 0.2723 | 0.0122 | 0.1228 | 0.0168 | 0.0677 | 0.0520 | 0.17 / 0.10 | 0.691 | 80.9 %（75.0 %） |
  | V2 | 0.2607 | 0.0124 | 0.1290 | 0.0126 | 0.0543 | 0.0516 | 0.41 / 0.25 | 0.727 | 83.7 %（78.7 %） |
  | **V2d** | **0.2521** | 0.0120 | 0.1234 | 0.0121 | 0.0523 | 0.0513 | 0.46 / 0.27 | 0.732 | 81.1 %（75.4 %） |
  | V2d 种子 2024 | 0.2524 | 0.0121 | 0.1234 | 0.0122 | 0.0527 | 0.0512 | 0.49 / 0.29 | 0.734 | 83.9 %（78.9 %） |
  | V0（拟合，只量未来） | 0.2395 | 0.0121 | 0.1206 | 0.0106 | 0.0441 | 0.0512 | 0.60 / 0.39 | 0.778 | 82.1 %（76.5 %） |

- **选择**（按上面写定的规则，`choice.json`）：领先者 V2d（0.2521）；种子线 = |0.2521 − 0.2524| = 0.0003；种子线内只有 V2d（V2 差
  0.0086，是种子线的 25 倍）→ **选 V2d：过去的运动和方向差两块都给**。
- **读法**：
  - 过去的运动（V1 → V2）降 0.0145，几乎都在高度、下降角两列：P(换) 从 0.18 / 0.11 升到 0.41 / 0.25。只看位置时，模型不知道飞机
    是不是已经开始下降，就说不准高度、下降角词什么时候换。方向差单独加（V1 → V1d）只降 0.0029，在有运动时（V2 → V2d）降 0.0086，
    主要在航向列（0.1290 → 0.1234）。
  - 未来信息的价值（V0 − V2d）：总共 0.0126；P(换) 高度 +0.14、下降角 +0.11，下降角前 1 +4.6 个点，其余各列在 ±0.01 以内。标注器
    读高度、下降角词用的就是这种居中平滑的速度，拟合窗口里后 7.5 s 的信息正好告诉模型"飞机马上要开始下降了"——这部分是用
    未来换来的，不能要。
  - 第 0 步跑道前 1 在同一变体的两个种子之间差 2.8 个点（V2d 81.1 % 对 83.9 %），比变体之间的差还大：跑道读数上各变体分不出高下。
  - 速度词最难：换词的步上 P(换) 只有 0.10–0.11，前 1 约 0.54，四个变体都一样——只靠位置和过去的运动，预测不了什么时候减速。
- **val 读一次**（V2d 种子 1337，10,542 架、205 万步，第 24 个 epoch）：每步负对数似然 **0.2542**（第 1 步起 0.2532，困惑度 1.29），
  同口径基线"重复上一条" 0.8403、"只看上一个词" 0.5593。分列（第 1 步起）：进近 0.0120、航向 0.1237、高度 0.0129、下降角 0.0526、
  速度 0.0521；换词的步上 P(换)：进近 0.25、航向 0.64、高度 0.45、下降角 0.27、速度 0.11；取值前 1：航向 94.2 %、高度 74.8 %、下降角
  72.7 %、速度 53.7 %；不换的步上误说要换：航向 1.7 %，其余 ≤ 0.13 %。第 0 步跑道前 1 **81.2 %**（前 2 98.1 %），"按机场各跑道落地频率
  猜" 57.4 %；交接时未许可 7,749 架 75.0 %（频率 48.8 %），已许可 2,793 架 98.2 %（频率 81.4 %）；分机场：KMSY 97.0 %、KRDU 67.9 %、
  KSJC 91.6 %、KSMF 75.3 %、KSTL 86.4 %。
- **对第三版的意义**：设计 §10"运动、方向差这两块输入"定为 V2d 的做法（都给，只用过去）。第二版的数不当第三版的基线：划分（按运行日）、
  第一个预测步的条件（第三版第一个预测步六列都要说，前 `N_look` 行只观察）、结构都变了。

---

## 3 第三版 第 0 步：划分、重建句子产物、数据普查（2026-09-24）

代码 `dev-prior-v3` `67e0e5c9`（干净的工作树），链 22:22–22:33 UTC。

- **划分**（提交在 `data/day_split_20260924.json`）：90 个运行日分为测试 14 / 验证 14 / 内部选择 9 / 训练 53 天。合格到达航班：测试日
  9,838 架（只从清单里数，不打开；其中 1,458 架原来按航班也在测试集里，是跨模型比较能用的样本）、验证 10,757、内部选择 6,918、
  训练 44,734。数据平面能用的（去掉 120 s 以下的短航迹 47 架）：训练 44,703、内部选择 6,913、验证 10,746。
- **句子产物** `outputs/POOLED/instruction_language/v4_20260924/`：词表规格在新训练集上重新测量，得 `145d6911e75b`（原 `0b4ea75be36d`）；
  变的只有下降角档的中心与边界（各差 ≤ 0.02°，最陡一档下沿 3.73° → 3.74°）、爬升角中心 1.33° → 1.32°、最大加速度 1.7 → 1.4 m/s²。
  标注：训练 44,362 架（拒绝 341）、内部选择 6,842（71）、验证 10,633（113）；拒绝原因最多的是地速不可能（209）、高度目标在错的一侧（109）、
  最后不在五边上（80）。
- **普查**（只读训练集运行日，`outputs/POOLED/prior/v3_census_20260924/census.json`）：
  - 句子长度中位数 154 行（约 5 分钟），p90 305 行，最长 1,148 行；没有一架短于 `N_look` + 1 行。
  - 到第 `N_look` = 8 行时已经在五边走廊里（截获完成）的占 25.6 %，许可已在第一个预测步之前说出的占 28.9 %。机场之间差很多：
    KSJC 60.0 %，KMSY 26.5 %，KRDU 19.5 %，KSMF 9.8 %，KSTL 7.5 %（截获完成）。
  - 此前 30 分钟有落地记录的预测步占 97.6 %（KMSY 94.9 % 到 KRDU 98.5 %）；30 分钟窗口伸进测试日的只占 0.03 %。落地统计里去掉的测试日
    落地：KMSY 965、KRDU 3,368、KSJC 2,588、KSMF 1,266、KSTL 1,726 架。
  - 每个预测步场上的其他飞机（有句子的 + 背景）：0 架 30.0 %、1 架 34.4 %、2 架 21.5 %、≥ 3 架 14.0 %；最忙的 KRDU ≥ 3 架占 24.9 %、
    最多 8 架，最闲的 KMSY 0 架占 48.8 %。背景飞机（被标注器拒绝的到达）几乎没有：每步平均 ≤ 0.04 架。
  - 有前机（同一条跑道、落地更早、离入口近不超过 10 km）的步占 16.5 %：KMSY 13.7 %、KRDU 14.0 %、KSJC 16.5 %、KSMF 13.7 %、KSTL 23.5 %；
    与最近前机的距离差中位数 6.6–7.7 km。
  - 按时间重叠串成的段：每段航班数中位数 1–3（KRDU 3），段长中位数 8–12 分钟，p90 18–47 分钟，最长 67–209 分钟（KSJC 209、KRDU 200）；
    只有一架航班的段占 34–52 %。
- **读法**：
  - 第一个预测步时，四分之三的航班还没截获五边——跑道和截获这件事大多要由先验来说，第一步单机模型的"跑道赢 B1、B3"要在这部分航班上读。
  - 落地统计几乎每一步都有（97.6 %），30 分钟窗口够用；伸进测试日的步少到可以忽略，不需要另作处理。
  - 在 25 km 范围里，有前机的步只占六分之一，≥ 3 架的步占 14 %：交互能看到的样本不多，但每个机场都有几十万步；§9 第 2 步（M1）按"有无
    前机"分开读是可行的。
  - 段长的长尾（KRDU、KSJC 最长 200 分钟以上、几十架）说明第 3 步起不能整段作一个样本；切法按设计 §3.2（在场上飞机最少的时刻切开）
    到第 3 步再定。
- **执行器**（执行器设计 §11）：在新产物上规格不变（`0d6a68a92c6f`）；新训练集运行日上每机场 400 架、新验证集运行日上全部已标注航班，
  机场 × 进近方式每格都过 95 % 的门（验证集：自己的动力学 9,246 架，落地 99.9 %、词 98.0 %、evaluation 98.6 %）。

---

## 4 第三版 第 1 步：单机先验（比较规则 2026-09-25 跑之前写定）

**配置**：句子产物 `instruction_language/v4_20260924`（按运行日划分，规格 `145d6911e75b`）；每架航班单独一个场景（设计 §9 第 1 步）；
前 `N_look` = 8 行只观察，第一个预测步六列都要说；输入只用说这一步之前知道的东西（位置、由过去位置算出的运动、候选跑道的相对
位置与方向差、此前 30 分钟的落地情况——去掉测试日的落地、也去掉这架飞机自己的落地、已经说过的词）；模型每层 = 沿时间的因果
注意力 → 同一步各架飞机之间的注意力（本步只有一架）→ 前馈，d 192、4 层、6 头，330 万参数；训练设置同第二版（AdamW 3e-4，预热
500 步，每批约 16,384 步，验证集连续 3 个 epoch 不降就停，最多 30 个 epoch）。代码在分支 `dev-prior-v3`。

**三个变体**（`prior.data.VARIANTS`）：

| 变体 | 候选跑道带落地情况 | 各列按顺序输出 |
|---|---|---|
| full | 是 | 是 |
| no-context | 否 | 是 |
| unordered | 是 | 否 |

**规则**（`experiments/prior_select.py`）：

1. 领先者：三个变体（种子 1337）里，在内部选择集（9 个运行日，6,842 架）上每个预测步负对数似然最低的；
2. 种子线：full 在种子 1337 和种子 2024 上的这个数之差的绝对值；
3. 选：与领先者相差不超过种子线的变体里，按 no-context（输入最少）→ full → unordered（设计选的是按顺序输出：自由生成时同一步
   说出的几个词要配得上）的顺序取第一个。
4. 然后只在选中的那一个上读一次验证集，与从它自己的训练航班数出的两个基线、每个机场自己的跑道频率、B0 / B1 / B3 三条规则比。

**跑的顺序**（同一张显卡，一个接一个）：full（1337）→ no-context（1337）→ unordered（1337）→ full（2024）→ 选择与验证集读数。

**读法先说好的几条**：分列的指标（第一个预测步的前 1、换词的步上的 P(换)、前 1）在按顺序输出的变体里，后面几列用到了前面几列
这一步的真值（teacher forcing），与 unordered 不能逐列比；选择只看总的负对数似然。跑道列在第一个预测步之后从不换词（标注里没有），
它的"换词"指标为空。

**读数**（队列 2026-09-24 23:28 → 09-25 01:41 UTC，代码 `47c7b790` 干净，`outputs/POOLED/prior/v3_step1_20260924/`）：

- **三个变体与第二个种子**（内部选择集 6,842 架、130 万个预测步；每个约 33 分钟，28–29 个 epoch，第 25–26 个最好）：

  | 变体 | 种子 | 每个预测步 | 第一步之后 | 第一步跑道前 1 |
  |---|---|---|---|---|
  | **full** | 1337 | **0.2840** | 0.2576 | 87.5 % |
  | full | 2024 | 0.2838 | 0.2577 | 86.8 % |
  | no-context | 1337 | 0.2848 | 0.2577 | 81.5 % |
  | unordered | 1337 | 0.2978 | 0.2713 | 87.3 % |

- **选择**（按上面的规则，`choice.json`）：领先者 full（0.2840）；种子线 0.0002；no-context 差 0.0008、unordered 差 0.0138，都在线外 →
  **选 full**（候选跑道带落地情况、各列按顺序输出）。
- **读法**：
  - 落地情况几乎只影响第一步的跑道：去掉它，第一步之后的负对数似然一样（0.2577 对 0.2576），跑道前 1 却从 87.5 % 掉到 81.5 %
    （还没建立在五边上的 83.7 % → 76.1 %）。总的负对数似然对它不敏感（跑道只是每架约 190 步里的一步），规则仍然选了它，是因为
    种子线很窄；这一点以后比较输入时要记住：跑道要单独看。
  - 按顺序输出主要帮的是"什么时候换"：unordered 在换词的步上给"换"的概率，速度 0.25 → 0.12、下降角 0.38 → 0.27；跑道几乎不变。
    （分列的数在两类变体之间不能逐列比，见上。）
- **验证集读一次**（full 种子 1337，10,633 架、191 万个预测步）：每个预测步 **0.2643**（第一步之后 0.2375，困惑度 1.30），同口径基线
  "重复上一条" 0.8836、"只看上一个词" 0.6135。

  | 列 | 模型 | 重复上一条 | 只看上一个词 | 第一步前 1 | 之后换词的步：P(换) / 前 1 |
  |---|---|---|---|---|---|
  | 跑道 | 0.0018 | 0.0051 | 0.0051 | 86.6 % | —（第一步之后不换） |
  | 进近 | 0.0127 | 0.0284 | 0.0284 | 91.8 % | 0.28 / 100 % |
  | 航向 | 0.1211 | 0.6086 | 0.3490 | 71.7 % | 0.66 / 94.0 % |
  | 高度 | 0.0203 | 0.0438 | 0.0435 | 60.9 % | 0.48 / 76.3 % |
  | 下降角 | 0.0483 | 0.1018 | 0.0974 | 86.3 % | 0.39 / 73.8 % |
  | 速度 | 0.0603 | 0.0960 | 0.0903 | 31.5 % | 0.26 / 54.1 % |

- **第一个预测步的跑道**（验证集，与三条因果规则、机场自己的跑道频率比；"左右"= 方向对的航班里跑道也对的比例）：

  | | 全部 前 1 | 方向 | 左右 | 未建立（7,651 架）前 1 / 方向 / 左右 | 已建立（2,982 架）前 1 |
  |---|---|---|---|---|---|
  | **先验** | **86.6 %** | **99.1 %** | **87.4 %** | **81.5 % / 98.7 % / 82.5 %** | 99.8 % |
  | B0 机场最常用跑道 | 59.3 % | 77.1 % | 76.8 % | 48.7 % / 71.0 % / 68.6 % | 86.4 % |
  | B1 此前 30 分钟最多 | 75.1 % | 95.8 % | 78.4 % | 69.6 % / 94.4 % / 73.7 % | 89.2 % |
  | B3 同方向上一架 | 77.6 % | 96.5 % | 80.4 % | 72.1 % / 95.4 % / 75.6 % | 91.8 % |

  分机场（先验 / B3 / B1）：KMSY 99.6 / 97.7 / 97.5 %，KRDU 79.0 / 62.5 / 55.4 %，KSJC 95.5 / 92.6 / 95.7 %，KSMF 75.6 / 65.0 / 60.0 %，
  KSTL 90.1 / 84.7 / 84.5 %（KSTL 的 B0 只有 17.7 %：验证集那些天多数时候不用它最常用的跑道）。KSJC 与 B1 持平，其余机场都赢。B3 的
  证据：上下文落地里 97–99 % 有已知的进场方向；B3 在 12.6 % 的航班上退回 B1。
- **第 1 步的门**（设计 §9）：**过**。每一列都赢两个基线；还没建立在五边上的航班上，跑道赢 B1 和 B3，方向、左右两层都赢
  （方向 98.7 % 对 94.4 / 95.4 %，左右 82.5 % 对 73.7 / 75.6 %）。
- 这些数与第二版的 0.2542 不能直接比：划分（按运行日）、第一个预测步要说全部六列、输入只用过去、样本（验证集运行日）都变了。

---

## 5 第三版 单机自由生成（2026-09-25，设计 §9.1）

### 5.1 内部选择集

**配置**：先验 `v3_step1_20260924/full_s1337`（第 1 步选中的 full）；执行器规格 `executor/v7_20260925`（内容与 v5、v6 相同，
`0d6a68a92c6f`；执行器改成可逐步推进后重写，100 架回放与改前逐字节相同）；代码 `dev-prior-v3` `0bbe6abf`（干净）。内部选择集，每机场
400 架自己动力学的航班（种子 1337），每架从第 `N_look` = 8 行的观测状态起飞：先验说词 4 句（温度 1，逐列采样，词表相容规则作屏蔽，
已许可 / 已截获后跑道锁住），另用标注的词飞 1 次作对照。时间上限 = 观测剩余时间 × 1.5。27 分钟。产物
`outputs/POOLED/prior/v3_freegen_20260925/select_400x4/`（`generation.json` 每架每句一行，`sentences.npz` 先验说的词）。

- **结局**（先验 8,000 句；标注的词 2,000 次）：

  | 组 | 标注的词：落地 | 先验：落地 | 超时 | 越过入口时偏出跑道 | 越过入口时未截获 |
  |---|---|---|---|---|---|
  | 全部 | 100.0 % | **89.3 %** | 7.1 % | 3.4 % | 0.2 % |
  | 直线进近 | 100.0 % | 93.8 % | 3.4 % | 2.7 % | 0.1 % |
  | 被引导 | 99.9 % | 83.3 % | 12.0 % | 4.3 % | 0.4 % |
  | KMSY 直线 / 被引导 | 100 / 100 % | 93.8 / 79.6 % | 3.6 / 13.6 % | 2.5 / 6.4 % | |
  | KRDU 直线 / 被引导 | 100 / 100 % | 93.7 / 84.6 % | 3.2 / 11.8 % | 2.9 / 3.4 % | |
  | KSJC 直线 / 被引导 | 100 / 100 % | 99.1 / 79.5 % | 0.3 / 15.3 % | 0.7 / 5.2 % | |
  | KSMF 直线 / 被引导 | 100 / 100 % | 92.7 / 84.3 % | 4.5 / 11.2 % | 2.4 / 3.4 % | |
  | KSTL 直线 / 被引导 | 100 / 99.5 % | 87.3 / 86.9 % | 6.6 / 9.6 % | 6.1 / 3.5 % | |

  同一架航班的 4 句里落地几句：4 句 1,391 架、3 句 418、2 句 147、1 句 29、0 句 15。
- **说了什么**（第一步之后，每架每列的词数；先验只数到它那一句结束）：航向 19.4（标注 15.3；被引导的 39.1 对 32.5），高度 0.61（0.66），
  下降角 1.8（2.7），速度 1.9（1.9），进近 0.68（0.72）；换跑道、复飞几乎没有。**落地的那些句子航向词 15.4 个，没落地的 52.0 个**——
  失败的多是一直在给航向、绕来绕去直到超时。超时的句子里 55 % 结束时已许可（许可了却没截获），45 % 一直没许可。
- **跑道**：第一步所指跑道与观测一致 84.8 %（第 1 步读数里第一步跑道前 1 是 87 % 左右，这里是采样，不取最可能的）；指了别的跑道的
  那些句子照样 85 % 落地（与观测一致的 90 %）——换一条跑道本身不是失败的主因。
- **落地时刻**（落地的，减观测）：中位数 −4 s；p10 −64 s、p90 +57 s（标注的词：−31 / +17 s）——先验说词时路径的长短散得多。
- **词表规则学会了多少**：屏蔽之前，模型每步放在被禁组合上的概率，下降角列 0.06 %，进近列 0，跑道锁 0.00004 %——相容规则它自己
  基本学会了，屏蔽几乎不起作用。
- **读法**：用教师强制训练出来的先验，第一次自己说词、让执行器照着飞，89 % 落地；标注的词在同样条件下 100 %。差距几乎都在被
  引导的航班上（83 % 对直线 94 %），失败的样子是"说个不停的航向词 + 迟迟不许可"：执行器飞出的路径一旦和观测不一样，先验读到的
  就是它训练时没见过的位置，于是一直修正航向。这正是设计 §7 后训练第一步"闭环监督微调"要解决的问题（先验按执行器飞出的状态
  学说词）。
- **门**：先只记录（用户 2026-09-25，设计 §9.1）。若按"每格 ≥ 95 %"算，10 格里只有 KSJC 直线进近过（99.1 %）。

### 5.2 验证集（只读一次）

**配置**：与 §5.1 相同（先验、执行器规格、每机场 400 架 × 4 句、种子 1337），换成验证集；代码 `dev-two-tier` `4205e186`（用户合并后，干净）。
24 分钟。产物 `outputs/POOLED/prior/v3_freegen_20260925/val_400x4/`。

- **结局**（先验 8,000 句；标注的词 2,000 次）：

  | 组 | 标注的词：落地 | 先验：落地 | 超时 | 越过入口时偏出跑道 | 越过入口时未截获 |
  |---|---|---|---|---|---|
  | 全部 | 99.9 % | **90.2 %** | 6.1 % | 3.5 % | 0.2 % |
  | 直线进近 | 99.9 % | 94.0 % | 3.1 % | 2.8 % | 0.1 % |
  | 被引导 | 99.9 % | 83.3 % | 11.6 % | 4.8 % | 0.4 % |
  | KMSY 直线 / 被引导 | 99.5 / 100 % | 96.7 / 78.2 % | 1.5 / 14.1 % | 1.8 / 7.0 % | 0 / 0.7 % |
  | KRDU 直线 / 被引导 | 100 / 100 % | 90.9 / 83.5 % | 4.2 / 13.7 % | 4.7 / 2.6 % | 0.1 / 0.2 % |
  | KSJC 直线 / 被引导 | 100 / 98.2 % | 99.3 / 83.5 % | 0.2 / 12.9 % | 0.4 / 3.6 % | |
  | KSMF 直线 / 被引导 | 100 / 100 % | 93.5 / 85.8 % | 3.8 / 9.8 % | 2.5 / 3.8 % | 0.2 / 0.5 % |
  | KSTL 直线 / 被引导 | 100 / 100 % | 88.0 / 86.2 % | 6.6 / 8.4 % | 5.3 / 5.4 % | 0.1 / 0 % |

  另有 1 句入口前触地（0.01 %）。同一架航班的 4 句里落地几句：4 句 1,407 架、3 句 433、2 句 131、1 句 27、0 句 2。
- **说了什么**（第一步之后，每架每列的词数）：航向 15.8（标注 12.7；被引导的 37.4 对 31.4），高度 0.6（0.6），下降角 1.7（2.3），速度 1.7
  （1.6），进近 0.6（0.7）。落地的句子航向词 12.4 个，没落地的 47.0 个。超时的句子里 56 % 结束时已许可。
- **跑道**：第一步所指跑道与观测一致 85.0 %，落地的句子落在观测跑道上 85.4 %；第一步指了别的跑道的句子 87.1 % 落地（一致的 90.7 %）。
- **落地时刻**（落地的，减观测）：中位数 −4 s；p10 −55 s、p90 +45 s（标注的词：−30 / +16 s）。
- **词表规则**：屏蔽之前放在被禁组合上的概率，下降角列每步 0.06 %，进近列 0，跑道锁 0.00007 %。
- **读法**：与内部选择集一致（落地 90.2 % 对 89.3 %，被引导的都是 83.3 %），失败的样子也相同（说个不停的航向词、许可给得晚）。
  这是闭环监督微调（设计 §9.2）之前的对照：微调选定的那一轮在同一批航班、同一个种子上再读一次。
- **能否逐位重现**：§5.1、§5.2 都是先验每步把整段重新编码时跑的；之后先验改成逐行编码（设计 §9.2 的实现，概率相差约 1e-6），同一个
  模型在新代码上重读，个别采样可能翻转，结果不会逐位相同。所以微调前后的比较，在新代码上把微调前的模型也重读一次（同一批航班、
  同一个种子）。

## 6 第三版 单机闭环监督微调（CAT-K，2026-09-25，设计 §9.2）——失败，不采用

**结论：失败。** 验证集总落地率 90.2 → 93.1 %，但全部来自直线进近（94.0 → 99.7 %）；被引导的航班变差（83.3 → 81.0 %，偏出跑道翻倍），
航向词只有标注的一半，下降角、速度词多了四到五成，teacher forcing 负对数似然 0.284 → 0.452（8 轮后 1.114）——它学成了"早许可、少说话"，
离数据越来越远。失败在目标上：按时间对齐的标注，在离开了观测航迹的闭环轨迹上不成立（链 220 s 时偏 1 km）。设计里已不再有这一步
（设计 §9.2 只留"不采用"及原因），后训练改为按落地强化（设计 §9.3，读数 §7）。

**配置**：起点 `v3_step1_20260924/full_s1337`；执行器规格 `executor/v8_20260925`（内容 `0d6a68a92c6f`）；代码 `dev-prior-v3` `e760ab1d`（干净）；
每轮从训练集运行日抽每机场 800 架（共 4,000 架，每轮换种子，与前面各轮重复的第 8 轮已到 2,267 架），每架 4 个分支、每 10 步留下离观测
最近的一支；目标按设计 §9.2 的规则重新定（回应时间窗 5 行）；AdamW 1e-4、前 100 步预热；每轮在累积的全部链上过一遍；8 轮。选择集读数：
每机场 200 架 × 2 句，每轮同一批航班、同一个种子。13:10–17:31 UTC。产物 `outputs/POOLED/prior/v3_sft_20260925/`（`catk_s1337/` 各轮，
`val_kept_400x4/`，`val_before_400x4/`）。

- **各轮**（选择集）：

  | 轮 | 链落地 | 分支分开的段 | 选择集落地 | 直线 / 被引导 | 超时 | 偏出跑道 | teacher forcing 负对数似然 | 进近 / 航向 / 下降角列 | 每架航向 / 下降角 / 速度词 | 落在观测跑道 |
  |---|---|---|---|---|---|---|---|---|---|---|
  | 0 | — | — | 90.0 % | 94.2 / 84.3 % | 7.0 % | 2.8 % | 0.284 | 0.013 / 0.131 / 0.052 | 18.3 / 1.8 / 1.9 | 83.9 % |
  | 1 | 95.3 % | 51 % | 91.3 % | 99.5 / 80.3 % | 3.6 % | 4.6 % | 0.452 | 0.105 / 0.147 / 0.084 | 6.7 / 3.6 / 2.6 | 83.7 % |
  | 2 | 96.0 % | 54 % | 72.2 % | 97.6 / 37.5 % | 13.4 % | 14.1 % | 0.731 | 0.133 / 0.376 / 0.096 | 16.0 / 7.4 / 5.4 | 86.4 % |
  | 3 | 87.9 % | 73 % | 76.5 % | 97.6 / 47.9 % | 12.6 % | 10.7 % | 0.911 | 0.142 / 0.547 / 0.092 | 9.3 / 7.5 / 6.3 | 85.4 % |
  | 4 | 91.9 % | 74 % | 85.0 % | 98.3 / 66.9 % | 7.0 % | 7.8 % | 0.926 | 0.107 / 0.601 / 0.091 | 7.6 / 7.2 / 5.4 | 84.5 % |
  | 5 | 96.0 % | 76 % | 86.8 % | 98.7 / 70.6 % | 4.5 % | 8.6 % | 1.001 | 0.142 / 0.647 / 0.088 | 6.1 / 5.8 / 4.8 | 84.4 % |
  | 6 | 96.7 % | 71 % | 90.4 % | 99.1 / 78.5 % | 1.8 % | 7.6 % | 1.053 | 0.149 / 0.682 / 0.095 | 6.2 / 6.2 / 4.7 | 85.1 % |
  | 7 | 97.2 % | 74 % | 91.0 % | 98.8 / 80.4 % | 1.9 % | 7.0 % | 1.083 | 0.116 / 0.731 / 0.110 | 6.8 / 7.1 / 4.9 | 83.7 % |
  | 8 | 97.4 % | 76 % | 92.2 % | 98.9 / 83.2 % | 1.2 % | 6.5 % | 1.114 | 0.136 / 0.755 / 0.099 | 5.9 / 6.2 / 5.0 | 83.7 % |

  按规则（落地率最高 92.2 %，差 1.5 个百分点以内取最早）**选中第 1 轮**。
- **链离观测多远**（第 1 轮，留下的分支到观测航迹同一行的距离）：中位数 60 m（20 s）、257 m（60 s）、462 m（100 s）、696 m（140 s）、1,014 m（220 s）；
  最远的 10 %：128 m、627 m、1,231 m、1,940 m、2,815 m。每 20 s 从 4 个分支里挑 1 个，拉不回来。
- **目标的构成**（第 1 轮，第一步之后的列 × 步）：要求说标注的词（回应窗内）2.66 %、一直要求许可 1.11 %、交接时已许可而链未许可 0.10 %；
  已回应 10.64 %；错过太久不计损失 7.23 %；截获后的航向不要求 1.81 %；违反相容规则 0.01 %。
- **验证集**（只读一次，每机场 400 架 × 4 句；"微调前"是同一代码上的重读，与 §5.2 旧代码那次逐句完全相同）：

  | 组 | 微调前 | CAT-K 第 1 轮 | 其中超时 / 偏出跑道（CAT-K） |
  |---|---|---|---|
  | 全部 | 90.2 % | **93.1 %** | 3.1 / 3.4 % |
  | 直线进近 | 94.0 % | **99.7 %** | 0.2 / 0.1 % |
  | 被引导 | 83.3 % | **81.0 %** | 8.6 / 9.5 % |
  | KMSY 直线 / 被引导 | 96.7 / 78.2 % | 99.8 / 83.9 % | |
  | KRDU 直线 / 被引导 | 90.9 / 83.5 % | 99.7 / 79.2 % | |
  | KSJC 直线 / 被引导 | 99.3 / 83.5 % | 100.0 / 84.8 % | |
  | KSMF 直线 / 被引导 | 93.5 / 85.8 % | 99.2 / 77.5 % | |
  | KSTL 直线 / 被引导 | 88.0 / 86.2 % | 99.9 / 81.7 % | |

  每架每列说的词（第一步之后；标注：航向 12.7、下降角 2.3、速度 1.6）：微调前 航向 15.8、下降角 1.7、速度 1.7；CAT-K 航向 **5.6**（被引导
  的 15.2，标注 31.4）、下降角 3.3、速度 2.4。落地时刻减观测 p10 / p50 / p90：微调前 −55 / −4 / +45 s，CAT-K −50 / +2 / +47 s。第一步跑道与观测
  一致 85.0 % → 84.7 %。
- **读法**：CAT-K 在总数上高 2.9 个百分点，但全部来自直线进近——它学会了早许可、少说话；被引导的航班反而差了（偏出跑道翻倍），说的
  航向词只有标注的一半，下降角、速度词多了四到五成；teacher forcing 下离数据越来越远（负对数似然三轮翻到 3 倍多）。原因（设计 §9.2
  已写明规则）：目标是按时间对齐的标注，而链离观测越飞越远，标注"此刻许可 / 此刻转向"在链上的飞机那里往往不成立，模型学到的是"没
  许可就许可"这样的捷径；链上的航向词多，标注的航向事件常被算作已回应，于是被教着少说航向；下降角、速度词没被回应时连着 6 步要求说，
  于是被教着多说；只用链上的数据训练，没有东西把它拉回数据。
- **后续**：按落地强化没有等本节跑完，15:18 UTC 从微调前的模型起跑（那时第 1–4 轮没有一轮高出第 0 轮 1.5 个百分点以上，第 2–4 轮大幅
  变差）；本节跑完后确认不采用 CAT-K，设计 §9.3 的起点随之写定为微调前的模型。

## 7 第三版 按落地强化（单机，2026-09-25，后训练设计 §2）

**结论：有效，采用。** 验证集先验说词落地 90.2 → **97.2 %**；和 CAT-K 不同，提升不只来自直线进近：被引导的航班 83.3 → **93.9 %**
（CAT-K 81.0 %），超时 11.6 → 4.8 %、偏出跑道 4.8 → 1.0 %。说法没有离开数据：teacher forcing 负对数似然不变（0.284 → 0.285），被引导
航班每架的航向词 37.4 → 31.5（标注 31.4），第一步跑道、落在观测跑道上的比例、落地时刻都与 base 模型相同。选中的是第 1 轮；之后 7 轮
平在 95–96 %，没有更好。

**配置**：起点和拉回的对象都是 base 模型（`v3_step1_20260924/full_s1337`）；执行器规格 `executor/v8_20260925`（内容 `0d6a68a92c6f`，
`dev-prior-rl` 那版代码）；代码 `dev-prior-rl` `e8ee1373`（干净）；每轮从训练集运行日抽每机场 400 架（共 2,000 架，每轮换种子；第 8 轮时
与前面各轮重复的 683 架），每架 8 句；奖励、优势、损失按后训练设计 §2（β = 0.04，λ = 1，AdamW 1e-5，dropout 关），每轮在这一轮有对比的
航班的句子上过一遍；8 轮。选择集读数：每机场 200 架 × 2 句，每轮同一批航班、同一个种子（第 0 轮与 CAT-K 的第 0 轮逐位相同）。与 CAT-K
同时跑，15:18–20:08 UTC。产物 `outputs/POOLED/prior/v3_rl_20260925/`（`grpo_s1337/` 各轮与 `choice.json`，`val_kept_400x4/`）。

- **各轮**（选择集；"训练集生成"是这一轮开始时用上一轮的模型在训练集上说的 16,000 句）：

  | 轮 | 选择集落地 | 直线 / 被引导 | 超时 | 偏出跑道 | teacher forcing 负对数似然 | 进近 / 航向 / 下降角列 | 每架航向 / 下降角 / 速度词 | 落在观测跑道 | 落在反方向 | 训练集生成落地 | 有对比的航班 |
  |---|---|---|---|---|---|---|---|---|---|---|---|
  | 0 | 90.0 % | 94.2 / 84.3 % | 7.0 % | 2.8 % | 0.284 | 0.013 / 0.131 / 0.052 | 18.3 / 1.8 / 1.9 | 83.9 % | 0.4 % | — | — |
  | 1 | **96.5 %** | 98.6 / 93.5 % | 2.7 % | 0.5 % | 0.285 | 0.013 / 0.129 / 0.051 | 15.4 / 1.9 / 1.7 | 84.4 % | 0.6 % | 90.0 % | 870 |
  | 2 | 95.3 % | 97.8 / 92.0 % | 3.5 % | 0.9 % | 0.282 | 0.013 / 0.129 / 0.051 | 16.5 / 1.9 / 1.8 | 84.7 % | 0.5 % | 96.8 % | 343 |
  | 3 | 95.5 % | 98.2 / 92.0 % | 3.5 % | 0.9 % | 0.281 | 0.013 / 0.129 / 0.051 | 16.4 / 1.9 / 1.8 | 85.7 % | 0.7 % | 96.7 % | 371 |
  | 4 | 95.1 % | 98.0 / 91.1 % | 3.6 % | 1.2 % | 0.281 | 0.013 / 0.129 / 0.051 | 16.4 / 1.8 / 1.7 | 84.4 % | 0.7 % | 96.3 % | 393 |
  | 5 | 95.2 % | 98.2 / 91.1 % | 3.3 % | 1.4 % | 0.280 | 0.013 / 0.129 / 0.051 | 16.2 / 1.8 / 1.8 | 83.1 % | 0.4 % | 95.8 % | 449 |
  | 6 | 96.0 % | 98.4 / 92.7 % | 2.9 % | 0.9 % | 0.280 | 0.013 / 0.129 / 0.051 | 16.6 / 1.7 / 1.8 | 83.2 % | 0.6 % | 96.1 % | 413 |
  | 7 | 96.0 % | 98.3 / 93.0 % | 2.9 % | 0.9 % | 0.280 | 0.013 / 0.129 / 0.051 | 16.0 / 1.8 / 1.7 | 84.0 % | 0.5 % | 96.0 % | 436 |
  | 8 | 95.1 % | 97.9 / 91.3 % | 3.2 % | 1.6 % | 0.280 | 0.013 / 0.129 / 0.051 | 16.4 / 1.7 / 1.8 | 83.9 % | 0.5 % | 96.2 % | 412 |

  护栏一轮都没排除；按规则（落地率最高 96.5 %，差 1.5 个百分点以内取最早）**选中第 1 轮**。每轮的训练只过一遍（第 1 轮 201 批、约
  30 s），时间几乎全花在自由生成上（每轮约 30–40 分钟）。
- **验证集**（只读一次，每机场 400 架 × 4 句，与 §5.2 同一批航班、同一个种子；"base 模型"是 §6 在同一代码上的重读）：

  | 组 | base 模型 | CAT-K 第 1 轮 | **按落地强化 第 1 轮** | 其中超时 / 偏出跑道（按落地强化） |
  |---|---|---|---|---|
  | 全部 | 90.2 % | 93.1 % | **97.2 %** | 2.2 / 0.4 % |
  | 直线进近 | 94.0 % | 99.7 % | **99.0 %** | 0.8 / 0.1 % |
  | 被引导 | 83.3 % | 81.0 % | **93.9 %** | 4.8 / 1.0 % |
  | KMSY 直线 / 被引导 | 96.7 / 78.2 % | 99.8 / 83.9 % | 99.3 / 89.8 % | |
  | KRDU 直线 / 被引导 | 90.9 / 83.5 % | 99.7 / 79.2 % | 99.3 / 93.8 % | |
  | KSJC 直线 / 被引导 | 99.3 / 83.5 % | 100.0 / 84.8 % | 99.6 / 96.0 % | |
  | KSMF 直线 / 被引导 | 93.5 / 85.8 % | 99.2 / 77.5 % | 97.7 / 95.6 % | |
  | KSTL 直线 / 被引导 | 88.0 / 86.2 % | 99.9 / 81.7 % | 98.8 / 96.5 % | |

  每架每列说的词（第一步之后；标注：航向 12.7、下降角 2.3、速度 1.6；被引导的航向 31.4）：base 模型 航向 15.8（被引导 37.4）、下降角
  1.7、速度 1.7；按落地强化 航向 **13.0**（被引导 **31.5**）、下降角 1.8、速度 1.6。第一步跑道与观测一致 85.0 → 84.6 %，落在观测跑道上
  85.4 → 84.8 %；落地时刻减观测 p10 / p50 / p90：base 模型 −55 / −4 / +45 s，按落地强化 −53 / −4 / +46 s；结束时已许可 97.3 → 99.2 %。
  每架 4 句里落地的句数：base 模型 4 句都落地 1,407 架、一句都没落地 2 架；按落地强化 1,818 架、3 架。
- **读法**：
  - 奖励只看结局，却正好改掉了自由生成的两种失败——一直给航向、迟迟不许可直到超时（被引导的超时 11.6 → 4.8 %），以及切不上航道偏出
    跑道（4.8 → 1.0 %）。被引导航班的航向词从比标注多两成降到与标注相同。
  - 数据项守住了说法：teacher forcing 负对数似然每轮都在 0.280–0.285，第一步跑道、落在观测跑道上的比例、落地时刻都没动。与 CAT-K 对比，
    CAT-K 同样在总数上提高，但靠的是早许可、少说话，并且离数据越来越远（§6）。
  - 第 1 轮之后就平了：训练集上生成的句子第 2 轮起就有约 96–97 % 落地，有对比的航班从 870 架降到约 400 架，每轮能学的信号少了一半多；
    剩下的是 8 句里只偶尔失败的航班和少数怎么说都不落地的航班（每轮 7–15 架 8 句全失败）。
  - 验证集总数比选择集高（97.2 % 对 96.5 %），因为验证集里直线进近占 65 %（选择集 58 %）；分组看两者接近（直线 99.0 对 98.6 %，被引导
    93.9 对 93.5 %）。验证集只读了这一次。

## 8 后训练第二阶段 第 0 步：程序高度下限在标注数据上（2026-09-25）

后训练设计 §3.6：屏蔽上线之前，先看标注的词和真实的航迹满不满足它。

### 8.1 开发测量（内部选择集，决定只留下滑道下沿）

**配置**：代码在 `dev-post-train`（未提交、未审查，所以不是正式读数）：`prior/procedure.py` 的第一版（RNAV 在 FAF 以外的"不低于"阶梯、
过渡段、FAF 以内下滑道 − 60 m，三种划法）和 runner `prior_procedure_check`；执行器规格 `executor/v7_20260925`。选择集 6,842 架标注航班
全部；执行器重飞：每机场 200 架有自己动力学的航班（共 1,000 架，种子 1337），照标注的词从第一个预测步飞。词的容差 15 m，航迹检查的容差
40 m；标注的高度词只数先验会说它的地方（第一个预测步生效的那个，和之后每个说出的）。

- **三种划法都远过不了 1 % 的线**：

  | 划法 | 标注高度词被屏蔽（平飞 / 下降至落地） | 观测航迹低于下限（起点 / 之后） | 重飞被结束（其中落了地） |
  |---|---|---|---|
  | 更严：只按沿航道距离 | 14.7 %（808 / 5,228、960 / 6,833） | 527 / 1,270 架 | 14.1 %（全部） |
  | 按程序：离航段 1 NM 以内 | 12.1 %（501 / 5,228、960 / 6,833） | 472 / 1,144 架 | 13.4 %（全部） |
  | 只对已许可的飞机 | 9.7 %（210 / 5,228、960 / 6,833） | 220 / 635 架 | 10.0 %（全部） |

- **按部件看**（按程序的划法，观测航迹在起点之后低于下限 40 m 以上的航班，共 6,842 架）：

  | 下限来自 | 航班 | 其中已许可 | 低多少（中位数） |
  |---|---|---|---|
  | FAF 以内的下滑道下沿 | 58（0.8 %） | 26 | 72 m |
  | IF→FAF 段的 FAF 高度 | 321（4.7 %） | 170 | 54 m |
  | IF 和更外的定位点 | 110（1.6 %） | 47 | 113 m |
  | 过渡段 | 850（12.4 %） | 421 | 235 m |

  过渡段的违反集中在 KRDU（23R 799 架里 337 架、05L 634 架里 275 架）。"下降至落地"被屏蔽的 960 个词里，921 个卡在 FAF 高度的直线检查上：
  说的地方离入口中位数 20 km、比下滑道低 86 m（p10 −248 m），一半是第一个预测步就生效的词。
- **只留下滑道下沿**（FAF 以内、LPV 扇形里，下滑道 − 60 m）：观测航迹 58 架（0.85 %；其中 37 架是 KRDU 32——公布的是 3.5° 的 LNAV/VNAV 路径，
  它的 245 架里 15 %；其余 0.3 %）；平飞高度词被屏蔽 12 / 5,228（0.2 %）；"下降至落地"若保留直线预判仍屏蔽 284 / 6,833（4.2 %），只查此刻
  是否已低于下沿则约为 0；重飞被结束 22 / 1,000（2.2 %，都落了地，16 架在被引导的航班上）。这 22 架都在"下降至落地"下，离入口 7.6–15 km，
  比下滑道低 100–380 m，而同一距离上观测的飞机在下滑道 ±30 m 以内或高得多——是执行器飞"下降至落地"的方式，不是数据违规。
- **读法**：记录里的飞机多数由雷达引导，按 MVA 飞，比 RNAV 过渡段和 IF 的高度低；有垂直引导的进近从下滑道下方切入、过 FAF 时低于 FAF 高度
  也常见。RNAV 在 FAF 以外的高度只约束沿程序飞的飞机，数据里分不出谁在沿程序飞，当硬约束会把模型从真实飞法推开。FAF 以内的下滑道下沿与
  数据相符。
- **决定**（用户 2026-09-25）：只保留 FAF 以内的下滑道下沿；"下降至落地"不做直线预判；执行器的下沉照样结束、训练里罚，重飞的通过线放到
  3 %。设计已按此改写（后训练设计 §3、§7、§8）。正式测量在训练集上做（§8.2，跑完再写）。
